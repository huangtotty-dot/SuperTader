# 2026-09-04 W35-W37 周复盘 · 修复施工方案

> 输入：`doc/review/weeklyReview/2026-W35-W37_周复盘.md` + 三路代码勘查（行号基于当前工作区 main.py 3083 行 / position_builder.py 2031 行）
> 分层原则：**A 层=bug/结构修复**（不占验证管线配额，修完即验）；**B 层=参数/条件变更**（走预注册闸门）；**C 层=owner 执行/拍板项**
> 勘查修正两处周复盘初判：①"sizing 不识别入金"→ 实为仓控无资金腿（设计如此）+ t_qty 未联动；②"veto 未接入门控"→ 实为 16:00 后 eod 重扫用陈旧日线（veto 盘中一直在拦，46 次成功）

---

## A 层 · 修复施工清单（按依赖排序，周末施工）

### F-1 【最高优先】陈旧日线 fail-closed（688037 伪信号 + tracker 污染共同上游）

- **问题**：`core/market_data/facade.py:85` forming bar 补充窗口 09:15-16:00，16:00 后 gm 日线仍无当日 bar → eod 重扫用陈旧特征判出 signal（688037 用 08-31 收盘价 370 判定，真实收盘 342）；指数同源（16:02 记录 index close=3986.30=08-31 收盘）。
- **施工点**：
  1. `core/market_data/facade.py:85`：forming 窗口延至 23:59（盘后快照 ts_date 闸 :91 已防伪造 bar）
  2. `core/build_decision.py:67-75 features_from_daily`：增加返回 `last_bar_date`；`core/position_builder.py:1016 _timing_verdict` 校验末 bar 日期 ≥ 当日，陈旧 → 走 `data_insufficient`（verdict=insufficient_data，**fail-closed 不判 signal**）
- **验收**：16:05 模拟时点回放 09-01 eod 扫描，688037 不得出 signal；`scripts/build_verdict_parity.py` 双侧一致；trace 每行 conditions 恒含 t_veto 键（配合 F-1b）
- **F-1b 连带**：`position_builder.py:1043-1046` t_veto 移出 `if _vetoes:`，恒写 `{"passed": bool, "detail": ...}`

### F-2 tracker 聚合口径修正 + 补采重跑

- **问题**：`t_io/validation/signal_outcome_tracker.py:183-188 load_final_verdicts()` 按 (code,date) 取 scan_time 最大一条（last-scan-wins），09-01 盘中 signal 600362/600900 因日内确认门降级后，16:04 陈旧重扫定稿为 approaching → 漏入 signal 池；且手工 patch JSON 会被每日重跑冲掉，必须改代码。
- **施工点**：`load_final_verdicts` 聚合策略改 **verdict 优先级（signal>watch>approaching>weak）+ 同级取首次触发行**；price/scan_time 用首次 signal 触发值；原最终 verdict 保留为新字段 `final_verdict`。F-1 落地前，对 scan_time>16:00 的行剔除或标记。
- **验收**：重跑 `python t_io/validation/signal_outcome_tracker.py` 后 09-01 signal 池=5 只（600362@49.47 09:30:49 / 600900@28.05 09:35:36 入池，T+1/T+3 可结算）；688037 条目在 F-1 后消失或标 stale

### F-3 eod_sync 尾部二次归账（002451 qty 分叉 4200 案）

- **问题（结构性）**：归账内嵌在 `_maybe_audit_closure`（main.py:1490，14:50-15:05 窗口一次性跑），审计自己建议的尾盘处置（14:51 TAIL 卖 1400）必然落在归账之后；盘后无任何进程再消费 bridge fill。
- **施工点**：`main.py:1490-1677` 拆出归账；新增 **15:02-15:05 第二次"尾部归账"**：重读 events jsonl，过滤 `fill.time > audit_ts`，净额经 `src/holdings_sync.py:40 apply_eod_sync` 冲减，closure_audit.jsonl 追加 `phase="tail_reconcile"` 记录。
- **连带**：`t_io/validation/daily_review/daily_review.py:177-184` 改"取最后一条"为"首条+tail_reconcile 增量叠加"（Q-20260904-6 同源修复）
- **验收**：回放 09-04 events+VT，二次归账后 002451 qty=审计值−1400；单测覆盖"14:50 后成交"场景（参照 `t_io/validation/test_holdings_sync_invariant.py`）
- ⚠️ 前置：周一 reconcile 先用截图裁定 002451 正确基数（含 VT 卖 400 口径=2800 / bridge-only=3200），再修代码

### F-4 Renko 门控 last_down 语义 bug（"等Renko向下砖(当前down)" 矛盾 158 次）

- **问题**：`core/t_decision.py:182-193` 的 `last_down` 每次 evaluate() 重置，仅"本 tick 新出向下砖"为 True；买入门控 :252 `last_down and m15>0` 要求向下砖与 MACD 金叉同 tick 发生——价格沿向下砖横盘后 MACD 再转正的买点会永久错过（09-04 实测侥幸无损：全天 m15>0 次数=0）。
- **施工点**：`core/t_decision.py:182-193/252/279-282`：改为处理完新增行后取 `builder.brick_direction=="down"`（最新砖方向）供门控与文案共用；"新砖 tick"语义仅保留给需要触发沿的场合。
- **验收**：单测"向下砖形成后第 N tick MACD 转正 → 出 BUY_LOW"；09-03/09-04 回放对比信号数差异（回归风险：`replay_day.py`/`t_engine_auto` 共用此引擎）；重放后矛盾文案出现次数=0
- **注意**：此项会改变信号产出，属逻辑 bug 修复而非改参，但上线前必须用闸门 A 的决赛窗口顺带跑对照，确认不与"14:00 后禁新开"叠加出意外

### F-5 sizing 仓控可观测性（归因透出 + pushed 语义拆分）

- **问题**：9 笔历史记录 pushed=true 但 qty=0；trace `max_buyable` 不含 index_factor 钳制（09-04 10:57 002451 落盘 400 实为 0）；拦截原因全部笼统写"满仓/熔断"。
- **施工点**（勘查1 S1~S5）：
  1. `core/position_sizer.py:239/251/258/271/276/283/296`：calc_buy_qty 各早退点返回原因码（`no_t_budget/full_position/single_stock_cap/index_circuit/index_target_cap_clamp/timing_gate_nogo`）
  2. `main.py:2181`：trace `max_buyable` 改钳制后真实值；`main.py:2319-2326`：`_advice` 增加 `block_reason` + `executable = suggested_qty>0`；`main.py:2202/2322`：pushed（飞书已推）与 executable（可执行）拆分落盘
  3. 复盘统计口径同步改（daily_review.py:153 正则）
- **验收**：三例单测（no_t_budget/full_position/index_target_cap_clamp）；回放 09-04，新 trace 逐笔 block_reason 与勘查归因表一致；`test_w33_b2_single_tier.py` 回归全绿
- **策略拍板项（不在本次施工）**：是否给买入侧接资金腿（读 available 现金）——当前体系刻意用 t_qty 股数预算隔离现金管理，勘查建议周复盘拍板后再议；若接，需配套 reconcile 入金联动 t_qty + 失效 `_TOTAL_EQUITY_CACHE`（main.py:1092 的 300s 缓存）

### F-6 ETF 日线数据链可观测性（588170「timing features 为空」）

- **问题**：gm 对 ETF 日线疑静默返空不打日志（`gm_provider.py:44-45`）→ 588170 实质单点依赖腾讯；腾讯缓存新鲜度闸（`tencent_provider.py:112`）早盘拒昨日缓存，每次扫描硬依赖实时接口，11:00-11:26 抖动即空特征；兜底分支（:173-188）裸 except 吞异常；附带 bug：兜底会产生重复日期行（实测 347 行含两行 09-04）。
- **施工点**：① `facade.py:64-72` gm 返空补 warning 日志；② `tencent_provider.py:173-188` 兜底加日志 + 修重复日期（追加 forming 前判 `rows[-1]['date'] != today`）；③（可选加固）:112 盘中缓存闸放宽为"昨日缓存可用但标 stale"
- **验收**：单测"gm 空 + 腾讯 501 + 有昨日缓存"三条件下 facade.daily 返回非空且 source=cache；gm 环境直验 `GmProvider().daily('588170')`（需自动盘终端）

### F-7 C18 建仓 signal 待确认清单推送（强制上线项，拍板 17 天）

- **现状澄清**：逐股即时推送已存在（position_builder.py:1892-1910 → push_signal_feishu :1221）；真正缺的是**收盘待确认清单**——盘后汇总卡（push_summary_feishu :1320）只反映最后一扫 verdict，盘中触发后降级的（600362/600900 类）从卡上消失。
- **施工点**：`main.py:1328-1331 _maybe_run_position_builder` 内 `_push_summary_feishu` 之后新增 `push_pending_confirm_list(today)`；新函数落 `core/position_builder.py`（仿 :1320），数据源读 `t_io/logs/manual_signals_{date}.jsonl` 的 build_signal 事件（:1956-1969，含盘中+eod 全量触发），按 code 去重取当日最高 verdict，标注"已即时推送/未推送"+ veto 状态；独立 dedup 键 `c18_list:{date}`；0 只日推"0 只待确认"卡（与 0 signal 计数器联动）
- **验收**：09-01 回放产出含 600362/600900/601318/601628/688037 共 5 只的清单卡

### F-8 15:10 自动复盘推飞书（W-20260904-1，owner 已拍板）

- **施工点**：推送放 `t_io/validation/daily_review/daily_review.py` 文件最末（:761，signal_outcomes 结算后）——子进程 fire-and-forget，main.py 轮询不可取。注意该脚本无 BASE 在 sys.path，需先 insert 再 `from config import send_feishu_payload`（config.py:141）；卡片构造函数内联简版（import main 代价大）；幂等去重写 `t_io/state/review_pushed.json` 按日标记
- **内容**：kpi 摘要（信号数/closed_loop/audit_problems）+ tracker 样本数 + 报告路径
- **验收**：`python t_io/validation/daily_review/daily_review.py --date 2026-09-04` 手动重放即时验证；次交易日 15:10 实盘确认群收卡

### F-9 bridge fill 补 fee 字段（L3 硬条件 4 前置）

- **施工点**：`execution/auto/gm_main.py:2039`（on_order_status 全成时）取 `order.get("filled_commission")`，None 回退估算并标 `fee_source:"gm"|"estimated"`；顺带补传 order_id（当前 events 恒空串）；`execution/auto/_gm/gm_bridge/writer.py:134-146 write_fill` 追加 fee/fee_source 两字段（append-only，消费端 .get 兜底）
- **消费端跟进**：main.py:1556 闭环审计 compute_t0_pnl 的 auto fill 腿改用实收费用；watcher.py:271-282 fill 推送文案加费用
- **前置验证（掘金终端一行）**：`filled_commission` 字段名属文档级认知，本机无 gm 环境，需先在终端 `dir(order)` 实证
- **验收**：下一笔真实 fill 的 events 行 fee 与终端对账单误差 <0.01

### F-10 焦点名单停用改造（Q-20260903-7，owner 已拍板废弃）

- **施工点**：preopen 焦点名单产出链路停用（focus_codes/active_codes/theme_rank 相关），竞价 akshare SSL 依赖随之一并评估是否还修（09-04 盘尾已自愈，SSL 为外部不稳定因素）
- **验收**：preopen_trace 不再含焦点名单字段或恒空且无降级告警噪音

### F-11 拦截可见性漏发排查（Q-20260904-5）

- **问题**：09-04 四笔共振拦截中三笔发了 🔕 通知，10:57:47 002451 那笔漏发
- **施工点**：排查拦截通知触发链（🔕 推送的调用点与条件），补覆盖
- **验收**：回放 09-04，四笔拦截四条通知

---

## B 层 · 验证管线执行（预注册闸门已写入周复盘 §5）

| 项 | 内容 | 本周末动作 |
|---|---|---|
| 闸门 A | Q-20260903-2「14:00 后禁新开买入」 | 执行全管线决赛：harness_backtest 生产口径 vs 候选口径，90 日窗口；验收线=总收益改善≥+3pp / 上午胜率≥70% 不回归 / 强平占比<45% / 止盈误伤≤10% |
| 闸门 B | C20+C28 基调模型双条件与门 | 回放 4 日背离案例+近 20 日；验收线=背离≤1 次/周、无新增误判、单边下行日不漏报 |

⚠️ 两项均为改参，**破闸即放弃回退，不强行上线**；F-4（Renko 门控 bug）与闸门 A 同引擎，决赛窗口需跑"F-4 单独 / 闸门A 单独 / 两者叠加"三组对照防交互意外。

## C 层 · owner 执行/拍板项

| # | 事项 | 时点 |
|---|---|---|
| C-1 | 周一 reconcile 裁定 002451 正确基数（2800 vs 3200 口径差=VT 卖 400 是否已在基数内） | 09-07 盘前 |
| C-2 | 600481 缺口 5500 买入接回（已拍板；2800 挂单 target 4.20 于 09-08 到期，另 2700 状态待查）→ 09-04 owner 确认执行口径：开盘后先确认 2800 挂单状态，缺口 5,500 以 4.20 附近限价接回；顺带核对台账 cost=28.216 脏数据 | 09-07 盘中 |
| C-3 | 588170 t_qty 是否扩 → ✅ 09-04 owner 拍板扩至 30%，t_qty 4900→13,350 已落 holdings.json | 已执行 |
| C-4 | F-5 资金腿策略拍板 → ✅ 09-04 拍板：暂不接入，维持纯股数预算；待 F-5 归因透出积累实盘 trace 后下周复盘带数据再议 | 下周复盘 |
| C-5 | P4 四件（回放重跑/GUI 目视/新终端复跑/否决因子实运行）——L1 启动前置，三周零进展 | 尽快 |
| C-6 | F-9 前置：掘金终端验证 `filled_commission` 字段 | 施工 F-9 前 |

## 施工顺序建议（周末）

```
F-1（陈旧日线 fail-closed）──► F-2（tracker 重跑补采，依赖 F-1 口径）
F-3（尾部归账，待周一 C-1 裁定后收尾验收）
F-4（Renko 门控 bug）──► 与闸门 A 决赛联动对照
F-5 / F-6（可并行）
F-7 / F-8（可并行，推送类低风险）
F-9（待 C-6 终端验证后）
F-10 / F-11（低优先扫尾）
闸门 A/B 决赛执行
```

**周一（09-07）开盘前必须完成**：F-1、F-3（至少尾部归账）、F-7、F-8——分别对应"周末不再产出伪 signal"、"台账周一不再分叉"、"signal 待确认可见"、"复盘推送生效"四个运营承诺。
