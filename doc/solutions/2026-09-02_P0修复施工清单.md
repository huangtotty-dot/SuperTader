# 2026-09-02 P0 修复施工清单

> 依据：复盘清单 V1.3 铁律——每日只记录不改参，**唯一例外：P0 级故障当日处理**。
> 来源：`doc/review/日复盘/2026-09-02_复盘.md` 异常表 ① 类 4 项。全部零阈值/参数变更；逐项独立 commit；次日复盘按清单末节逐项验证。
> 今日好消息：昨日方案 8 项已全部施工，3 项运行验证通过；本清单处理剩余 2 项未闭环 + 今日新增 2 项。

## 总览

| 序 | 候选编号 | 问题 | 根因一句话 | 改动面 | 预计工时 |
|---|---|---|---|---|---|
| T-1 | Q-20260902-A1 | 收盘自动复盘 SyntaxError 首战崩溃 | main.py 内嵌 `python -c` 分号后接 for 循环 | main.py 1 处 | 10 分钟 |
| T-2 | Q-20260902-A2 | 分钟快照"修复后仍 0 文件" | **假象**：数据写到了仓库外 `E:\t_io\`（exec 加载器 `__file__` 不重定向雷） | 3 文件各 1 行 + 数据回收 | 30 分钟 |
| T-3 | Q-20260902-A3 | 大盘基调推送缺失 + 竞价断档 | 进程 09:32 才启动（P3-2 冲突 fail-closed 退出所致），推送逻辑硬窗口无迟到补发 | main.py +15 行 / preopen.py +8 行 + 计划任务 | 40 分钟 |
| T-4 | Q-20260902-A4 | 002409 池分管冲突 ERROR（今晨 reconcile 引入） | reconcile 写 holdings pool=both 但未同步 watchlist_buy.json（缺省=manual） | 数据已盘中补正 ✓；防复发改 1 处 | 20 分钟 |

---

## T-1 收盘自动复盘 SyntaxError（Q-20260902-A1，对应昨日方案 P0-7 收尾）

- **根因**：commit `e29768b8` 在 main.py 新增的 `_maybe_run_daily_review` 用 `python -c` 单行内嵌命令，分号后接 `for` 循环（语法非法），子进程启动即崩（`t_io/logs/daily_review_2026-09-02.log` 全文仅一条 SyntaxError）。触发链路本身 ✅（15:10:07 如期拉起）。
- **修复动作**：把内嵌 `-c` 命令改为直接顺序调用两个脚本文件（同一子进程内 `daily_review.py --date {today}` 再 `forward_tracker.py --date {today}`），或把 -c 内容落为临时脚本文件再执行。推荐前者，改动最小。
- **验证**：手动触发一次（或等明日 15:10），确认 `daily_review_{date}.json` + `kpi_{date}.json` 自动产出、`signal_outcomes.json` generated_at=当日、日志无 SyntaxError。
- **风险**：极低，只动子进程命令构造。

## T-2 分钟快照路径重定向（Q-20260902-A2，对应昨日方案 P0-1 收尾）

- **根因（调查修正复盘口径）**：快照**今天全部写成功了**（8 只持仓股齐全），但落到了 `E:\t_io\minute_snapshots\2026\09\`（仓库外）。机制：main.py 的 exec 加载器（`main.py:88-99`）只切 `__name__` 不切 `__file__`，`core/utils.py:12` 的 `Path(__file__).resolve().parents[1]` 因此算到 `E:\`。昨日 a507a8e6 的 `default=str` 修复是真实有效的——两层 bug 叠加造成"修了没用"的假象。同模式受害：`execution/preopen.py:26`、`execution/auction_analyzer.py:33`（preopen 自 08-30 起已被重定向）。
- **修复动作（保守方案 B，今夜可施工）**：
  1. `core/utils.py:12` → `BASE = Path(globals().get("BASE_DIR") or Path(__file__).resolve().parents[1])`（复用 index_regime/daily_sentiment 已生产验证的免疫模式）；
  2. 同法修 `execution/preopen.py:26`、`execution/auction_analyzer.py:33`；
  3. **数据回收**：`E:\t_io\minute_snapshots\2026\09\` 8 个文件 + `E:\t_io\preopen\preopen_2026-08-30/31.json` 拷回仓库对应目录（拷回不移动，仓库外残留另清）；
  4. 给 exec 加载器立 backlog 项：`main.py:98` 补 `shared['__file__'] = mod_path` 是治本方，但需同步修 `core/signal_engine.py:27` 的"歪打正着"路径（防回归），留周六。
- **验证**：静态预检（模拟 exec 加载后断言 `_snapshot_file()` 路径以 `E:\superTrader` 开头）；明日首轮扫描后仓库内 `t_io/minute_snapshots/2026/09/03/` 出现 8 文件、`E:\t_io\` 无新增。
- **风险**：低；不改 signal_engine 则无回归点。

## T-3 大盘基调迟到补发 + 进程拉起保障（Q-20260902-A3）

- **根因**：`_maybe_push_index_regime_morning`（`main.py:970`）硬窗口 `09:26-09:31`，窗口外直接 return、无补发分支；今日进程因 P3-2 冲突 fail-closed 退出（09:22:48）、人工修复后 09:32:55 才重启 → 基调推送 + preopen 支撑压力位推送（`execution/preopen.py:521`，窗口 09:25-09:30）双双错过，竞价 09:20/09:22 断档。
- **修复动作**：
  1. `main.py:970` 窗口判断后加迟到补发分支：`09:31 < t ≤ 上午收盘` 且当日未推 → 仍推送，卡片加一行「⚠️ 迟到补发（进程晚于推送窗口启动）」；
  2. `execution/preopen.py:512` `_maybe_push_pivot_report` 同法加补发分支；
  3. **流程层（owner 配合项）**：建 Windows 计划任务每日 09:10 拉起 main.py（留 10 分钟余量覆盖启动失败人工处置），配"失败 1 分钟重试"；fail-closed 设计本身正确，不改为 fail-open；
  4. 可选：`_index_regime_morning_pushed_date` 内存态改落盘 `t_io/state/`（消除"推送后崩溃重启重推"的既有小缺陷）。
- **验证**：mock `now=09:33` 调函数断言补发分支触发且含迟到标注；`now=09:27` 正常路径回归；次日 09:15 前日志首行 ≤09:12。
- **风险**：低；迟到时 C20 校验可能读到回填口径竞价档，已有降级分支兜底。

## T-4 池冲突防复发（Q-20260902-A4）

- **根因**：P3-2 检测（`config/auto_pool.py:59-77`）交叉判定——watchlist_buy.json 侧缺省 manual ∧ holdings 侧 pool∈{auto,both} = 冲突。600176/000988 当年走 GUI `add_auto_stock`（唯一双写同步入口）；002409 由 `add_to_watchlist` 建条目（**不写 pool 字段**）+ 今晨 reconcile 直写 holdings pool=both → 冲突。**数据已盘中补正**（watchlist 侧 002409→auto，自检 `conflicts: []` ✓）。
- **修复动作**：
  1. reconcile 写入 holdings 时加同步兜底：凡置 `pool ∈ {auto,both}`，同步把 watchlist_buy.json 该码 pool 置 auto（抽 `t_gui.py:3381-3393` 逻辑为 `src/holdings_repo.py` 公共函数共用）；
  2. 防御性：`t_gui.py:3843 add_to_watchlist` 新建条目时若非 manual 池票则直接写 `pool="auto"`，消除"缺省 manual"陷阱。
- **验证**：`python config/auto_pool.py` → `conflicts: []`；模拟复发（watchlist 改回 manual）自检能报出；次日启动无 P3-2 ERROR。
- **风险**：低；注意同步方向必须与 `add_auto_stock` 行为严格一致。

---

## 施工顺序与 commit 计划

1. T-1（10 分钟，立即见效）→ `fix(review): 收盘自动复盘子进程改脚本直调（修 SyntaxError）`
2. T-4（20 分钟，防明日再炸启动）→ `fix(pool): reconcile/add_to_watchlist 同步 watchlist pool 防 P3-2 冲突`
3. T-2（30 分钟，含数据回收）→ `fix(snapshots): exec 加载下 BASE 路径重定向——BASE_DIR 回落×3 + 仓库外数据回收`
4. T-3（40 分钟）→ `feat(push): 早盘基调/支撑压力位迟到补发分支`

## 次日（09-03）复盘验证清单

- [ ] 15:10 自动复盘产出 `daily_review_2026-09-03.json` + generated_at=当日（T-1）
- [ ] 仓库内 `t_io/minute_snapshots/2026/09/03/` 8 文件、`E:\t_io\` 无新增（T-2）
- [ ] 09:15 前进程在线（计划任务）；基调推送正常或带迟到标注（T-3）
- [ ] 启动日志无 P3-2 ERROR（T-4）

## 明确不在今日清单

- preopen 焦点名单恢复 vs 废弃 —— **等 owner 拍板**（已空 43 天）
- exec 加载器治本（`__file__` 切换 + signal_engine 连带修）—— 周六 backlog
- 002639 涨停预警（Q-20260902-B1）、600176 保护链核查（B2）、Renko 数值观察 —— 周六管线
