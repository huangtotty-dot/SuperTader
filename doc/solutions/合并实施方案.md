# 合并实施方案（实施方执行规格）

> 版本：V1.0（2026-08-28）
> 上位文档：`自动化交易平台合并架构方案.md`（架构与阶段划分）、`合并实施审核清单.md`（验收闸）
> 本文档是**实施方唯一执行依据**：每个任务给出精确的文件路径、函数签名、数据契约与验收命令。
> 有歧义时不许自由发挥——回查本文档；本文档没写的，问，不要猜。
> 已拍板决策：单仓库合并（迁入 superTrader）；数据源以 gm 为主（P0 实测后最终确认）；人工/自动分管标的池。
>
> **2026-08-28 决策修正（owner，新终端迁移）**：
> 1. 掘金终端由旧版换为**国盛掘金3 专业版**，新策略项目落位于 `C:\Users\Lenovo\.gszq\projects\95e85ee3-a287-11f1-9a76-98fa9b8df5e7`（启动方式=终端内点"启动"按钮）。
> 2. **旧 goldminer3 仓库（`.goldminer3\projects\e8bb1f4d-...`）即刻冻结**：代码不再演进，仅作历史参考与复盘档案源（其 `docs/` 复盘资料按《合并复盘体系统一设计》归档合并）；其 merge-p0~p3 提交中的仓外改造（如 20b4083 副本对齐）随之作废，不在新架构生效。
> 3. **一切合并改动只落在 `E:\superTrader` 仓库内**；auto 侧执行代码在 superTrader 内重建（P4），再以"策略部署"方式同步进新终端策略项目运行。
> 4. P0 的 token 动态发现机制（查 `gmterm-serv.exe` 进程 `--token`）基于旧终端，**换新终端后进程名/参数可能已变，P4 开工前必须复测**（复测不过则 gm 数据链路与 auto 启动链路都要先修）。

---

## 0. 全局约定

### 0.1 代码映射（codec）规范——所有代码必须先读本节

三套代码格式共存，**转换只允许通过 `core/market_data/codec.py` 的四个函数**，禁止在业务代码里再写 `startswith(("5","6"))` 判断：

| 格式 | 样例 | 用途 |
|------|------|------|
| 内部纯 6 位 | `600481` | superTrader 全部内部逻辑、缓存文件名 |
| 内部多账户后缀 | `600481_A` / `600481_B` | superTrader 多账户持仓（goldminer 无此概念） |
| GM 格式 | `SHSE.600481` / `SZSE.002639` | 掘金 SDK 入参/出参 |

```python
# core/market_data/codec.py 必须实现的四个函数
def to_gm(code: str) -> str:        # "600481"→"SHSE.600481"；"600481_A"→"SHSE.600481"（后缀剥离，映射日志记 warning）
def to_internal(gm_code: str) -> str:  # "SHSE.600481"→"600481"
def market_of(code: str) -> str:    # "600481"→"SH"；规则：5/6/9 开头=SH，其余=SZ
def strip_account(code: str) -> str:  # "600481_A"→"600481"；无后缀原样返回
```

### 0.2 数据契约（DataFrame schema，任何 provider 必须产出同样形态）

| 方法 | 签名 | 返回列（顺序、dtype） | 单位/口径 |
|------|------|----------------------|-----------|
| 日线 | `daily(code: str, days: int = 800) -> pd.DataFrame` | `date(str YYYY-MM-DD), open(f64), high(f64), low(f64), close(f64), volume(f64)` | **前复权**；volume 单位=**手** |
| 分钟线 | `minute(code: str, date: str) -> pd.DataFrame` | `time(str "HH:MM"), open, high, low, close, volume, amount(f64)` | 当日 1 分钟；volume 单位=**手**；amount 单位=元 |
| 实时快照 | `snapshot(codes: list[str]) -> dict[str, dict]` | `{code: {price, open, high, low, volume, ts_date}}` | volume=手；`ts_date`=快照时间戳日期（YYYY-MM-DD），用于新鲜度判断（08-28 摩恩电气事故教训） |
| 指数日线 | `index_daily(index: str = "sh000001", days: int = 800) -> pd.DataFrame` | `date, close`（至少这两列） | 前复权 |

**单位裁决（最终口径，替代方案文档中的模糊表述）**：内部统一用 **手**。GM SDK 返回的 volume 单位是**股**，`gm_provider` 内统一 ÷100 收敛；腾讯侧本来就是手，不转换。**任何 provider 外的地方出现 ×100/÷100 都视为 bug。**

### 0.3 工程纪律

- 分支策略：直接在 main 上小步提交（用户单兵项目），但每阶段一个里程碑 commit，commit message 前缀 `merge-pN:`；
- 任何阶段验收闸不过 → 回退该阶段改动，不得带债进入下一阶段；
- 运行时产物（缓存/日志/状态）不入库；`analysis/t_io/`、`execution/t_io/` 这类目录错位产物出现即说明有模块写盘路径写错，必须修；
- 缓存格式不变式：`t_io/cache/daily_kline/*.json` 现有格式（`{date, saved_at, rows:[{date,open,close,high,low,volume}]}`）保持不变，provider 写缓存必须兼容旧文件。

---

## P0 前置验证（预计 0.5 天）

### 任务 P0-1：gm 配额实测

新建 `t_io/experiment/gm_quota_probe.py`（不入生产代码），实测并输出报告：

1. `history_n(symbol, "1d", 800, adjust=ADJUST_PREV, end_time=今天)` × 50 只（取 superTrader watchlist 前 50）连续拉取：记录成功率、总耗时、有无限流报错；
2. `history_n(symbol, "60s", 240)` 同样 50 只；
3. `subscribe(symbols=50只, frequency="60s")` 在掘金终端内能否建立（如受模拟盘权限限制，记录报错原文）；
4. 指数：`history_n("SHSE.000001", "1d", 900)`；
5. 产出：`t_io/experiment/gm_quota_probe_report.md`，结论必须明确回答："gm 作为主数据源是否可行"，若限流，给出限流阈值。

**验收闸**：报告落盘 + 结论明确。若结论为"配额不足"→ 停下来找用户，数据主源改为"gm 供 auto 侧、腾讯供 manual 侧"，方案文档 §9-2 重议。

### 任务 P0-2：token 安全

1. goldminer `main.py:2309` 的硬编码 token 移除，统一走 `utils/gm_token.py`（读取顺序：环境变量 `GM_TOKEN` → `t_io/state/gm_config.json`）；
2. 用户侧手动换发 token（审核方提醒，不代做）；
3. `grep -rn "token" --include="*.py"` 确认两仓库无明文残留。

> ⚠️ **2026-08-28 审核发现（机制修正）**：国盛定制版 gm SDK 的数据接口实际认证的是**掘金终端会话 token**
> （gmterm-serv 进程启动参数 `--token=`，随终端重启轮换），静态配置文件 token 不可行。
> `gm_config.json` 降级为兜底/覆盖项；主机制改为**动态发现**：查 `gmterm-serv.exe` 进程命令行取
> `--token`（参考实现见审核清单 P0 打回记录）；终端未运行 → 返回 None → facade 降级腾讯。
> goldminer `main.py` 的 `run(token=...)` 同样改走该发现机制。

**验收闸**：仓库内 grep 不到 token；token 获取机制在终端重启后可自动恢复（实测：重启终端后不重配置仍能拉数）。

---

## P1 数据同源（预计 2-3 天）

### 任务 P1-1：新建 `core/market_data/` 包

```
core/market_data/
├── __init__.py          # 导出 get_provider()
├── codec.py             # §0.1 四个函数 + 单元测试
├── provider.py          # MarketDataProvider 抽象基类（§0.2 四方法）
├── gm_provider.py       # gm 实现（主源）
├── tencent_provider.py  # 腾讯实现（收编现有内联逻辑，含 WAF 多主机兜底/代理清除）
└── facade.py            # get_provider()：默认 gm，失败/超时降级 tencent，每次降级 log.warning 并在返回 DataFrame 的 attrs["source"] 标记 "gm"/"tencent"/"cache"
```

- `provider.py` 的抽象基类方法签名严格按 §0.2；
- `tencent_provider.py` 的日线/分钟线实现**从现有代码原样搬迁**（`core/position_builder.py:302` `fetch_daily_kline`、`src/data_fetcher.py:647` `fetch_minute_bar`、`core/timing_gate.py:62` `_fetch_index_daily` 的腾讯部分），不改变其行为，只加单位/契约断言；
- `gm_provider.py` 用 P0-1 验证过的调用方式；volume ÷100；日线强制 `adjust=ADJUST_PREV`；
- **快照新鲜度**：snapshot() 返回必须带 `ts_date`（gm 快照用其时间戳字段），契约与 08-28 修复后的 `_live_quote_forming` 对齐；
- 缓存读写保留在 provider 内（路径/格式不变式见 §0.3）。

**验收命令**：
```bash
python -m pytest t_io/validation/market_data/ -q        # 新建的 codec/契约单测
python -c "from core.market_data import get_provider; p=get_provider(); df=p.daily('600481'); print(df.tail(3), df.attrs.get('source'))"
```

### 任务 P1-2：调用点收敛（逐一改，改完一处跑一处冒烟）

| # | 文件:位置 | 现状 | 改为 |
|---|-----------|------|------|
| 1 | `core/position_builder.py:302` `fetch_daily_kline` | 内联腾讯+缓存 | 函数体改为 `return get_provider().daily(code)`，**保留函数名和签名**（外部调用方不变） |
| 2 | `core/timing_gate.py:62` `_fetch_index_daily` | 内联腾讯指数日线 | 改调 `get_provider().index_daily()`，保留缓存陈旧校验（B-1）逻辑 |
| 3 | `src/data_fetcher.py:647` `fetch_minute_bar` | 内联腾讯分钟+CSV缓存 | 改调 provider.minute()，保留 CSV 缓存与 TTL |
| 4 | `src/data_fetcher.py:39` `_fetch_daily_bar_tencent` | 日线兜底 | 改调 provider.daily() |
| 5 | `analysis/index_regime.py:691` | 指数日线主源 | 改调 provider.index_daily() |
| 6 | `analysis/index_regime_intraday.py:154` | 指数分时 | 改调 provider（必要时指数分钟走腾讯兜底，记录在案） |
| 7 | `core/market_review.py:159`、`execution/daily_sentiment.py:405,842` | 腾讯日线 | 改调 provider.daily() |
| 8 | `t_gui.py:2429` `_live_quote_forming`、`:320/492` 指数/行情条 | 内联快照 | 改调 provider.snapshot()（保留 08-28 ts_date 新鲜度语义！） |
| 9 | `execution/auction_analyzer.py:313`、`t_io/preopen/auction_collector.py:36,75` | 竞价快照 | 竞价时段 gm 无虚拟匹配价概念——**保留腾讯**，迁为 `tencent_provider.snapshot_auction()` 并注释原因 |
| 10 | `sector_rotation/data_fetch.py:86,152`、`stock_hunter/modules/market_data.py:99` | 批量快照/日线 | 改调 provider |

**验收闸**：`grep -rn "gtimg.cn\|fqkline" --include="*.py" . | grep -v market_data | grep -v t_io/` 应只剩注释/文档命中；main.py 全流程冒烟（盘前自检推送正常、做T扫描正常、建仓扫描正常）；GUI 打开无异常。

### 任务 P1-3：双源对照（一周）

新建 `scripts/data_source_parity.py`：每日收盘后对 watchlist 全部股票分别用 gm/腾讯拉当日日线，比对 close 差异；产出 `t_io/validation/data_parity_{date}.json`（差异率 = |gm_close/tx_close - 1| > 0.001 的股票占比）。

**验收闸**：连续 5 个交易日差异率 <0.1% 或差异全部可解释（分红除权/复权口径）。

---

## P2 做T引擎同源（预计 3-4 天，最大不确定项）

### 任务 P2-1：EngineContext 依赖注入改造

`core/signal_engine.py` 改造目标形态：

```python
@dataclass
class EngineContext:
    holdings: dict                  # 持仓真源引用
    virtual_trades: dict            # 虚拟成交记账
    minute_fetch_status: dict
    t_mode: str
    now: Callable[[], datetime]     # 时间注入（对齐 goldminer SIM_NOW 契约）
    append_jsonl: Callable
    fetch_minute_bar: Callable      # = market_data provider 的 minute

class SignalEngine:
    def __init__(self, ctx: EngineContext, params: dict = None): ...
```

- 现 exec 共享全局（`main.py:73-99` 注入的 `MINUTE_FETCH_STATUS/HOLDINGS/VIRTUAL_TRADES/T_MODE/DAILY_DECISION_STATS/BACKTEST_DAY_CACHE` 及函数）逐个迁移为 ctx 字段；
- main.py 主循环改为构造一个 EngineContext 传入；**对象引用同一性必须保持**（ctx.holdings 就是 HOLDINGS 本体，不是拷贝）；
- `core/signal_engine.py:47-91` 的 standalone fallback 桩删除，改为"无 ctx 不许实例化"。

**验收闸（硬指标）**：`python main.py --replay-today 2026-08-27`（或等价回放入口）改造前后两次运行，产出的信号序列（代码/方向/时间/价格/分数）**逐条 diff = 0**。

### 任务 P2-2：goldminer 补丁归账

产出 `doc/solutions/引擎分支补丁归账表.md`：goldminer 侧 WP-B 系列（B07/B14/B15/B18/B19/B22 等）及全部带日期注释的实战补丁，逐条列：补丁内容 | 解决的问题 | 裁决（合入公共引擎 / 仅 auto 执行侧保留 / 已过时放弃）| 证据。审核方逐条复核。

### 任务 P2-3：双消费接线

Signal 产出后分叉：manual → 现有 `notify()` 飞书链路（不动）；auto → `execution/auto/` 的闸链+下单适配器。分叉点代码上必须显式（`if channel == "manual" / "auto"`），不允许隐式共享副作用。

**验收闸**：goldminer `tests/` 14 个 mock 测试通过；superTrader 回放无回归；同一交易日（回放）双引擎输出一致。

---

## P3 建仓加仓同源（预计 1-2 天）

### 任务 P3-1：口径差落地（已裁决）

| 项 | 统一口径 | 实施 |
|----|---------|------|
| 箱体窗口 | 150 日/30 窗（superTrader 现值） | goldminer 侧删除其 60 日/24 窗副本，直接用公共 `core/position_builder.py` |
| 量能 | 实时量（废 T-1 冻结） | auto 侧注意"当前 bar 未完成"语义：用量能时用上一根已完成 bar |
| 冰点预热 | 盘前用 gm `history_n(60s×240)` 预取，消灭预热空窗 | auto 侧 init 预取逻辑调整 |

> **2026-08-28 落地形态（修正）**：公共建仓判定（方案A 时机门控 + 否决因子 + W35 日内确认 +
> verdict 映射）收敛为 superTrader `core/build_decision.py` **纯函数单一真源**（无 IO，可跨仓
> importlib 加载）；`core/timing_gate.py`/`core/position_builder.py` 已委托重构（行为保持，
> 金样 0 diff）。旧 goldminer3 仓库 20b4083 的"副本参数对齐"随旧仓库冻结**作废**——auto 侧
> 不再维护任何判定副本，P4 接线时直接加载 `core/build_decision.py`。

### 任务 P3-2：标的池分管（已拍板）

- `watchlist_buy.json` 每股增加 `"pool": "manual" | "auto"` 字段（存量默认 "manual"）；
- auto 侧候选池（现 goldminer `main.py:33-52` 硬编码 17 票）迁入 `config.py` 或独立配置文件，标注 pool="auto"；
- 交集裁决：同一标的不得同时属两池（启动时校验，冲突则拒绝启动并提示）；GUI 加池筛选。

**验收闸**：同一交易日双侧 verdict 一致；否决因子/数据不足路径双侧生效；池冲突校验生效。

> **2026-08-28 验收方式（修正）**：双侧一致性的可执行验证 = `python scripts/build_verdict_parity.py
> --date <交易日>`（manual 生产判定链 vs gm 直拉数据路径 + 同一决策核，逐票比 verdict/go/veto/regime）。
> auto 侧的**实运行接线**（execution/auto 内调 build_gate 判定）属 P4 任务，见下。

---

## P4 执行链路与 GUI（预计 2-3 天）

> **2026-08-28 状态修正**：P4-1/P4-2/P4-3 已由实施方落地（commit 41a032f0 / ec3f72ee /
> 6e982395，**待审核**）。因 owner 同日裁决换新终端（国盛掘金3 专业版）且改动一律落 superTrader
> 仓内，P4 增补 P4-4~P4-6 三个任务。

### 任务 P4-1：goldminer 执行层迁入（已落地，待审核）

```
execution/auto/
├── __init__.py
├── gm_main.py         # 原 goldminer main.py：gm 回调/闸链/下单/台账（唯一 import gm.api 的文件）
├── sell_channels.py   # P0-P6 卖出通道 + _sell_arbiter（★独立边界，验收=行为与迁移前一致）
└── sell_state.py      # sell_state.json 读写（pos_key 指纹逻辑保留）
```

- 迁移日显式重置 `sell_state.json` 并在日志记录（指纹校验对持仓源敏感）；
- 止盈止损通道代码**不做任何逻辑改动**，只做 import 路径适配；
- 验收=用同一历史日回放，P0-P6 各通道触发时点与迁移前一致。

### 任务 P4-2：事件桥迁移（已落地，待审核）

- goldminer `runtime/bridge/` → `t_io/bridge/`；schema 不变；
- `t_io/gm_bridge/` 旧冻结文件归档保留不动；
- **单文件单写者**：events/heartbeat 由 auto 侧写；manual 侧若需写信号事件，新建 `manual_signals_{date}.jsonl`，不写现有文件；
- watcher.py 迁入后继续只读 bridge，推送逻辑不动。

### 任务 P4-3：GUI 自动盘页（已落地，待审核）

- `web/index.html` 新增 tab"自动盘"；`t_gui.py` 新增 Api 方法 `load_auto_status()`：读 `t_io/bridge/heartbeat.json`（positions/cash/index_regime）+ 当日 events（order/fill/reject/risk 计数与最新 10 条）；
- KILL_SWITCH 存在时全页顶部红色横幅；
- 持仓/做T页双台账并列展示，标注来源（人工/自动）；
- t_gui 直联网 4 处在 P1-2 已收敛，此处复查无残留。

### 任务 P4-4：新终端适配（2026-08-28 增补）

- **token 发现机制复测**：国盛专业版终端的会话进程名/启动参数可能与旧版 `gmterm-serv.exe --token`
  不同——实测 `core/market_data/gm_token.py` 的发现链在新终端下是否命中；不命中则扩展发现规则
  （进程名/参数模式），并复跑 P0-1 配额探针（`history_n` 日线/60s + subscribe 上限，新终端权限可能不同）。
- `execution/auto/gm_main.py` 运行依赖核查：新终端内嵌 Python 环境的包可用性（pandas/numpy/requests），
  策略启动方式 = 终端"启动"按钮（run() 入口与旧终端的差异适配）。

### 任务 P4-5：新策略项目部署（2026-08-28 增补）

- 在 `C:\Users\Lenovo\.gszq\projects\95e85ee3-a287-11f1-9a76-98fa9b8df5e7` 建策略骨架：
  终端策略入口文件（init/on_bar 薄壳）经 `SUPERTRADER_ROOT` 定位并加载 superTrader
  `execution/auto/gm_main.py` 的逻辑——**策略代码唯一真源在 superTrader 仓内**，.gszq 项目只放
  部署壳（类比 auto_pool.py 的 importlib 跨仓加载模式；禁止复制代码进 .gszq）。
- `SUPERTRADER_ROOT` 环境变量在新终端运行环境的配置方式落文档（默认 `E:\superTrader`）。
- 部署清单：KILL_SWITCH 位置（`t_io/bridge/`）、日志/事件桥路径、故障时回退旧终端的预案。

### 任务 P4-6：auto 侧建仓判定接线（2026-08-28 增补，P3 遗留执行项）

- `execution/auto/gm_main.py` 的 BASE 建仓块放行判据切换为 `core/build_decision.py` 决策核
  （方案A 时机门控：regime → 追强/抄底 → 否决因子爆量/偏离MA60 → W35 日内确认），
  数据适配参考已被回滚的 goldminer `signals/build_gate.py` 设计（日线行缓存 + 指数日线 +
  bar_cache 1m → 决策核）；WP-B20 双通道降为参考留痕（与 manual 侧 `result["channels"]` 同定位）。
- 数据不足 fail-closed；否决因子/数据不足路径与 manual 侧同时生效（P3 验收闸的执行侧闭环）。
- 接线后重跑 `scripts/build_verdict_parity.py` 扩展版（auto 链路改走 execution/auto 真实代码路径）。

**验收闸**：auto 模拟盘在**新终端**全流程跑通（信号→闸链→下单→成交回报→桥事件→GUI 可见）；
watcher 推送正常；人工链路零回归；P4-6 接线后双侧 verdict 一致（扩展版 parity 全票通过）。

---

## P5 并行观察期（2 周日历时间）

- 每日收盘后对账：双侧信号序列、成交、持仓，差异写入 `t_io/validation/merge_parity_{date}.md`，要求差异 100% 可解释；
- 每日复盘 §4 的"实现健康检查"（否决因子/数据不足/timing_gate 故障）在合并后链路继续执行；
- 观察期满且无未解释差异 → 合并完成，goldminer 旧仓库归档只读。

---

## 附：实施方常见歧义点答疑（先写死，免得来回问）

1. **"数据源同源"是指 manual 链路也切 gm 吗？** 是。P1 完成后 manual/auto 都走 `core/market_data`，腾讯只是 provider 内的降级兜底。竞价虚拟匹配价是唯一例外（gm 无此数据，保留腾讯，已收敛进 provider）。
2. **量能单位到底用什么？** 手。gm provider 内部 ÷100。其它地方出现换算系数即打回。
3. **goldminer 的建仓闸链（池闸/槽位闸等）算"建仓逻辑"吗？** 不算。它是执行侧风控，归 execution/auto/ 独立边界。公共建仓判定 = position_builder + timing_gate。
4. **可以顺手重构/美化无关代码吗？** 不可以。最小改动原则贯穿全程。
5. **遇到方案文档与现有代码冲突？** 停下来，在审核记录表登记，等裁决。不许自行取舍。
