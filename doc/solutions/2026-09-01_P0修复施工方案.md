# 2026-09-01 复盘问题修复施工方案

> 依据：复盘清单 V1.3 铁律——每日只记录不改参，**唯一例外：P0 级故障（告警失效/数据缺失/reconcile 类运营事故）当日处理**。
> 来源：`doc/review/日复盘/2026-09-01_复盘.md` A-3 异常表（14 条中的 ① 类执行 bug）。② 结构类（300364 失明口径 B1、buyback 高接 B2、regime 分裂 B3）与 ③ 数值类（C1/C2）**不进本方案**，留 09-05 周六管线。
> 本方案只含故障修复，**不含任何阈值/参数数值变更**；红线：逐项独立 commit，次日复盘逐项验证。

## 总览

| 序 | 候选编号 | 问题 | 根因定位 | 改动面 | 风险 |
|---|---|---|---|---|---|
| P0-1 | Q-20260901-A5 | 分钟快照全量停摆 2 日 | `core/utils.py:151` json.dump 遇 pd.Timestamp 抛异常被静默吞 | 2 行 | 极低 |
| P0-2 | Q-20260901-A1 | backtrace 镜像路径错位 | `execution/auto/gm_main.py:761` dirname 少退一级 | 1 行 | 极低 |
| P0-3 | Q-20260901-A6② | 早盘预警卡片静默未发（4 次） | `core/signal_engine.py:339/363` 传 str 列表 vs `main.py:497` 期望 dict | 3 处 | 低 |
| P0-4 | Q-20260901-A2 | BUY_PENDING confirm 后悬空 | `gm_main.py:372` 消费耦合在"信号复发"路径，无独立消费者 | gm_main 单文件 | 低 |
| P0-5 | Q-20260901-A4 | 做T盈亏两口径冲突 | `main.py:1305-1306` 均价算法 bug + 14:50 清空时间窗错位 | main.py+core 配对函数 | 低 |
| P0-6 | Q-20260901-A3 | holdings 写入零留痕 | `src/holdings_repo.py` 三个写函数无 actor/reason 审计 | repo+5 调用点 | 低 |
| P0-7 | Q-20260901-A6① | daily_review/forward_tracker 无自动触发 | 全仓无任何调度方，历来手工跑 | main.py 新增钩子 | 低 |
| P0-8 | Q-20260831-A5 续 | preopen 焦点名单空 43 天 + 口径污染 | 07-21 V2 重写删了 `_pick_focus`；persist 覆写污染盘前产物 | execution/preopen.py | 低 |

---

## P0-1 分钟快照停摆（数据缺失，最优先）

- **根因（已定位）**：08-28 提交 `74d76b58`（merge-p1.3）把 gm/腾讯分钟线 `time` 列统一改为 datetime64（`gm_provider.py:69`、`tencent_provider.py:266`），但快照写盘 `core/utils.py:151` 的 `json.dump` 未加 `default=str` → 必抛 `TypeError: Timestamp is not JSON serializable` → 被 `core/utils.py:152-157` C19-3 静默吞掉，2 个交易日零告警。叠加次因：C-5 白名单（`main.py:1818-1831`）对 qty=0 清仓股合法跳过采集（603667 等 08-21 起逐个停更是设计内行为）。
- **修复动作**：
  1. `core/utils.py:151` → `json.dump(_json_safe(rec), f, ensure_ascii=False, default=str)`（`str(Timestamp)` 输出与历史文件 bars 格式逐字节一致，下游零适配）。
  2. `core/utils.py` 顶部补 `import time`（P0-4 自包含遗漏的潜伏雷：line 148/164 用 time 但未 import，独立 import 链路会 NameError）。
  3. `core/utils.py:152` 静默 except 加 `log.warning`（对齐 line 171 口径）——"写盘失败必须可见"。
  4. **回填今日**：读 `t_io/cache/minute_{code}_2026-09-01.csv`（31 个尚存）重建今日快照；08-31 需 gm 历史分钟回填（gm 终端在线时可选做）。
- **验证**：单元级用 pd.date_range 构造 DataFrame 调 `_snapshot_write('999999',...)` 成功且 `bars[0].time` 为 str；`ls t_io/minute_snapshots/2026/09/` 出现当日持仓股文件；明日日志 `总快照=` 计数 >0。
- **遗留拍板项**：603667 等清仓股是否恢复采集（需把 `main.py:1986` 前移到白名单 continue 之前，改变 C-5 语义边界）——📝 owner 拍板，不与主修复捆绑。

## P0-2 backtrace 镜像路径错位

- **根因（已定位）**：`execution/auto/gm_main.py:761` 用 `dirname(PROJECT_DIR)` 推导仓库根少退一级，镜像落到 `execution/t_io/logs/auto_backtrace.jsonl`（470/470 今日行，内容完整但路径错）；清单期待路径 `t_io/logs/auto_backtrace.jsonl` 不存在。
- **修复动作**：修正根路径推导（`Path(__file__)` 向上一级或直接用 PROJECT_DIR）；把 `execution/t_io/` 错位目录下今日文件迁移到正确路径后删除该残留目录（注意它是 git 未跟踪目录）。
- **验证**：`t_io/logs/auto_backtrace.jsonl` 存在且含当日行；`execution/t_io/` 已清除。

## P0-3 早盘预警卡片静默未发（告警失效）

- **根因（已定位）**：`core/signal_engine.py:339/363` 调用 `send_morning_alert` 时把规则 dict 退化成 `[rule.get("name")]` str 列表，而卡片构建器 `main.py:497` 期望 dict 列表（`r.get('name')`）→ AttributeError 被 except 吞掉只记 warning。**后果：今日 4 次 L1/L2 预警（09:40/09:41/13:00×2，4 只不同股票）飞书卡片全部未发出**。次级错位：signal_engine 传 `max_gain` 键、`main.py:504/569` 读 `max_gain_after_open`——修好异常后"最高涨幅"栏仍恒 N/A。
- **修复动作**（双点都做）：
  1. `main.py:497` 防御归一化：`r if isinstance(r, dict) else {"name": str(r), "desc": "", "precision": 0}`。
  2. `core/signal_engine.py:339/363` 传完整 `[rule]`；`"max_gain"` 键改名 `"max_gain_after_open"`。
- **验证**：构造 dict/str 两种入参调 `build_alert_card` 不抛异常；次日若触发预警，日志出现 `✅ 早盘预警已推送` 且飞书卡片"最高涨幅"非 N/A。

## P0-4 BUY_PENDING confirm 悬空（人工确认链路断）

- **根因（已定位）**：`BUY_DECISION.json` 唯一消费点在 `gm_main.py:372`（`_buy_confirm_gate`），而该闸门只在"同标的信号再次触发"时被调用（`gm_main.py:1741-1748` 加仓 / `:1462` 建仓），且 `if not _ab_now` 前置使回补路径整体旁路。今日时间线：09:46 挂起 → 09:49 卖出建立回补记忆 → 09:55 新信号走回补通道旁路闸门下单 1800 股（pending 未作废）→ 10:17 confirm 到达后再无信号复发 → 永久悬空，无任何日志/告警/日内作废。
- **修复动作**（gm_main 单文件，纯结构）：
  1. **日内巡检扫描器**：on_bar 主循环（挂 1135 行附近）对 `_buy_confirm_pending` 逐 bar 检查——decision=confirm 但超时未转化为 order 的，`write_confirm(..., "approved_not_executed")` + 飞书告警 + 清 pending + `_persist()`（超时常量 `BUY_CONFIRM_EXEC_TIMEOUT_SEC` 的**具体数值留周六拍板**，今晚可先写代码、常量置位周六填）。
  2. **回补旁路时作废旧 pending**：1741 行 else 分支（回补下单处）若存在同 code pending → `write_confirm(..., "superseded")` 清除。
  3. **信号褪化留痕**：1747 行 `continue` 前，对"confirm 已到达但 score 掉阈"场景补 `write_confirm(..., "signal_faded")`。
- **验证**：`test_buy_confirm_gate.py` 既有 T1-T9 全绿 + 新增 T10（confirm 后无信号 → approved_not_executed）/T11（回补成交 → superseded）；次日实盘 events 中 confirm 后必有 order 或 approved_not_executed 之一。

## P0-5 做T盈亏口径统一（①栏数字不可考核）

- **根因（已定位）**：三层叠加——① `main.py:1305-1306` 把均价覆盖为"总额÷配对量"，结果恒等于净现金流而非配对利润，且未配对腿也全额计佣（588170 的 -0.19 就是尾盘幻影卖出信号的裸佣金）；② 时间窗错位：closure_audit 14:50 `VIRTUAL_TRADES.clear()` 后扫描循环继续，14:56 两条 SELL_HIGH 重新写入残影，14:59 daily_pnl 读到的是另一份数据；③ 手续费默认值两处不一致（1270 行 0.00015 vs 1430 行 0.00025，config 实为 0.00025）。另：600176 闭环漏记=12:14 进程重启丢内存态 `t_entry_price`（`core/t_decision.py:149`，`_persist_intraday_state` 不含它）。
- **修复动作**：
  1. 抽统一配对函数 `compute_t0_pnl(vt, commission_rate)`：均价=各自总量加权；费用只计 matched 双腿；未接回部分单列 open_qty/open_ref 不进实盈。`main.py:1292-1307` 与 `:1445-1454` 共同调用，删除 1301-1306 覆盖逻辑。
  2. 消除时间窗：clear 前移快照（deepcopy 存模块级变量供 daily_pnl 读）或把 clear 移到 daily_pnl 之后；且 14:50 后信号不再写 VIRTUAL_TRADES（`record_trade_action` 入口加收盘闸）。
  3. 两处手续费默认值统一为 0.00025（对齐 `config.py:318`，一致性修复非调参）。
  4. `_persist_intraday_state` 增加 `t_entry_price` 持久化/回灌；`execution/auto/t_engine_auto.py:260-280` 镜像同样改动。
- **口径切换声明**：修复后飞书卡片"今日做T实盈"数值口径变化（如今日 -1.40 实为主算法 bug 产物）——复盘 A-1 ①栏自明日起用新口径，历史不改写。
- **验证**：单元构造单边卖出期望 t0=0（不再 -0.19）；重放今日两账本输出一致；模拟重启后 600176 has_entry=True 不再重复买入；次日 `daily_pnl.jsonl` 与 `closure_audit.jsonl` 数字一致。

## P0-6 holdings 写入强制审计（reconcile 可信地基）

- **根因（已定位）**：518880 条目形态（name=code、mirror_cost=0.0、account=""）与 `src/holdings_repo.py:73-99 upsert_auto_entry()` 默认产物完全一致，且 watchlist pool 同步被改（`t_gui.py:3384-3390` 特征）——**最可能写入者=t_gui「添加自动盘标的」链路（`web/app.js:1680` → `t_gui.py:3344-3395`），置信度高**。真正根因：holdings 全部写入口（repo 三函数 + `main.py:1404` 直写 + GUI 三入口）均无 actor/reason 留痕。
- **修复动作**：
  1. `src/holdings_repo.py` 新增统一 audited writer：所有写入强制带 actor/action/reason/code/before/after/changed_fields/pid/ts（可选 stack），审计落 `t_io/logs/holdings_write_audit_{date}.jsonl`；不改 holdings.json schema。
  2. 三函数签名加 `actor/reason` 必填；更新全部 5 个调用点（t_gui 3376/3429/3501、main 1537/1404——1404 的直写改走 repo）。
  3. GUI 双写（holdings+watchlist pool）建立同一 request_id 关联留痕。
  4. 审计写入失败不得静默：至少写 sys 日志 + 飞书告警。
- **验证**：`py_compile` 三文件 + `check_holdings_consistency.py` PASS + monkeypatch smoke test（audit 行含 actor/reason）+ grep 确认无绕过 repo 的直写残留。
- **待 owner 确认**：518880×mirror 2000 是否你手动在 GUI 添加？（决定该条目保留还是移除）

## P0-7 收盘自动化触发（新鲜度闸不靠人工）

- **根因（已定位）**：daily_review.py / forward_tracker.py 全仓无任何自动触发方（唯一 bat 从未注册计划任务；07-21 `4d97f1ad` 移除了 main.py 收盘钩子，残留 `main.py:1759-1760 pass`），历来靠复盘手工补跑。
- **修复动作**：main.py 主循环收盘钩子链新增 `_maybe_run_daily_review(now)`，挂在 `main.py:1768` `_maybe_run_position_builder` 之后：
  1. 触发条件 `t >= 15:10`（保证在 15:05 建仓 eod 扫描之后）、周一~五、成功才去重占位、失败 10 分钟节流重试（照抄 `_maybe_run_position_builder` 模式）。
  2. 子进程隔离（照抄 `_launch_sentiment_backfill` 模式）：`subprocess.Popen(CREATE_NO_WINDOW)`，同一子进程内串行跑 daily_review → forward_tracker（后者读前者产出），输出落 `t_io/logs/daily_review_{date}.log`。
  3. 两脚本本身幂等，重复触发安全。
- **验证**：次日 15:10-15:20 观察子进程启动日志 + `daily_review_{date}.json`/`kpi_{date}.json` 自动产出；手工再跑一次确认幂等无重复段。

## P0-8 preopen 焦点名单（Q-20260831-A5 续：从"恢复产出"到"恢复内容"）

- **根因（已定位，调查修正昨日口径）**：焦点名单空不是"连续 4 日"而是**连续 43 天**——07-21 V2 重写（`4d97f1ad`）删除了 `_pick_focus`/`_sort_codes_by_auction_score` 填充逻辑，四字段退化为兼容占位（`execution/preopen.py:50-54`），且全仓无下游消费者。另两缺陷：① `persist()` 整文件覆写，盘中重启（今日 12:14 akshare 恰好恢复）把盘前降级产物覆盖——口径污染；② akshare top20 失败全程静默（`preopen.py:228` 仅 log.debug），竞价窗口恰是其最不稳时段（今日 5/5 全败），兜底静默裸奔。
- **修复动作**：
  1. **覆写守卫**：当日文件已含 top20 成功结果时，竞价窗口外的 empty 结果不覆盖（或盘前产物时间戳另存 `generated_at_preopen`）。
  2. **告警补链**：top20 返回 empty → `log.warning` + `preopen_fail` trace；竞价窗口内失败升 `log.error`。
  3. **清死代码**：`preopen.py:439-465` `_writeback_auction_summary` 残骸（落在 return 后永不执行）删除。
  4. 📝 **owner 拍板项**：焦点名单"恢复 vs 正式废弃"——建议恢复（从 `4d97f1ad^` 找回 `_pick_focus` 骨架接入现版 evaluate()，旧阈值原样保留标注"周六评审"）；若废弃则删字段+改清单 S0 口径，消除"永远空"假 bug。
- **验证**：重启 t_trader 后 preopen_trace 新行 focus_codes 非空（若选恢复）；9:25 后 preopen_{date}.json 的 generated_at 停在盘前窗口不再被午后覆盖；top20 失败时 preopen_fail 有记录。

---

## 施工顺序与 commit 计划（逐项独立 commit）

1. P0-1 分钟快照（2 行+回填）→ `fix(snapshots): json.dump 兼容 pd.Timestamp + import time + 写盘失败告警`
2. P0-2 backtrace 路径（1 行+迁移清理）→ `fix(auto): backtrace 镜像根路径少退一级`
3. P0-3 早盘预警（3 处）→ `fix(manual): send_morning_alert 契约对齐——str/dict 归一化+max_gain_after_open 键名`
4. P0-7 收盘钩子（main.py 一处新增）→ `feat(review): 收盘自动触发 daily_review+forward_tracker（子进程隔离+幂等）`
5. P0-4 BUY_PENDING（gm_main 单文件）→ `fix(auto): confirm 悬空三件套——approved_not_executed/superseded/signal_faded`
6. P0-5 盈亏口径（main.py+core，含口径切换声明）→ `fix(pnl): 做T盈亏统一配对口径+t_entry_price 持久化`
7. P0-6 holdings 审计（repo+5 调用点）→ `feat(state): holdings 写入强制审计留痕`
8. P0-8 preopen（守卫+告警+死代码；焦点名单恢复待拍板后单独 commit）

> 顺序逻辑：1-3 是最小改动最快见效（数据源/告警地基）；4 让明日起新鲜度闸自动；5-7 涉及行为语义变化放中间仔细验证；8 含拍板项垫底。

## 次日（09-02）复盘验证清单

- [ ] `ls t_io/minute_snapshots/2026/09/` 有 09-02 当日文件（P0-1）
- [ ] `t_io/logs/auto_backtrace.jsonl` 存在且当日增长；`execution/t_io/` 已删（P0-2）
- [ ] 若触发早盘预警：日志 `✅ 早盘预警已推送`、卡片"最高涨幅"非 N/A（P0-3）
- [ ] 15:10-15:20 `daily_review_2026-09-02.json` 自动产出（非复盘补跑）（P0-7）
- [ ] 若有 confirm：events 中 confirm 后必有 order / approved_not_executed 之一（P0-4）
- [ ] `daily_pnl.jsonl` t0_realized 与 `closure_audit.jsonl` Σest_pnl 一致（P0-5）
- [ ] `t_io/logs/holdings_write_audit_2026-09-02.jsonl` 有记录（收盘 pre_close 回写至少 1 条）（P0-6）
- [ ] preopen 盘前产物 9:25 后不被覆盖；top20 失败有 preopen_fail 记录（P0-8）

## 留 09-05 周六（不在本方案）

- ② 结构类：300364 单票失明适配（B1）、buyback downgrade 后高接（B2）、regime 双侧口径统一（B3）、前瞻表实体结构（D1/D2）
- ③ 数值类：Renko 强平占比/胜率（C1）、qty=0 满仓参考口径（C2）、BUY_CONFIRM_EXEC_TIMEOUT_SEC 具体值
- W35/W36 合并周复盘 + 阶段看板（18 天未更新）
