# 2026-09-10 今日必修清单

> 来源：09-10 复盘（44bac296 / 220e73b）+ 三路根因定位（py-spy 活体取证 + 代码走读）。
> 原则：每日只记录不改参，①类 bug 例外——本清单全部为已确认的 ①类 bug，owner 已要求修。

---

## P0 —— 明早（09-11）开盘前必修

### 1. 600481 BUY 6100 股 sizing 失控（Q-20260910-1）

**根因一句话**：`execution/auto/gm_main.py:1997` 把个股预算上限 `max_pos_shares`（≈20,400 股）当作 sizer 目标仓位 `target_t` 传入，sizer 按 30% 出量 = 6,100；而确认弹窗预估 `_project_buy_qty`（gm_main.py:312-325）用另一套小口径（=400）——**所谓"步长 400"从来只是弹窗错觉，实单路径一直是预算级**。

**改动点**（1 行）：

```python
# gm_main.py:1997
# BEFORE:
target_t = max(max_pos_shares, _base_ref + _t_head, pos_qty)
# AFTER:
target_t = max(_base_ref + _t_head, pos_qty)
```

上限防护不丢：由既有 `_check_max_pos_cap`（gm_main.py:2001-2004）承担。

**修复后预期**：11:20:59 场景 = 500 股、10:54:59 场景 = 400 股。

**验证**：
1. PositionSizer 复算断言（复算脚本断言修复后 qty 与预期一致）；
2. replay_verify 增加 600481 09-10 场景断言 `BUY qty == 500`。

**估时**：改 10 分钟 + 验证 30 分钟。

**关联待处置（明早一并处理，非代码）**：
- 仿真侧 600481 已持 6,100 股（vs mirror 1,400）——仿真/实盘口径分叉；
- 仿真侧 300054 串入 800 股（=实盘持仓数，疑似仿真终端手工单，见 P2-3A）。

**登记不实施（第二防线，挂周六）**：回补路径 `_ab_now` 旁路（gm_main.py:1933-1940）加数量帽；confirm 超时从 `confirm_ts` 起算（gm_main.py:439）。

---

## P1 —— 今天修

### 2. 收盘段主进程冻死：gm SDK 无超时挂死（Q-20260910-3）

**根因一句话**（py-spy 活体取证 pid=22852）：主循环第 244 轮在 300054 的 `_attach_dynamic_t_decision → daily_sentiment.stock_daily_features → facade.daily → gm SDK history_n`（gm/api/query.py:659）**无限期阻塞**——gm SDK 无超时，挂死不返回也不抛异常，facade 熔断只捕异常，对"永不返回"无效。EOD 段 + daily_pnl 全缺席。

> 事实纠正：日志末行 15:00:08「日复盘摘要已推送」是独立 daily_review 进程写的，非主进程；signal_outcomes 15:00:02 同为外部 daily_review 写入。主进程 14:55 后无任何输出。

**改动点**：

a. `core/market_data/facade.py`（+28/-4 行）：新增 `_call_gm` 线程池硬超时 12s（单 worker ThreadPoolExecutor，超时重建池并抛 TimeoutError），4 处调用点改造——facade.py:95 `daily` / :184 `minute` / :201 `snapshot` / :212 `index_daily`。全部复用既有 `except Exception → _note_gm_down → 腾讯兜底` 路径，零新分支。

b. `execution/auto/main.py:1406`：daily_pnl 窗口去右界（`t < dtime(14,59)` 即可补扫）。

**验证**：故障注入脚本——mock `p._gm.daily = lambda: time.sleep(300)`，断言 <20s 返回且 source=tencent。

**今日补数**：
```bash
taskkill /PID 22852 /F   # 若冻死进程仍在
python t_io/validation/daily_review/daily_review.py --date 2026-09-10
```

**估时**：改 40 分钟 + 验证 20 分钟。

---

## P2 —— 今天修（各单文件小改）

### 3A. 300054 孤儿 fill 入账（Q-20260910-2A）

**根因一句话**：**任务书方向错了**——不是 manual 写入，是 `gm_main.py:2160` 的 `on_order_status` 账户级回调对非本策略成交（掘金仿真终端手工单）零隔离，无条件 write_fill。

**改动点**（~18 行）：gm_main.py:2151 加孤儿闸 + 新增 `_strategy_ordered_today(code)`（扫当日事件桥有无本策略 order 事件，fail-open）；孤儿单只写 `risk:orphan_fill` 留痕，不进台账。

**⚠️ 待 owner 确认**：真实成因是有人在掘金仿真终端手工买了 800 股 300054（恰好=实盘持仓数）——是否本人操作？

### 3B. preopen open_gap 错位一日（Q-20260910-2B，第三次复发）

**根因一句话**：`execution/preopen.py:285/287/302` 算的是 昨收/前收-1 = **昨日**涨跌幅（000988 报 +8.22%，真实 -0.73%），C-4 高开急拉预警结构性失明。

**改动点**（~20 行）：preopen.py:282 前加载 `auction_{today}.json` 09:25 档（逐档降级 09:22/09:20），`pct_vs_preclose/100` 优先作为 open_gap + 落 `gap_source` 字段；auction 缺失走旧路径兜底。

### 3C. K2 快照 stale（Q-20260910-2C）

**根因一句话**：`t_io/validation/daily_review/daily_review.py:264-272 archive_holdings_snapshot` 幂等"已存在则跳过"，晨跑（09:03:21）锁死 reconcile（09:03:55）前的旧值。

**改动点**（~15 行）：重写归档规则——date==今日时：holdings.json mtime 非今日不建档、比快照新则覆盖自愈；历史日期只读护栏。
**顺手**：daily_review.py:21 `--date` default 硬编码 `"2026-08-03"` 改为当天。

---

## 挂周六（09-13）不动

- 588170 亏损高抛（Q-20260909-8）
- qty=0 照推 / 假拦截 / pushes 不落正文
- c1_m5_iceberg 全卡
- Renko 审查（连续 3 日 <60%：40→25→52.9%）
- Q-20260910-12「600481 只做减仓」预警拍板（明早系统还会推，owner 已知悉忽略）

---

## 执行顺序建议

| 序 | 项 | 截止 |
|---|---|---|
| 1 | P0 sizing 1 行 + 复算验证 | **明早 09:15 前** |
| 2 | 仿真侧 600481 6,100 股 / 300054 800 股口径处置 | 明早开盘前 |
| 3 | P1 facade gm 超时 + 故障注入验证 | 今晚 |
| 4 | P1 补跑 daily_review（补今日 EOD 数据） | 今晚 |
| 5 | P2 三件小改（可并行） | 今晚 |
