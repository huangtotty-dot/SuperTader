# 新终端（国盛掘金3 专业版）部署清单（P4-5）

> 2026-08-28 · owner 裁决：旧 goldminer3 冻结，一切合并改动落 superTrader 仓内，策略以部署壳同步进新终端运行。

## 部署结构

```
C:\Users\Lenovo\.gszq\projects\95e85ee3-a287-11f1-9a76-98fa9b8df5e7\main.py   # 部署壳（薄，代码零复制）
    └─> E:\superTrader\execution\auto\gm_main.py                                # 策略代码唯一真源
            ├─ sell_channels.py / sell_state.py / _gm/                           # 执行层支撑（superTrader 内）
            └─ core/market_data（gm_provider/gm_token/codec）+ core/build_decision.py
```

- 部署壳：`SUPERTRADER_ROOT`（缺省 `E:\superTrader`）→ `sys.path.insert` → importlib 绝对路径加载
  `execution/auto/gm_main.py` → 透传 `init/on_bar/on_order_status/on_backtest_finished`。
- **禁止在 .gszq 项目复制业务代码**；业务代码唯一真源在 superTrader 仓内（与 config/auto_pool.py 同款跨仓模式）。

## SUPERTRADER_ROOT 配置

- 环境变量 `SUPERTRADER_ROOT`（缺省 `E:\superTrader`）。终端启动策略时若未设置，走缺省路径。
- 在 Windows 用户环境变量设置后，重启终端生效；或直接在部署壳头部硬编码缺省值（当前已内置 `E:\superTrader`）。

## 运行时路径（由 superTrader 仓内决定）

| 项 | 位置 |
|----|------|
| 事件桥 events_*.jsonl | `E:\superTrader\t_io\bridge\` |
| heartbeat.json | `E:\superTrader\t_io\bridge\heartbeat.json` |
| KILL_SWITCH | `E:\superTrader\t_io\bridge\KILL_SWITCH`（存在则停止下单） |
| 卖出状态 sell_state | `E:\superTrader\t_io\state\auto_sell_state.json` |
| 审计 backtrace | `execution/auto/gmcache/backtrace.jsonl` |
| 策略日志 | `execution/auto/logs/`（ops_guard.bootstrap_logging） |

## 启动与验证

1. **live（模拟盘）**：终端内点「启动」→ 运行部署壳 `run(mode=MODE_LIVE)`。
2. **回测**：`python main.py --backtest`（缺省窗口 2026-04-24~07-24）。
3. **token**：动态发现（`core/market_data/gm_token.load_token`，查 gmterm-serv.exe / gsgm3.exe 进程 `--token`）；
   终端重启后会话 token 轮换，无需重配。

## 回退预案

- 旧 goldminer3 仓库已冻结只读；回退 = 停用本策略 / 切回旧终端（旧 token 机制）。
- 若新终端数据/订阅异常：P1 facade 降级腾讯兜底（gm 不可用自动回退）。

## 验证记录（P4-4 复测，2026-08-28）

- token 发现链命中：`load_token()` 返回新终端会话 token（gmterm-serv.exe `--token`）。
- P0-1 配额探针新终端复跑：日线 800×41 ok、60s×240×41 ok、subscribe 41/41 收到 60s bar、指数 900 行。
- Python 依赖：pandas 3.0.5 / numpy 1.26.4 / requests 2.34.0（系统 Python311，与 superTrader 同环境）。
