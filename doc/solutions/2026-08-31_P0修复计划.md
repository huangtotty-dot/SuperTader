# 2026-08-31 晚间 P0 修复计划

> 依据：复盘清单 V1.3 铁律——每日只记录不改参，**唯一例外：P0 级故障（告警失效/数据缺失/reconcile 类运营事故）当日处理**。
> 本计划只含故障修复，**不含任何阈值/参数变更**；结构类（②）与数值类（③）问题一律留到周六管线。
> 红线：代码改动必 commit，逐项独立 commit，次日复盘验证。

## 一、必须今晚解决（P0：告警失效 / 数据缺失）

### P0-1 auto 飞书推送通道修复（告警失效，43/43 sent=false）
- **根因（已定位）**：`runtime/feishu_webhook.txt` 缺失（runtime/ 目录不存在），`GM_FEISHU_WEBHOOK` 环境变量未设置 → `execution/auto/_gm/gm_bridge/feishu.py:24 get_webhook()` 返回空串，全天发送短路。与 08-17 事故同型。
- **修复动作**：
  1. 📝 **owner 操作项**：提供「掘金模拟盘」机器人的 webhook URL（关键词=掘金模拟盘）。注意不能复用 manual 侧 webhook（`t_io/state/config.json`，关键词=做T猎手预警），关键词不匹配会被飞书拦截。
  2. 创建 `runtime/feishu_webhook.txt`（纯 URL 一行；runtime/ 已 gitignore，不入库）。
  3. 用 watcher 自检或手动调 `send_feishu_payload` 发一条测试消息验证 sent=true。
- **加固（防第三次复发）**：watcher 启动自检增加「webhook 可解析」断言，失败时打印明显错误（当前只 print，无声失败）。
- **验证**：明日 S0/B-2 对账 sent=true。
- ⚠️ 若 owner 今晚无法提供 URL，则设 `GM_FEISHU_WEBHOOK` 环境变量亦可；两者都缺则明早盘前必须完成，否则明日仍是"聋子日"。

### P0-2 backtrace「缺失」订正（实为路径口径漂移，数据没丢）
- **实况（已核实）**：backtrace 当日正常落盘在 `execution/auto/gmcache/backtrace.jsonl`（今日 281 条，14:58 更新）。清单期待的合并后路径 `t_io/logs/auto_backtrace.jsonl` 从未接线。
- **修复动作（二选一，建议 a）**：
  a. eod_sync 或复盘采集加一步：把 `execution/auto/gmcache/backtrace.jsonl` 当日部分导出/复制到 `t_io/logs/auto_backtrace.jsonl`；
  b. 修订清单 V1.3 路径为实际路径。
- **验证**：明日 B-2「backtrace 落盘」项在清单路径下可查。
- 备注：今日红色判定因此项而起，修复路径后该红视为"口径事故"而非"数据丢失"，但当日判定不回改（历史不改写）。

### P0-3 竞价采集修复（数据缺失，`ModuleNotFoundError: core`）
- **根因（已定位）**：`t_io/preopen/auction_collector.py` 函数内 `from core.market_data.tencent_provider import TencentProvider`（行 37/47），但 `sys.path` 不含项目根（BASE 在行 22 自解析正确，只是没加进 sys.path）。
- **修复动作**：行 22 后加 `sys.path.insert(0, str(BASE))`；随后执行 `python t_io/preopen/auction_collector.py --backfill --date 2026-08-31` 回填今日 9:25 替代口径。
- **验证**：生成 `t_io/preopen/auction_2026-08-31.json` 且 rows 非空。

### P0-4 preopen 焦点名单连续 3 日无产出（08-27/28/31，且无日志）
- **修复动作**：定位 preopen 生成器的调度入口（cron/启动脚本），补调度或补日志——今晚最低限度做到"失败必有日志、缺产出必告警"，避免再次无声缺口。
- **验证**：明日 09:25 前产出 `preopen_2026-09-01.json`，或失败时日志可归因。

### P0-5 renko 复盘脚本口径修复（复盘工具假阴性）
- **根因（已定位）**：`scripts/renko_t_daily_review.py:44` 读 `renko_t_{date}.jsonl`，实际落盘自 08-27 起为 `renko_t_{date}_{date}.jsonl`，失败时静默报"无信号"。
- **修复动作**：修正文件名匹配（兼容两种命名），无文件时显式输出 🔴 报警而非"信号数 0"；修后重跑 `--date 2026-08-31` 与今日复盘 §4.6 人工重算结果（17 买/12 卖、闭环胜率 83.3%）交叉验证。
- **验证**：脚本输出与人工数字一致。

### P0-6 sentiment_daily 今日行未落盘（数据缺失）
- **修复动作**：查 sentiment 落盘路径（文件 mtime 14:31 但无 08-31 行，疑似写入失败），修复后补记今日行。
- **验证**：`t_io/logs/sentiment_daily.jsonl` 出现 2026-08-31 行。

## 二、今晚可选（告警类加固，不算改参）

- **S-1 启动自检加"GM 交易会话登录态 + cash>0"断言**（对应今日账号未登录 2.5h、6 笔拒单）：未通过即飞书加急告警（依赖 P0-1 先修复）。
- **S-2 拦截可见性推送 sent=false 时不写防重标记**（002451 被拦全天零感知）：逻辑上属 bug 修复（防重 key 写入时机移到发送成功回调后），但涉及推送链路行为变更——**若今晚改动多、明天想观察，可降级到周六**。建议今晚改，改动面小。

## 三、明确留到周六（09-05 合并周复盘）——今晚不动

| 项 | 类别 |
|---|---|
| "卖全推、买全封"不对称结构（Q-20260831-B2，用户反馈=不认可接回环境，方向需重估） | ②结构 |
| base 通道对 data_insufficient 票递延一致性（B1） | ②结构 |
| regime 午后再评估机制（B4） | ②结构 |
| 最低佣金口径、尾盘强平占比、参考建议 VWAP 偏离（C5） | ③数值 |
| 600176 T+5 手工补结、样本 2→0 回退核查、C18 上线、三周候选积压 | 周六管线 |
| parity verdict 落盘、515180 mirror 残留、D10 字段持久化等 | 运营排期 |

## 四、执行顺序与验证

1. P0-1（先要 webhook URL）→ 发测试消息
2. P0-3 → 回填今日竞价 → P0-4（同源排查顺手做）
3. P0-5 → 重跑今日 renko 验证
4. P0-2 → 导出/接线 → P0-6
5. 可选 S-1 / S-2
6. 每项独立 commit；全部完成后在明日复盘 S0/S5 逐项验收

**预计工作量**：P0-1/2/3/5 合计约 30 分钟（均为小改动）；P0-4/6 需先定位调度与写入路径，约 30-60 分钟。
