# 上线验收报告（2026-08-21 评审方案 · A/B/C 批）

> 验收人：Agent ｜ 验收时间：2026-08-21 16:10 ｜ 依据：`评审纪要暨修改方案_20260821.md` 逐项验收标准
> 被验提交：f74725b3（A 批）、802b38a4（A-5+在途）、7b99f46e（B 批）、69150c49（C 批）、1ae3b25a（在途代码/产物）

## 一、验收总表

| 项 | 内容 | 代码审阅 | 运行验收 | 结论 |
|---|---|---|---|---|
| A-1 | forward_tracker BASE 自解析 + 每日管线接入 + 退出侧 max_drawdown | ✓ 自解析 `parents[3]`；daily_review 末尾调 `run_settle`，异常仅 warn | ✓ daily_review 全量跑通，`generated_at` 更新至 08-21 15:59，456 条 verdict 结算，**signal T+5 样本 0/20 → 2/20** | **通过** |
| A-2 | 竞价分析回写 preopen auction_summary | ✓ `_writeback_auction_summary` 读改写合并 6 字段，推送成功后触发 | 待次日 09:31 实战观察 | **通过（待实战确认）** |
| A-3 | Top20 空返回重试 + empty 标注 | ✓ 拆 `_fetch_top20_attempt`，重试 1 次/20s，仍空标 `top20_status=empty` | 与 A-2 同待实战 | **通过（待实战确认）** |
| A-4 | open_gap 单位注释 | ✓ preopen.py + 每日Review.md 双处 | — | **通过** |
| A-5 | 300153/300364 archived | ✓ status=archived，过滤条件排除 | ✓ 两股已 archived | **通过** |
| B-1 | 指数缓存日期校验 | ✓ 工作日≥09:15 且缓存<今日则重拉；失败回退+stale 标记 | ✓ 缓存已含 08-21；`_regime` 返回 `index_cache_stale` 字段 | **通过** |
| B-2 | C20 竞价校验（双条件与门） | ✓ 与门逻辑、降级单条件、_B 行剔除、只纠乐观错（uni_up）均符合 | ✓ 回放：08-19→**Level2**、08-17→0（与门防误伤）、08-21→0、08-18→0（无数据安全不触发）、08-11→Level1 | **通过** |
| B-3 | regime 缓冲带 1.005 | ✓ 中间带归 range，config `regime_up_buffer` 可调 | ✓ 08-17~08-21 全 range（08-18 ratio 1.0023 被缓冲带拦下） | **通过** |
| B-4 | range 市 watch_signal | ✓ 回撤用多头口径 -3%；position=None 不推不建仓 | ✓ 离线重跑 08-18：002202/002451/300054 → watch_signal score=60 | **通过** |
| C-1 | SELL_HIGH 跳过共振门控 | ✓ 门控内部 bypass + `RESONANCE_GATE` 开关；trace 状态可追溯 | 执行方自验 bypass✓/BUY_LOW 不 bypass✓ | **通过** |
| C-2 | 个股 MA5 闸（破线只卖不买） | ✓ 数据缺失返回 None 不拦截（安全向）；trace 写 below_ma5 原因 | ✓ 运行实测：600176/000988/588170/002639 当前全部 below_ma5=True（符合灾后现实） | **通过** |
| C-3 | 拦截可见性 | ✓ 低优飞书 + 每股每向每日 1 条去重 json | 待实战 | **通过** |
| C-4 | 高开急拉预警 | ✓ 09:30-09:45 窗口、gap>0.03 小数口径正确、冲高>2%、每日 1 条 | 待实战 | **通过** |
| C-5 | 信号评估白名单（C15+C25+C27） | ✓ qty=0 全跳过+trace；t_qty=0 评估不推；集中 `_signal_whitelist` 一处 | 执行方自验 603667/002639 零推送✓ | **通过** |

**捆绑红线检查**：B-1/B-2/B-3/B-4 同 commit（7b99f46e）整批上线 ✓；全部功能带 config 开关可回滚 ✓。

## 二、在途代码完整性

- 用户未提交的 30/60 分钟背离检测（position_builder +97 行）、t_gui 重构、divergence.py 已由执行方完整提交（802b38a4 / 1ae3b25a），工作区无残留丢失。✓

## 三、发现的偏差与遗留（不阻塞，登记）

1. **preopen 盘后被覆写**（轻微）：15:48/16:03 两次盘后运行把 preopen_2026-08-21.json 的 top20 从 09:25 竞价的 10/10 覆写为盘后 spot 的 17/3。对当日 C20 结论无影响（两种口径都不触发），但说明 preopen 引擎在非窗口期重跑会污染竞价口径——建议后续给 `auction_summary` 加"窗口锁"（09:31 后不再覆写 top20 字段），列入下轮候选。
2. **C20 取 slot 用 dict 序而非排序**（轻微）：`snaps[list(snaps.keys())[-1]]` 依赖插入序，当前采集器按时间插入没问题，建议改 `sorted(snaps)[-1]` 防御。
3. **main.py import 副作用重**（验收过程教训）：import 即启动 GUI、重置 VIRTUAL_TRADES、重写 t_mode.json/preopen——后续任何验收脚本不得 import main，用源码 exec 提取（本次已按此执行）。
4. **进程诊断订正**：t_trader 主循环 14:52 停跳，但 GUI 进程（pid 19980）仍存活——主循环可能 hang 而非退出。已订正 2026-08-21_复盘.md §3.5。**周一开盘前需人工确认进程状态并重启**。

## 四、未交付项（符合排期，不属本次验收范围）

- D-1 C18 待确认清单：排期下周二前（依赖 B-4 watch_signal 供给——已就绪）
- E-1 竞价量能 09:25 末段采集、E-2 加仓框架主线落地、E-3 现金水位校验、C29 心跳失联告警

## 五、下周一实战观察清单（按优先级）

1. **08:45-09:25 S0**：reconcile 正常 + 主循环重启确认 + t_qty 两问（600481/000988）用户拍板
2. **09:26 C20 首日实战**：基调推送卡片是否带竞价校验（触发看横幅、不触发看日志 `C20_level` 标记）
3. **09:31 A-2/A-3 实战**：preopen auction_summary 是否回写、Top20 是否还有 0/0
4. **盘中 C 批首日**：持仓全破 MA5（当前 4/4 below）→ 预期买推送为 0 + 出现"已拦截"低优提示；SELL_HIGH 不再被共振拦
5. **盘后**：daily_review 自动结算 signal_outcomes（A-1 第二日）、建仓扫描 trace 应出现 watch_signal 行
