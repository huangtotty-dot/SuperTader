# t_trader 周末测试优化清单 v2（2026-07-25/26）

> 背景：07-24 深度复盘 → V1.30/V1.30b 系统修复（commit `f207d26`，基线 `847c691`）。
> v2 变化：**每一步给出量化指标表（指标 / 目标值 / 测量方法 / 判定）**，全部用数字判定，不接受"看起来正常"。
> 基准锚点（已实测，判定用）：基线组 07-24 回放 = 54 推送 / 8 幽灵账 / 000988 空推×12 / 588170 卖 4000 股 / 600176 轮次 34 / shadow 0 条。
> 铁律：本周末**不改任何买卖阈值**（除非 Phase B3 样本外验证通过）。

---

## Phase A：修复点静态验证（周六上午，约 2 小时）

> 工具：`replay_day.py`；产物：`t_io/replay/{baseline_trace, fixed_trace, fixed_locked}/`

### A1. 卖出地板验证

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 588170 卖出总股数 | **= 2000 股**（恰好 base 4000 × 0.5） | `fixed_trace/summary.json` 按 code 汇总 SELL qty | 硬指标 |
| 588170 卖出笔数 | **= 2** | 同上按笔计数 | 硬指标 |
| `sell_floor_protect` 拦截次数 | **≥ 1**（第 3 笔卖出必须被拦） | grep `events.jsonl` | 硬指标 |
| `max_sell_times` 拦截次数 | **≥ 1** | grep `events.jsonl` | 硬指标 |
| 全标的净持仓底线 | 每只 ≥ base × 0.5（588170≥2000、600176≥150、600481≥700、603667≥200、000988≥150） | summary.json 期末持仓 | 硬指标 |

### A2. 幽灵账与空推送消除验证

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 幽灵入账笔数（cycle=0 却有虚拟记录） | **0**（基线 = 8：600481×5、603667×3） | 对比 `virtual_trades.json` 与 events 推送序列 | 硬指标 |
| qty=0 空推送条数 | **0**（基线：000988×12 + 600176 午后连续空推） | grep events 推送记录 qty 字段 | 硬指标 |
| 虚拟记录↔推送事件映射率 | **100%**（每条账能指到一条真实推送） | 脚本逐条 join（time+code+side） | 硬指标 |

### A3. 盘中状态持久化验证（重启不清零）

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 重启前后 cycle_count 连续性 | 重启后首个 cycle **= 中断时值**（不归 0） | 11:00 附近 kill 进程续跑，读 `intraday_state.json` | 硬指标 |
| 全程轮次上限 | 588170 ≤ 2、600176 ≤ 4、其余 ≤ 4 | summary.json 轮次字段 | 硬指标 |
| 冷却期内重复推送 | **0 条**（冷却期信号应为 `HOLD_*_COOLDOWN`） | grep decision_trace 冷却窗口 | 硬指标 |
| 开关正确性 | 实盘/回放 `PERSIST_INTRADAY_STATE=True`，optimizer 回测 `=False` | 查两处调用点配置 | 检查项 |

### A4. HOLD 原因码覆盖验证

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| HOLD 原因码覆盖率 | **100%**（裸 HOLD = 0 条） | 脚本统计 `fixed_trace/decision_trace_2026-07-24.jsonl` | 硬指标 |
| 原因码种类 | ≥ 5 类均出现（BUY_BLOCKED / SELL_BLOCKED / COOLDOWN / SELL_PRIORITY / BELOW_THRESHOLD） | 同上 group by | 参考指标 |
| 抽查可解释性 | 588170 11:10 前后记录能直接读出"买分 47 被 sell_priority/buy_blocked 压制" | 人工抽 5 条 | 检查项 |

### A5. shadow_signals 恢复与复盘链路验证

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| shadow 文件存在性 | `fixed_trace/shadow_signals_2026-07-24.jsonl` 存在 | ls | 硬指标 |
| shadow 记录条数 | **≥ 400 条**（实测锚点 428） | wc -l | 硬指标 |
| daily_review.py 读取 | 退出码 0、异常数 0、输出非空信号清单 | 运行并观察 | 硬指标 |
| 记录含决策原因码 | 100% | 脚本抽 20 条检查字段 | 硬指标 |

---

## Phase B：完整 Optuna 寻优（周六下午~晚上挂机）

> 前提：Phase A 硬指标全过。

### B1. 五标的单月冒烟（2025-06）

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 每只 n_exceptions | **= 0** | optimizer 诊断计数输出 | 硬指标 |
| FAILED trial 数 | **= 0** | Optuna study 统计 | 硬指标 |
| 四层漏斗可解释性 | n_signals ≥ n_pushed + n_blocked，且 n_signals > 0（锚点：600176 单月 = 80/20/16/0） | 诊断输出 | 硬指标 |
| 任一标的 0 信号 | 不允许 | 同上 | 闸门：不达标先修链路 |

### B2. 全量寻优（5 标的 × 2025-03~08，n_trials ≥ 100/只）

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 有效 trial 占比（非 -9999） | **≥ 30%**（修复前 = 0%，63% 全 -9999） | study trials 分布统计 | 硬指标 |
| 评分梯度 | best 与 worst（有效 trial 内）差值 ≥ 5 分；best ≠ 唯一极值点（前 10 trial 分数互不相同） | study 排序 | 硬指标 |
| best vs 默认 composite | best ≥ 默认 + 3 分 → 进 B3；差值 < 3 分 → 结论"现参数合理"，跳 Phase C | 同窗口默认参数对照跑 | 分支判定 |
| 参数重要度报告 | 输出 ≥ 1 份（fanova 或 importance 排序） | Optuna importance | 交付物 |

### B3. 样本外验证（2025-09~11）

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 样本外 composite | best_params ≥ 默认参数 × **100%**（不劣化） | 同区间双跑对照 | 硬指标 |
| 样本外闭环数 | best_params ≥ 默认 × **90%** | 回测交易配对统计 | 硬指标 |
| 采纳决策 | 两项全过 → 列"待采纳"（周一后再进 config）；任一不过 → **放弃，维持现阈值** | — | 铁律 |

---

## Phase C：数据质量修复（周日上午，约 1.5 小时）

### C1. pre_close 陈旧校正

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| pre_close 正确率 | 5/5 标的 = 真实 07-23 收盘：588170=0.98、600176=38.36、600481=3.94、603667=52.75、000988=111.91 | 对照 tushare/akshare 日线 | 硬指标 |
| today_ret 误差 | 每标的 \|计算值 − 真实涨跌幅\| ≤ **0.5%**（当前 600176 误差 ≈ 10 个百分点） | 校正后重跑回放对比 | 硬指标 |
| pre_close 每日自动更新 | 收盘流程中存在且生效（若无 → 补代码；有 → 验证周五已更新） | 走查 main.py 收盘段 + 实测 | 硬指标 |
| 校正后评分位移 | 重跑 07-24 回放，逐时点买/卖分与校正前差异可解释（±5 分内占 ≥ 90%） | 双跑 diff 脚本 | 参考指标 |

### C2. qmt_orders 落盘恢复

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 断写根因定位 | 明确到文件:行号（路径变更/异常吞掉/重写丢失 三选一） | 走查 qmt_trader.py | 交付物 |
| 委托捕获率 | 周一实盘委托 100% 落盘（委托数 = qmt_orders.jsonl 新增条数） | 周一盘后核对 | 硬指标 |

---

## Phase D：三市况回放——防修复过拟合单日（周日下午，约 2 小时）

### D1. 三市况对照（基线 vs 修复版）

| 市况 | 指标 | 目标值 | 判定 |
|---|---|---|---|
| 横盘震荡日 | 正常 T0 闭环数（修复/基线） | **≥ 90%** | 硬指标：低于 → 查地板/轮次是否过紧，可调 `sell_floor_ratio`（结构参数），**不可动阈值** |
| 横盘震荡日 | 做T利润（修复/基线） | **≥ 90%** | 硬指标 |
| 单边上涨日 | 高抛卖出主段参与率（修复/基线卖出均价差） | 修复版卖出均价 ≥ 基线 × **99%** | 硬指标：地板不得显著压低高抛均价 |
| 单边下跌日（07-24 已测） | 底仓保留率 | **≥ 50%**（已达标：2000/4000） | 复核 |
| 三种市况 | 系统性漏单（该交易未交易且无原因码解释） | **0 笔** | 硬指标 |

### D2. 止损联动验证（止损从未实盘触发过）

| 指标 | 目标值 | 测量方法 | 判定 |
|---|---|---|---|
| 止损触发正确性 | 构造下跌序列中止损推送触发率 100%（该触发必触发） | 回放构造用例 | 硬指标 |
| 单 K 线退出类推送数 | **= 1 条**（止损/熔断/保本有明确优先级，不重复） | grep events | 硬指标 |
| 止损记录质量 | 100% 带 price、正常入账、可配对 | 查 virtual_trades | 硬指标 |
| 止损与熔断互斥 | 同一持仓同一时点不同时出现止损推送 + 熔断误放行买入 | 回放检查 | 硬指标 |

---

## Phase E：周一开盘前冒烟 + 实盘首日（周日晚~周一全天）

### E1. 干跑自检（不连 QMT）

| 指标 | 目标值 | 判定 |
|---|---|---|
| 启动异常栈 | **0 条** | 硬指标 |
| intraday_state.json 加载 | 成功且字段完整（cycle/cooldown 非空） | 硬指标 |
| preopen 产出 | 正常生成 preopen_trace | 硬指标 |
| 面板裸 HOLD | **0 条**（全带原因码） | 硬指标 |

### E2. 熔断显性化（若盘前判 uni_down）

| 指标 | 目标值 | 判定 |
|---|---|---|
| "🧊 买入熔断"飞书卡 | **恰好 1 张/日**（不多发不漏发） | 硬指标 |
| 面板熔断文案 | 显示"停手：买入熔断(单边下行)"，非静默 HOLD | 硬指标 |

### E3. 实盘首日监控点（09:30-15:00）

| 指标 | 目标值 | 判定 |
|---|---|---|
| qty=0 空推送 | **0 条** | 硬指标 |
| 地板拦截（若触发卖出潮） | 每标的净持仓 ≥ base × 0.5，拦截日志 ≥ 1 | 条件触发 |
| 14:50 同步校验 | 日志出现校验记录；记录不完整时 = 跳过 + 橙卡（不允许静默写入 holdings.json） | 硬指标 |
| shadow 盘中写入 | 15:00 时 `shadow_signals_2026-07-27.jsonl` 存在且 > 0 条 | 硬指标 |
| 重启状态保留（若发生重启） | cycle/cooldown 不归 0 | 条件触发 |
| 未解释异常栈 | **0 条**（ZeroDivisionError/NameError/AttributeError） | 硬指标 |

### E4. 周一收盘后首日审计（按 `doc/复盘检查清单.md`）

| 指标 | 目标值 | 判定 |
|---|---|---|
| virtual_trades price 字段覆盖率 | **100%**（缺 price = 0 条） | 硬指标 |
| 收益三方核对（virtual_trades vs decision_trace vs qmt_orders） | 数量差异 = **0** | 硬指标 |
| holdings.json 反写正确性 | 与虚拟账一致，无 phantom 仓位 | 硬指标 |
| 审计 est_pnl 可信度 | 逐对配对计算（非 avg 近似），伪影型天文数字 = 0 | 硬指标 |

---

## 验收总表（量化版）

| 阶段 | 硬指标数 | 通过标准 | 关键数字锚点 |
|---|---|---|---|
| A 修复验证 | 17 | 17/17 全过 | 588170=2000 股/2 笔；幽灵 0；空推 0；原因码 100%；shadow ≥400 |
| B 寻优 | 8 | 硬指标全过 + B3 有明确采纳/放弃结论 | 有效 trial ≥30%；样本外不劣化 |
| C 数据质量 | 5 | 5/5 全过 | pre_close 5/5 正确；today_ret 误差 ≤0.5% |
| D 三市况 | 9 | 9/9 全过 | 震荡日闭环 ≥90% 基线；单 K 线推送 =1 |
| E 实盘首日 | 13 | 硬指标全过 | 0 空推 / 0 异常 / price 覆盖 100% / 三方差异 0 |

> 任一硬指标不达标 → 当日修复后重测该阶段，**不带病进入下一阶段**。

## 明确不做的事

- ❌ 不改任何买卖阈值（除非 B3 样本外通过）
- ❌ 不动正 T 首买 max_buyable=0 的 sizer 设计（下轮单独评估）
- ❌ 不做盘中热部署（07-24 崩溃循环的教训：任何代码改动只在收盘后/周末进行）
- ❌ 不清理 `/e/06_T_baseline` worktree（稳定运行一周后再删）
