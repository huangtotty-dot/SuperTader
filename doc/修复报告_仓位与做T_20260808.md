# 修复报告 · 仓位与做T（position_sizer.py / main.py）

修复人：修复员_仓位与做T · 日期：2026-08-08 · 范围：仅 `position_sizer.py`、`main.py`
关联审核报告：`doc/审核_建仓信号与加仓观察_20260808.md`（P0-9 / P0-15 / P1-5）

## 修复明细

| 编号 | 文件:行号 | 改动内容 | 验证结果 |
|------|-----------|----------|----------|
| B1 单股上限死代码 | main.py:658-700（新增 `_compute_total_equity`）；1522-1548（调用点） | 新增 `_compute_total_equity()`：读 `t_io/state/portfolio_config.json` 双账户 total_capital，现金≈capital−持仓成本，市值用 DAILY_DECISION_STATS 快照最新价（回退 pre_close→cost），带 300s 缓存；在两处 `calc_sell_qty/calc_buy_qty` 调用补传 `total_equity=` | PASS（真实数据算出 272,463.66 元，量级正确） |
| B2 弱信号保底买一手 | position_sizer.py:291 | `calc_buy_qty` 取整前加 `if qty <= 0: return 0`（个股/ETF 统一，覆盖接回与首加两分支） | PASS（strength=1 弱信号买入返回 0） |
| B3 纯底仓口径 | position_sizer.py:129、233、356 | 三处 `t_qty or qty` 回退改为严格 `t_qty`；t_qty=0 时买卖均返回 0，与 main.py 动态份数口径一致 | PASS（t_qty=0/qty=500 时买=0 卖=0） |
| B4 双账户合并 | position_sizer.py:22-28（`set_all_holdings`）、86-95；main.py:1522 | 新增模块级 `_ALL_HOLDINGS_REF` + `set_all_holdings()`；`_check_position_limit` 按底层代码（`code.split("_")[0]`）合并 A/B 市值后再判定；main.py 调用前注入 `HOLDINGS` | PASS（A+B 各 15 万同代码合并 30 万>18 万上限→超额 800 股；未注入时单账户不误判） |
| B5/B6 min_unit 放大 | position_sizer.py:104（B5）、161/168/173/181（B6） | 超额/减半卖出量由「floor 后 max(min_unit)」改为「向上取整到 min_unit，若超过可卖净量则取净量向下整手」；覆盖 `_check_position_limit`、reduce/defensive 超额分支、reduce 减半分支（个股+ETF） | PASS（3 股超额→100；超额 200>净量场景取净量整手 300；减半不超净量） |
| B7 cost<=0 静默 | position_sizer.py:82-84 | cost/qty/price 合并判断拆开：cost<=0 时 `log.warning` 标注「数据缺失」后返回 0 | PASS（告警可见，返回 0） |
| 收盘档断供（P0-15 关联） | main.py:752-798（`_maybe_run_position_builder` 重写） | 根因：08-07 进程 15:00:02 进低频保活后疑似休眠/挂起（15:00~15:43 零心跳），错过 15:05-15:15 硬窗口；且原逻辑异常也写入日期占位→当日不重试。修复：①15:05 后任意时刻补扫一次（超 15:15 日志标注「（补扫）」）；②扫描成功才占位，异常 10 分钟节流重试；③补 weekday  guard 周末不扫（原逻辑周末 15:05 会误扫）；④eod 扫描与盘中线程共用互斥锁（acquire timeout=120s，超时跳过下轮重试，finally 释放） | PASS（15:20 补扫触发/当日不重复/周末不扫/15:05 前不扫/异常不占位+10min 重试/锁占用跳过/锁获取释放配对，共 9 项桩测试全过） |
| P1-5 扫描阻塞 | main.py:702-749（`_maybe_run_position_builder_intraday` 重写） | 盘中建仓扫描移入 daemon 线程（name=position-scan-intraday）：提交后立即返回不阻塞做T主循环；上一轮线程未结束则跳过本轮且不推进 5 分钟节流时间戳；与 eod 共用 `_position_scan_lock` 保证 trace 写盘互斥 | PASS（假线程桩测试 5 项全过；注：本沙箱环境无法真实启动线程——裸 Thread.start() 亦超时，属环境限制，逻辑路径已完整验证） |

## 验证

- `python -m py_compile position_sizer.py main.py` → 均通过
- position_sizer 功能冒烟 10 项全过（B2/B3/B4/B5/B6/B7 全覆盖）
- main.py 函数级桩测试（AST 抽取 + 桩环境）14 项全过
- 未提交 git；未改 config.json 任何用户参数

## 遗留说明

1. B1 现金口径为近似值（total_capital − 持仓成本，忽略已实现盈亏/手续费），如需精确现金需券商端对账数据，当前无此数据源。
2. B4 合并市值时姊妹账户（_B）价格用同一信号价估算（同一底层标的，合理近似）。
3. 收盘档「必然执行」依赖进程在 15:05 后至少存活一轮扫描；若进程整日未运行（如 08-07 用户 12:03 曾停止盯盘），任何调度修复都无法补扫，需配合进程守护/开机自启（超出本范围）。
4. `position_builder.py` 的 environ 清理、trace 内部写盘逻辑不在本人范围，互斥锁已保证本进程内 scan 不并发。
