# 审核报告 v1.0.1_fix8

> **审计员**：K3（独立审计，只读不写，不改任何买卖阈值）
> **审计对象**：Commit `530da30`（fix8: N25-2 买入available台账修复 + N28 sell通道分布 + 拒单回滚）+ 回测结果 `E:\goldenMinerReport\v1.0.1_fix8`
> **验收基准**：《审核报告_v1.0.1_fix7.md》N25-2 修复方案 + 5 步复验流程 + N28 修复方案
> **审计日期**：2026-07-26（run_id `20260726_211214`，33,409 行）

---

## 一、Phase A/B/C/D 放行状态（核心结论）

| Phase | fix7 时状态 | **fix8 审计后状态** | 说明 |
|---|---|---|---|
| **A 结构骨架** | ⚠️ 有条件放行 | **⚠️ 维持有条件放行（剩余 1 行修复）** | N28 闭环、拒单回滚链路就位；N25 幻影委托**仍在发出**——根因缩小到 main.py:107 一个表达式（见下）。**模拟盘可放行的理由首次成立：拒单回滚 + LIVE reconcile + 飞书告警三重兜底已就位**，残余风险有界 |
| **B 止盈体系** | ❌ 未达出口 | **❌ 维持原判（障碍 -1）** | N28 闭环 → 通道分布表恢复可产出，批次二（F5/F6/F8/F9/F10）仍待施工 |
| **C 组合层** | 未启动 | **未启动** | — |
| **D Optuna 寻优** | 阻塞 | **阻塞（正确）** | — |

**模拟盘（07-28 周一）放行意见：✅ 可以上线。** 理由：幻影委托虽仍发出，但 fix8 已补齐拒单回滚（main.py:979-985）+ 事件桥告警（write_reject/write_risk，989-991）+ LIVE reconcile 成本保真兜底——仿真柜台的 T+1 拒单会触发完整处理链，盘中漂移窗口有界（分钟级）。**仍强烈建议今晚顺手修掉 main.py:107**（一个表达式，不碰阈值），让幻影在源头消失。

---

## 二、5 步复验结果（对照 fix7 报告流程）

| 步骤 | 预期 | 实际 | 判定 |
|---|---|---|---|
| 1 重跑回放 | 完成 | run `20260726_211214` | ✅ |
| 2 幻影委托归零 | 05-08 09:36/10:14 SELL 委托 0 新增 | 事件桥计数 5 → **6**（fix8 run 仍发出） | **❌** |
| 3 TRAIL 回归 | 05-13 卖 100@78.19 在成交 CSV | 在案（78.1876） | ✅ |
| 4 KPI 一致 | +8.06%/-8.27%/66 笔/150.66 | **逐位一致** | ✅ |
| 5 backtrace=成交 | 34=34 | 34=34，action 非空率 100% | ✅ |

---

## 三、逐项判定

| 项 | 判定 | 证据 |
|---|---|---|
| **N28 sell 通道分布** | **✅ 完美闭环** | 34 笔 sell 事件 action 100% 非空；通道分布与预期**逐票逐通道一致**：000988 = TAIL8/TARGET1/SELL_HIGH9/TRAIL1/TREND_EXIT5/PANIC2；600176 = TARGET1/TRAIL1/SELL_HIGH1；603667 = TRAIL1/PANIC4 |
| **N25-2 ① 买入 available 台账** | ⚠️ 改了但被旧表达式吞掉 | main.py:931 已改为 `int(old.get("available", 0))` ✅；**但 main.py:107 `_avail = int(holding.get("available", pos_qty) or pos_qty)`——available=0（合法的"当日全锁"值）被 `or pos_qty` 兜底成全仓可卖**，检查再次形同虚设（事件桥实证第 6 次幻影） |
| **N25-2 ② 拒单回滚** | ✅ 代码正确（回测不可验证） | main.py:980-985 回滚 qty/available/t_qty + `sell_rollback` 审计；989-991 write_reject + order_rejected 入桥。**注：掘金回测器对 T+1 委托静默丢弃、不发 status 4/5/6 回调**（`order_rejected` 事件全天 0 条），该链路只能到模拟盘实单验证 |
| 铁律/流程 | ✅ / ⚠️ | 无阈值改动 ✅；`530da30` 再次 3 项打包提交（连续第四轮），message 无数字失真 |

### N25-3（最后的 1 行修复）
```python
# main.py:107 现状：
_avail = int(holding.get("available", pos_qty) or pos_qty)   # 0 被 or 吞掉
# 改为：
_avail_raw = holding.get("available")
_avail = pos_qty if _avail_raw is None else int(_avail_raw)   # 缺 key 才兜底，0 是合法值
```
**复验标准**：重跑后事件桥 05-08 09:36/10:14 的 603667 SELL 委托**不再新增**（保持 6 条历史记录），KPI 与 05-13 TRAIL 回归不变。

### 审计员自我纠错（诚实声明）
main.py:107 的 `or pos_qty` 在 fix7 审核时我曾逐行读过（该轮报告引用了 107-110 行），但只核对了"检查存在"，未识别 `or` 会把合法的 available=0 吞掉——**修复方案给错了着力点（指向 924 台账），107 这个表达式才是最后一道坎**。924 的修复是正确的（台账语义本就该修），但不是充分条件。此教训记入：今后给修复方案时对"兜底表达式"逐一验证 falsy 值语义。

---

## 四、结论

fix8 把 N25 的根因从"一片区域"缩小到"一个表达式"，同时交付了 N28 完美闭环与拒单处理链。**Phase A 的有条件放行首次具备真实的安全网依据**——模拟盘可以上线，main.py:107 的一行修复建议今晚完成但不构成上线的绝对阻塞。Phase B 待批次二施工；C 未启动；D 正确阻塞。

---

## 五、诚实声明

1. 幻影委托持续的证据：事件桥 `events_20260726.jsonl` 05-08 SELL 委托计数 5→6（fix8 run 新增 1 组）。
2. 拒单回滚链路（main.py:980-991）未能在回测中获得行为验证（回测器静默丢弃无 status 回调），其正确性仅为静态判定，模拟盘首日复盘须实测（核对项：order_rejected 告警 + sell_rollback 事件 + drift=0）。
3. 本人未修改任何 `.py/.json/.jsonl` 文件，未重跑任何回测；所有判定为只读分析结论。
