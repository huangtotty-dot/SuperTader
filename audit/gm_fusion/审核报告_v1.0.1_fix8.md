# 审核报告 v1.0.1_fix8

> **审计员**：K3（独立审计，只读不写，不改任何买卖阈值）
> **审计对象**：Commit `530da30`（fix8: N25-2 买入available台账修复 + N28 sell通道分布 + 拒单回滚）+ 回测结果 `E:\goldenMinerReport\v1.0.1_fix8`
> **验收基准**：《审核报告_v1.0.1_fix7.md》N25-2 修复方案 + 5 步复验流程 + N28 修复方案
> **审计日期**：2026-07-26（run_id `20260726_211214`，33,409 行）

---

## 一、Phase A/B/C/D 放行状态（核心结论）

| Phase | fix7 时状态 | **fix8 审计后状态** | 说明 |
|---|---|---|---|
| **A 结构骨架** | ⚠️ 有条件放行 | **⚠️ 维持有条件放行（剩余 1 行修复）** | N28 闭环、拒单回滚链路就位；N25 幻影委托**仍在发出**——根因缩小到 main.py:107 一个表达式（见下）。**模拟盘可放行的理由首次成立：拒单回滚 + LIVE reconcile + 飞书告警三重兜底已就位**，残余风险有界 |
| **B 止盈体系** | ❌ 未达出口 | **❌ 维持原判（障碍 -1）** | N28 闭环 → 通道分布表恢复可产出，批次二（F5/F6/F8/F9/F10）仍待施工 |
| **C 组合层** | 未启动 | **未启动** | — |
| **D Optuna 寻优** | 阻塞 | **阻塞（正确）** | — |

**模拟盘（07-28 周一）放行意见：✅ 可以上线。** 理由：幻影委托虽仍发出，但 fix8 已补齐拒单回滚（main.py:979-985）+ 事件桥告警（write_reject/write_risk，989-991）+ LIVE reconcile 成本保真兜底——仿真柜台的 T+1 拒单会触发完整处理链，盘中漂移窗口有界（分钟级）。**仍强烈建议今晚顺手修掉 main.py:107**（一个表达式，不碰阈值），让幻影在源头消失。

---

## 二、5 步复验结果（对照 fix7 报告流程）

| 步骤 | 预期 | 实际 | 判定 |
|---|---|---|---|
| 1 重跑回放 | 完成 | run `20260726_211214` | ✅ |
| 2 幻影委托归零 | 05-08 09:36/10:14 SELL 委托 0 新增 | 事件桥计数 5 → **6**（fix8 run 仍发出） | **❌** |
| 3 TRAIL 回归 | 05-13 卖 100@78.19 在成交 CSV | 在案（78.1876） | ✅ |
| 4 KPI 一致 | +8.06%/-8.27%/66 笔/150.66 | **逐位一致** | ✅ |
| 5 backtrace=成交 | 34=34 | 34=34，action 非空率 100% | ✅ |

---

## 三、逐项判定

| 项 | 判定 | 证据 |
|---|---|---|
| **N28 sell 通道分布** | **✅ 完美闭环** | 34 笔 sell 事件 action 100% 非空；通道分布与预期**逐票逐通道一致**：000988 = TAIL8/TARGET1/SELL_HIGH9/TRAIL1/TREND_EXIT5/PANIC2；600176 = TARGET1/TRAIL1/SELL_HIGH1；603667 = TRAIL1/PANIC4 |
| **N25-2 ① 买入 available 台账** | ⚠️ 改了但被旧表达式吞掉 | main.py:931 已改为 `int(old.get("available", 0))` ✅；**但 main.py:107 `_avail = int(holding.get("available", pos_qty) or pos_qty)`——available=0（合法的"当日全锁"值）被 `or pos_qty` 兜底成全仓可卖**，检查再次形同虚设（事件桥实证第 6 次幻影） |
| **N25-2 ② 拒单回滚** | ✅ 代码正确（回测不可验证） | main.py:980-985 回滚 qty/available/t_qty + `sell_rollback` 审计；989-991 write_reject + order_rejected 入桥。**注：掘金回测器对 T+1 委托静默丢弃、不发 status 4/5/6 回调**（`order_rejected` 事件全天 0 条），该链路只能到模拟盘实单验证 |
| 铁律/流程 | ✅ / ⚠️ | 无阈值改动 ✅；`530da30` 再次 3 项打包提交（连续第四轮），message 无数字失真 |

### N25-3（最后的 1 行修复）
```python
# main.py:107 现状：
_avail = int(holding.get("available", pos_qty) or pos_qty)   # 0 被 or 吞掉
# 改为：
_avail_raw = holding.get("available")
_avail = pos_qty if _avail_raw is None else int(_avail_raw)   # 缺 key 才兜底，0 是合法值
```
**复验标准**：重跑后事件桥 05-08 09:36/10:14 的 603667 SELL 委托**不再新增**（保持 6 条历史记录），KPI 与 05-13 TRAIL 回归不变。

### 审计员自我纠错（诚实声明）
main.py:107 的 `or pos_qty` 在 fix7 审核时我曾逐行读过（该轮报告引用了 107-110 行），但只核对了"检查存在"，未识别 `or` 会把合法的 available=0 吞掉——**修复方案给错了着力点（指向 924 台账），107 这个表达式才是最后一道坎**。924 的修复是正确的（台账语义本就该修），但不是充分条件。此教训记入：今后给修复方案时对"兜底表达式"逐一验证 falsy 值语义。

---

## 四、结论

fix8 把 N25 的根因从"一片区域"缩小到"一个表达式"，同时交付了 N28 完美闭环与拒单处理链。**Phase A 的有条件放行首次具备真实的安全网依据**——模拟盘可以上线，main.py:107 的一行修复建议今晚完成但不构成上线的绝对阻塞。Phase B 待批次二施工；C 未启动；D 正确阻塞。

---

## 五、诚实声明

1. 幻影委托持续的证据：事件桥 `events_20260726.jsonl` 05-08 SELL 委托计数 5→6（fix8 run 新增 1 组）。
2. 拒单回滚链路（main.py:980-991）未能在回测中获得行为验证（回测器静默丢弃无 status 回调），其正确性仅为静态判定，模拟盘首日复盘须实测（核对项：order_rejected 告警 + sell_rollback 事件 + drift=0）。
3. 本人未修改任何 `.py/.json/.jsonl` 文件，未重跑任何回测；所有判定为只读分析结论。

---

## 六、Phase B 施工细则（批次二 WP 拆解，接 fix6 手册批次二）

> **定位**：Phase B 是当前唯一卡点。以下 WP 沿用"改动内容/测试方案/验收标准"三要素，行号锚点为 fix8 终态代码（1,015 行快照，存 `E:\06_T\audit\gm_fusion\v1.0.1_fix8\code\main.py`）。
> **全局纪律**：① 不改买卖阈值（唯一例外 WP-B1' 的 TRAIL 结构补交付，临时值带 TODO(PhaseD) 且 commit message 列明）；② **每 WP 独立 commit**——连续四轮打包提交，本轮必须纠正；③ commit message 数字须与产物可核对；④ 对照基线 = fix8 存档（KPI +8.06%/MDD -8.27%/66 笔，通道分布见 N28 验收值）；⑤ 模拟盘运行期间施工，任何 WP 上线前须先在同窗口回放验证，不得在模拟盘时段改线上代码（盘后施工、盘前确认）。

### WP-B0（前置，1 行）：N25-3 表达式修复
- **改动**：main.py:107 `_avail = int(holding.get("available", pos_qty) or pos_qty)` → 区分 None 与 0：
  ```python
  _avail_raw = holding.get("available")
  _avail = pos_qty if _avail_raw is None else int(_avail_raw)
  ```
- **测试**：全窗口回放。
- **验收**：事件桥 05-08 09:36/10:14 SELL 委托不再新增；KPI 与 05-13 TRAIL 回归逐位不变；其余同 fix7 报告 5 步流程。

### WP-B5'（1 行 + 回放）：开盘缓冲覆盖 TARGET_SELL
- **改动**：main.py:681 条件 `sig.action == "SELL_HIGH"` → `sig.action in ("SELL_HIGH", "TARGET_SELL")`。
- **测试**：全窗口回放，统计 09:31-09:35 非保护类卖出。
- **验收**：① 09:35 前 SELL_HIGH/TARGET_SELL 成交 **0 笔**（成交 CSV）；② 保护类即时性回归——603667 06-26 09:31 PANIC、05-13 09:31 TRAIL 仍在案；③ `morning_sell_blocked` 事件照常记录（含 TARGET_SELL 被拦样本，若窗口内有）。
- **回退**：单 commit revert。

### WP-B1'（半天）：N20 仲裁器优先级倒挂修复 + N23 TRAIL k×ATR 带界
- **改动 1（N20，2 处守卫）**：
  - main.py:686-696 TREND_EXIT 块：加守卫 `if (sig is None or sig.action not in ("PANIC_SELL", "TRAIL_SELL")) and ...`——不得覆盖更高优先级信号；
  - main.py:698-709 TARGET_SELL 块：守卫名单（现排除 TREND_EXIT/PANIC）**补入 TRAIL_SELL**。
  - 修复后声明优先级 `P1 PANIC > P2 TRAIL > P3 TREND_EXIT > P4 TARGET > P5 SELL_HIGH` 与代码逐条一致（PANIC 无条件覆盖正确，保留）。
- **改动 2（N23，TRAIL 触发线结构化）**：main.py:666
  ```python
  _back = max(0.03, min(1.5 * daily_atr, 0.08))  # TODO(PhaseD): 寻优 MIN_BACK/k/MAX_BACK
  if _drawdown > _back and not _panic_on_cooldown:
  ```
  `daily_atr` 取自本票 daily_ctx（N18 修复后已按票隔离）；激活线 0.08（main.py:658）维持临时值不变。
- **测试**：① 状态机单测（本地脚本，模拟价格序列）：INACTIVE→ARMED→触发→COOLED→复位全路径 + 带界上下限截断分支 + 部分卖出后 peak 保持；② **fix2 场景主验收回放**（000988 单票、底仓 800、20 万、fix2 同窗口）；③ 同 bar 双通道候选统计（TRAIL 与 TREND_EXIT 同 bar 均满足时执行 P2）。
- **验收（可证伪）**：① fix2 场景 TRAIL 退出均价 ≥ 成本×1.07（fix2 实际结局 -33.3% 的对照）；② 单测全路径通过；③ backtrace TRAIL_SELL 记录带 profit_pct/trail_peak/触发线三字段；④ 同 bar 双候选仲裁符合优先级表、无重复卖/超卖；⑤ 全窗口回放 KPI 与 fix8 基线差异逐笔归因（ATR 带界使部分 TRAIL 触发点移动属预期，须列清单）；⑥ 5 月震荡段误触发次数入报告（供 Phase D 定值）。
- **回退**：`ENABLE_TRAIL` 开关 + revert。

### WP-B2'（文档 WP，无代码）：600176 尾仓处置显性决策
- **背景**：TREND_EXIT 只卖超 base_ref 部分（main.py:692 `_excess`），尾仓（=底仓本体）设计上永不触发；叠加地板保护（base×50%=150）与最小单位约束，600176 尾仓 200 股每 run 触发 floor_protection 27 次（fix6 实证）——退出通道实质只剩 100 股空间。
- **决策选项**：(a) 底仓 0% 处置（与实盘"底仓 intact 默认态"同构，K3 建议）；(b) BREAKDOWN 时底仓减半；(c) 底仓全部落袋。
- **交付物**：决策写入 fix5 手册 B2 节（底仓处置比例 ⟨占位⟩ → 定值 + 理由）+ 本报告留痕；B2 验收标准同步修订为"利润仓落袋、底仓不被 TREND_EXIT 触碰"。
- **验收**：文档修订完成；全窗口回放中任意时点 TREND_EXIT 卖出后持仓 ≥ base_ref（持仓 CSV 可证，fix6/fix8 数据已支持该判定）。
- **附带处理**：若选 (a)，600176 的 floor_protection 高频事件属预期行为，watcher 已折叠不推飞书（fix5 已配），无需再改。

### WP-B4（可选，不阻塞出口）：TARGET_SELL L2/L3 分档
- 现状 L1（+10%，一次性）已满足 fix5 手册 B4 验收（≥1 档落袋、同档 0 重复）。L2/L3 刻度为 ⟨占位⟩，**建议整体推迟到 Phase D 寻优时一并定值**，Phase B 出口不作要求。

### WP-B-回放包（3 次回放）：三窗口场景包（Phase B 出口硬材料）
| 场景 | 配置 | 关键验收指标 |
|---|---|---|
| fix2 场景 | 000988 单票、底仓 800、20 万、fix2 同窗口 | TRAIL 退出均价 ≥ 成本×1.07（WP-B1' 复用） |
| fix3 场景 | 000988 单票、底仓 800、20 万、fix3 同窗口（+10.6% 峰值） | TARGET_SELL ≥1 档落袋且同档 0 重复 |
| fix4 场景 | 4 标的、15 万、2026-04-27~07-24 全窗口 | 亏损 SELL_HIGH 0 笔；连续 ≥5 日割肉序列 0；09:35 前非保护卖出 0 笔；绞肉机场景结构性消失 |
- **每包交付**：回放 PDF + 交易/净值/持仓 CSV + backtrace.jsonl + **通道分布表**（N28 闭环后已可产出）+ 与 fix8 基线的差异归因清单，统一存 `E:\06_T\audit\gm_fusion\v1.0.1_fix9\<场景>\`。

### WP-B-出口（K3 验收 checklist）
- [ ] B0：幻影委托源头消除（事件桥无新增）；
- [ ] B1：WP-B1' 验收 6 项全部打钩（含 fix2 场景 ≥1.07）；
- [ ] B2：WP-B2' 决策落地 + 利润仓落袋行为可证 + 底仓未被 TREND_EXIT 触碰；
- [ ] B3：亏损 SELL_HIGH 0 笔（fix4 场景复证，fix6/fix8 已连续两轮 0 笔）；
- [ ] B4：fix3 场景 ≥1 档落袋、同档 0 重复、无超卖；
- [ ] B5：WP-B5' 验收 3 项全部打钩；
- [ ] 三窗口回放包含齐、通道分布表含全通道枚举、backtrace=成交逐笔一致；
- [ ] 模拟盘首周复盘数据无 P0 级新缺陷（拒单链路实测、drift、飞书时延）。
- **出口判定**：全部打钩 → **Phase B 关闭，准许拆解 Phase C（组合层）。**

### 施工节奏（与模拟盘并行）
| 时间 | 动作 |
|---|---|
| 07-26 晚 | WP-B0（1 行）+ 回放复验 |
| 07-28（周一） | **模拟盘上线**；盘后 WP-B5' + WP-B2' 决策记录 |
| 07-29 ~ 07-31 | WP-B1' 施工 + 单测；模拟盘每日复盘照常 |
| 08-01 ~ 08-02 | 三窗口回放包 |
| 08-03 | K3 出口验收 → Phase B 关闭 |
