# 审核报告 v1.0.1_fix7

> **审计员**：K3（独立审计，只读不写，不改任何买卖阈值）
> **审计对象**：Commit `d51b9ee` / `5ed1ec2`（另核对中间两个未申报 commit：`1bb5d6d` fix6-F4 回放存档、`fcb5914` chore，均在 git 日志中，无漏报）
> **验收基准**：《审核报告_v1.0.1_fix6.md》第七节 fix7 清单（N25/N26/N27/F2 补全）
> **审计日期**：2026-07-26
> **回测窗口**：2026-04-27 ~ 2026-07-24（run_id `20260726_194455`，backtrace 已按 run 重置，33,409 行）

---

## 一、Phase A/B/C/D 放行状态（核心结论）

| Phase | fix6 时状态 | **fix7 审计后状态** | 说明 |
|---|---|---|---|
| **A 结构骨架** | ⚠️ 有条件放行 | **⚠️ 维持有条件放行（条件未完全满足）** | N25 **未根治**：幻影委托仍在发出（实证见下），只是不再错误记账。放行硬条件更新为：**修复买入成交回调的 available 台账（1~2 行，见 N25-2）** |
| **B 止盈体系** | ❌ 未达出口 | **❌ 维持原判 + 新增观察项 N28** | sell 事件丢失 action 字段，通道分布表（Phase B 出口硬材料）暂时无法产出，批次二施工前须补 |
| **C 组合层** | 未启动 | **未启动** | — |
| **D Optuna 寻优** | 阻塞 | **阻塞（正确）** | — |

**模拟盘（07-28 周一）放行意见：不建议按当前代码上线。** 幻影委托在模拟盘=真实券商拒单 + manual_position 盘中漂移（LIVE 模式 reconcile 30 分钟可愈合且成本已保真，但漂移窗口内卖出决策基于错误持仓）。修复量 1~2 行、不碰阈值，**周一开盘前修完即可放行**；若实在来不及，可降级为"已知风险接受 + 首日复盘重点核对拒单告警"。

---

## 二、逐项验收

| 项 | 判定 | 证据 |
|---|---|---|
| **N25 T+1 可用量检查** | **❌ 未根治** | 检查代码在位（main.py:107-110 `min(qty, pos_qty, _avail)`）✅；但**台账源头有洞**：买入成交回调 main.py:924 设 `available: new_qty`——当日买入（含底仓）立即变成"可卖"，检查形同虚设。**行为实证**：事件桥 `events_20260726.jsonl` 中 603667 于 05-08 09:36/10:14 的 SELL 委托各 5 条 = 今日 5 次运行（4×fix6 + **1×fix7**）全部发出 → fix7 幻影委托仍在发出 |
| **N26 sell 审计移成交时** | **✅（附副作用 N28）** | main.py:953-954 成交回调写入；fix7 backtrace sell 事件 34 笔与成交 CSV 34 笔**逐笔一致**（fix6 为 36 笔含 2 幻影）；603667 卖 5 笔=5 成交 ✅ |
| **N27 TAIL 入 backtrace** | **✅** | 经 N26 机制自然实现：000988 sell 事件 26 = 18 信号 + 8 TAIL，与成交 CSV 一致 ✅ |
| **F2 补全（成本保持 + 审计）** | **✅** | main.py:191-193 对账覆盖时保留我方 cost；main.py:194-195 `reconcile_fix` 审计事件（old_qty/new_qty）；模式条件化 fix6 已验 |
| KPI 回归 | ✅ 符合预期 | 与 fix6 **逐位一致**：+8.06% / MDD -8.27% / 66 笔（32买34卖）/ 费用 150.66 全>0——幻影单从未成交，行为无变化，正确 |

### N28（新发现，P1）：sell 事件丢失 action/score 字段
- fix7 sell 事件的 action 全部为空（backtrace 实证：34 笔均为 `?`）——成交回调拿不到信号通道信息。
- **后果**：通道分布表（PANIC/TRAIL/TREND_EXIT/TARGET/SELL_HIGH/TAIL 各几笔）无法产出，而这是 fix6 手册 WP-F9/F10 的 Phase B 出口硬材料。
- **修复建议**：下单时在 context 挂 `pending_action[symbol] = sig.action`，成交回调读取并写入 sell 事件；TAIL 写 "TAIL"。约 5 行，无阈值变化。

### N25-2（修复指引）：available 台账 T+1 语义
- main.py:924（买入成交）：`available` 应保持旧值（当日买入只加 qty/t_qty），即 `"available": old.get("available", 0)`；
- 每日 09:31 结转（main.py:595-598）`available = qty` 正确（隔夜全部解锁），无需改；
- 卖出成交回调 `available = new_qty`（main.py:946）可保留；
- 复验标准：重跑全窗口，事件桥中 05-08 09:36/10:14 的 603667 SELL 委托 **0 条**，且 05-13 TRAIL 卖出不受影响（成交 CSV 仍在）。

---

## 三、流程合规

- ⚠️ `d51b9ee` 再次打包 3 个修复项（N25/N26/N27）为一个 commit——连续第三轮出现打包提交，独立 commit 纪律仍未建立（影响：单点 revert 粒度丢失）。
- ✅ commit message 无统计数字失真；中间两个未申报 commit（回放存档 + 清临时文件）已核对无害。
- ✅ 铁律：本轮无 params.py 改动，未触碰任何买卖阈值。

---

## 四、结论

fix7 交付了 N26/N27/F2 三个**真实闭环**（审计一致性显著改善：backtrace 与成交 CSV 首次逐笔对齐），但核心项 N25 只修了"记账"没修"委托"——检查加了、台账没修，幻影委托仍在发出。**Phase A 维持有条件放行，条件不变：周一开盘前修 N25-2（1~2 行）+ 复验事件桥无幻影委托。** N28 不阻塞模拟盘，但必须在批次二（Phase B 出口）前修复。

---

## 五、诚实声明

1. 幻影委托的判定证据：事件桥 `E:\06_T\t_io\gm_bridge\events_20260726.jsonl` 中 5 组完全一致的 05-08 SELL 委托记录（对应今日 5 次运行）；代码证据：main.py:924 的 available 赋值。
2. 回测引擎对 T+1 委托为"异步拒单"（无同步异常、无成交、无 reject 事件入桥），故幻影委托的唯一可见证据是事件桥 order 记录；`rejected_order_count`（main.py:956/990）在回测终端日志中，未入数据包，本轮无法核验其数值。
3. 本人未修改任何 `.py/.json/.jsonl` 文件，未重跑任何回测；所有判定为只读分析结论。
