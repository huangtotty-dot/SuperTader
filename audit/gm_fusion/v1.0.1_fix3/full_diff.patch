diff --git a/config/params.py b/config/params.py
index 8723528..c27d713 100644
--- a/config/params.py
+++ b/config/params.py
@@ -227,6 +227,20 @@ STOCK_PARAMS = {
         "notify_sell_threshold": 63,
         "notify_buy_threshold": 43,
     },
+    # 以下标的沿用全局 PARAMS 默认值，标记为"未适配"
+    "600481": {
+        "_note": "双良节能 - 未适配，沿用全局默认参数",
+    },
+    "600176": {
+        "_note": "中国巨石 - 未适配，沿用全局默认参数",
+    },
+    "603667": {
+        "_note": "五洲新春 - 未适配，沿用全局默认参数",
+    },
+    "588170": {
+        "_note": "科创芯片ETF - 未适配，沿用全局默认参数",
+        "type": "etf",
+    },
 }
 
 # ==================== 大盘态势判定参数（index_regime.py） ====================
diff --git "a/docs/\345\256\241\346\240\270\346\212\245\345\221\212_v1.0.1_fix2.md" "b/docs/\345\256\241\346\240\270\346\212\245\345\221\212_v1.0.1_fix2.md"
new file mode 100644
index 0000000..bf4a9a0
--- /dev/null
+++ "b/docs/\345\256\241\346\240\270\346\212\245\345\221\212_v1.0.1_fix2.md"
@@ -0,0 +1,168 @@
+# 审核报告：v1.0.1_fix2（掘金融合版第二轮修复验收）
+
+- **审计对象**：Commit `65ac3c0118f9c21407626ae11282f0f3fc488c06` + 回测结果 `E:\goldenMinerReport\v1.0.1_fix2`
+- **回测区间**：2026-04-26 ~ 2026-07-24（59 个交易日），标的 000988.SZ，初始资金 200,000
+- **审计基准**：`审核报告_v1.0.1_fix1.md` 给出的 fix2 八项优先修复清单 + T2 八项硬指标门槛
+- **审计材料**：fix2 代码快照（main.py / signal/engine.py / signal/position_sizer.py / config/params.py）、full_diff.patch、回测 PDF、成交/委托/交易 CSV、backtrace.jsonl（14,160 行，单 run）
+- **审计人角色**：独立审计员（只读，不改代码、不改阈值、不重跑回测）
+- **审计日期**：2026-07-25
+
+---
+
+## 一、结论速览
+
+**⚠️ 重大进步，但 T2 八项门槛仅过 4 项 —— 判定：不可进入 Optuna 参数寻优，需 fix3。**
+
+fix2 做对了最重要的一件事：**卖强通道彻底打通**。strong_uptrend 禁卖移除后，5-6 月 17 笔卖出全部成交在 132→184 的递增价位上，100% 胜率（17/17），把 fix1 的"满仓死扛 -6.05%"扭转为 **+18.29%、夏普 1.18、回撤 21.2%**，大幅跑赢同期买入持有（≈ -5.5%）。这证明信号引擎的卖出侧是真实有效的，N1 修复方向正确。
+
+但三个新发现的实现级 bug 使系统仍不合格：
+
+1. **现金/仓位双保险形同虚设（N8）**：`account().cash` 属性被当方法调用，异常被静默吞掉，永远用假现金 100,000 计算 —— 导致 6 条柜台拒单、仓位一度冲到 1,700 股（≈99.5% 满仓），N2/N3 两项修复全部落空。
+2. **PANIC 止损在暴跌期"自解除"（N9）**：-5×日ATR 触发线随波动率爆炸从 -28.7% 放宽到 -58.7%，185→108 主跌段全程 0 天可达，18 个交易日 0 笔交易，底仓 800 股从浮盈 +13.9% 拿到浮亏 -33.3%。
+3. **仓位计算器实际死亡（N10）**：t_qty≡qty 导致 max_buyable 恒为 0，sizer 恒返回 0，全部买入走 fallback 固定 300 股 —— 仓位管理名存实亡。
+
+另有费用口径回退（N11，fix2 未设佣金/滑点，与 fix1 不可直接比较）和审计 run_id 未生效（N12）两处工程瑕疵。
+
+---
+
+## 二、KPI 四版对照
+
+| 指标 | v1.0.0（MACD模板） | v1.0.1（初版融合） | fix1 | **fix2** | 同期买入持有 |
+|---|---|---|---|---|---|
+| 累计收益 | -7.35% | +8.43%（仅5笔） | -6.05% | **+18.29%** | ≈ -5.5%（121.3→114.7） |
+| 夏普 | — | — | — | **1.18** | <0 |
+| 最大回撤 | — | — | 38.6% | **21.2%** | ≈ -41.6%（185→108 峰谷） |
+| 胜率 | — | — | 7.7% | **100%（17/17）** | — |
+| 成交笔数 | 多（负期望） | 5 | 35+ | **37（买15/平17，含底仓）** | — |
+| 交易天数占比 | — | 瘫痪 | 高但亏损 | **≈34%（20/59）** | — |
+| 柜台拒单 | — | — | 35 条 | **6 条** | — |
+
+> 口径警示：fix2 回测请求**未设 commission/slippage/adjust**（成交 CSV fee 全 0.0），fix1 为 0.00015。fix2 的 +18.29% 为**零费用口径**，约高估 0.5~1 个百分点；两版 KPI 不可直接相减对比（详见 N11）。
+
+**fix2 净值结构拆解（关键洞察）**：
+
+- 期末 nav 236,587，其中已实现利润主要来自 5-6 月卖强（17 笔，132→184 递增卖出）；
+- 期末仍持有 800 股底仓，成本 162.14，现价 ≈108，**浮亏 fpnl -43,163**；
+- 也就是说：卖强赚的钱，近一半被底仓深套吐了回去。系统"会卖强、不会止损"的不对称性仍未解决。
+
+---
+
+## 三、fix1 八项优先修复 · 逐项验收
+
+| # | 修复项 | 判定 | 证据 |
+|---|---|---|---|
+| 1 | N1：strong_uptrend 移除禁卖 | ✅ | engine.py 271-273、380-382 diff 确认 sell_block 移除；5-6 月 17 笔卖在 132→184 递增价位，"分够就卖"行为恢复 |
+| 2 | P0-2：record_trade_action 接入 on_order_status | ✅ | main.py on_order_status(status==3) 调用 engine.record_trade_action；连续分钟连发消失，买入间隔 26~30 分钟 > 25 分钟冷却线 |
+| 3 | N2：仓位上限 80% | ❌ | main.py:522-528 假现金 bug（见 N8）→ 分母失真；04-29 10:36 真实仓位 82%>80% 仍成交，04-30 达 1,700 股 ≈99.5% 满仓。params.py 唯一改动 max_single_position_pct 0.30→0.80 本身已配置，但被 bug 架空 |
+| 4 | N3：买入现金预检 | ❌ | 同上根因。预检用假现金 100,000 放行，柜台以真实现金拒单：04-30~05-07 共 6 条"资金不足 可用=900.999"WARN（fix1 为 35 条，数量大降但根因未除） |
+| 5 | N4：PANIC 改日线 ATR | ⚠️ 部分修复 | 量纲修复属实（engine.py:345-349 改用 daily_ctx.daily_atr，main.py 注入 TR14/close）；但 -5×日ATR 无封顶，暴跌期自解除（见 N9），主跌段 0 次触发 |
+| 6 | N5：底仓拒单重试 ×3 | ✅ | MAX_BASE_RETRY=3 实现；04-27 底仓 800 股一次成交 |
+| 7 | N6：09:30-09:35 买入隔离 | ✅ | 全窗口最早买入 09:36，开盘 5 分钟隔离生效 |
+| 8 | D8：no_signal 审计字段 | ⚠️ 基本到位 | no_signal 记录带 buy_blocks/sell_blocks/decision_reason，7 月静默可解释（每日 max_sell 46~62.5，从未过卖出阈值 63）；但 `_AUDIT_RUN_ID` 赋值缺 global 声明，14,160 行 run_id 全为空串（见 N12） |
+
+**八项验收：4✅ / 2⚠️ / 2❌。两处 ❌ 同源于一个一行级 bug（N8）。**
+
+---
+
+## 四、新发现问题（N8 ~ N12）
+
+### N8 ❌【P0】`account().cash` 属性当方法调用 → 现金预检与仓位上限双双失效
+
+- **预期**：读取账户真实可用现金，超上限/现金不足时拦截买入。
+- **实际**：main.py:526 `available_cash = float(_acct.cash())` —— gm3 的 `account().cash` 是**属性（dict）而非方法**，调用即抛 TypeError，被 527-528 行 `except Exception: pass` 静默吞掉，`available_cash` 永远保持默认值 100,000。
+- **行为证据链**：
+  1. 假现金 100,000 使 N3 预检分母失真 → 04-30~05-07 六条柜台拒单"资金不足 可用=900.999"（预检放行、柜台拒绝）；
+  2. N2 仓位上限用 `available_cash + current_pos_value` 做总权益分母（main.py:539）→ 04-29 10:36 真实仓位 82% 仍成交，04-30 达 1,700 股 ≈99.5% 满仓。
+- **根因方向**：API 形态误判 + 静默 except 掩盖故障。静默 except 是二次伤害——若吞错时打印一次告警，此 bug 在首次回测就会暴露。
+- **建议 Pro 检查**：`main.py:522-542`。修法参考（不改任何阈值）：
+  ```python
+  c = getattr(_acct, 'cash', None)
+  c = c() if callable(c) else c
+  available_cash = float(c.get('available', 0) if isinstance(c, dict) else c)
+  ```
+  并把 `except Exception: pass` 改为至少一次性 WARN 输出。
+
+### N9 ❌【P0】PANIC 止损"自解除"：-5×日ATR 触发线随暴跌放宽，全程不可达
+
+- **预期**：深度浮亏时 PANIC 强平，185→108 主跌段至少触发 1 次。
+- **实际**：7 月 18 个交易日 0 笔交易；backtrace.jsonl 显示每日 sell_blocks 全空、reason 全"无信号"。
+- **数据验证**（审计员用独立日线数据复算：archive 日线 CSV 至 07-06 + 分钟快照重建 07-07 起，TR14/close）：
+
+  | 日期 | 收盘 | 日ATR% | 触发线(-5×ATR) | 相对成本162.14浮亏 | 应触发? |
+  |---|---|---|---|---|---|
+  | 06-24 | 167.89 | 5.7% | **-28.7%** | +3.5% | 否 |
+  | 07-06 | 149.70 | 8.1% | **-40.3%** | -7.7% | 否 |
+  | 07-16 | 130.69 | 9.8% | **-49.0%** | -19.4% | 否 |
+  | 07-20 | 105.86 | 11.7% | **-58.7%** | -34.7% | 否 |
+  | 07-24 | 108.19 | 10.3% | **-51.6%** | -33.3% | 否 |
+
+  23 天逐日对照：**hit 天数 = 0**（按收盘与按盘中最低价均为 0）。ATR 从 5.7% 爆炸到 11.7%，触发线同步放宽，浮亏永远追不上触发线——**越暴跌越不止损**。
+- **根因方向**：engine.py:349 `is_deep_loss = profit_pct < -5 * daily_atr`，ATR 系数无上限。波动率自适应止损在单边暴跌中必然自解除，这是设计缺陷而非参数问题。
+- **建议 Pro 检查**：`signal/engine.py:345-349`、`main.py` daily_atr 计算段。修复方向（属设计修正，不涉及买卖阈值数值调优）：触发线改为 `max(固定下限如 -12%, -5×ATR)`，或对 ATR 封顶（如 `min(daily_atr, 0.03)`），保证止损线始终可达。
+- **诚实标注**：复算基于 tushare 日线+分钟快照重建，与 gm3 回测内部 daily history 可能有微小差异，但 ATR 量级（5.7%→11.7%）与结论稳健；即使成本口径不同，-40%~-59% 的触发线在实践中也不可达。
+
+### N10 ❌【P1】position_sizer 实际死亡：t_qty≡qty → max_buyable 恒 0 → 全部买入走 fallback 300 股
+
+- **预期**：sizer 按信号强度与仓位结构计算加仓位。
+- **实际**：position_sizer.py:56 `total_t = t_qty or qty`，:70-71 `hold_qty = qty; max_buyable = total_t - hold_qty` —— 融合版持仓模型中 t_qty 与 qty 始终同步相等，max_buyable 恒为 0，:73 恒返回 0。main.py:519-520 兜底 `qty = 300`。
+- **行为证据**：成交 CSV 全部买入均为 300 股整（04-29、04-30、05-18、06-01…06-25），无一笔由 sizer 算出。
+- **根因方向**：T 仓语义未在持仓模型中区分——t_qty（目标 T 仓）与 qty（当前持仓）在融合层被同步更新，sizer 的"剩余可买额度"概念落空。
+- **建议 Pro 检查**：`signal/position_sizer.py:55-77` 与 main.py 持仓同步段。需要在持仓模型里真正区分 base/t_qty/qty 三层语义，或在融合层为 sizer 提供独立的"目标仓位"输入。
+
+### N11 ❌【P1】D9 费用口径回退：fix2 回测未设佣金/滑点/复权
+
+- **预期**：延续 fix1 口径（commission=0.00015 等），保证版本间可比。
+- **实际**：fix2 回测请求缺 commission/slippage/adjust 参数，成交 CSV fee 全 0.0。fix2 +18.29% 为零费用口径，约高估 0.5~1 个百分点；与 fix1（-6.05%，含费）不可直接对比。
+- **建议 Pro 检查**：回测发起脚本/参数。fix3 重跑时必须固定统一口径，并在报告头部声明。
+
+### N12 ⚠️【P2】审计 run_id 未生效
+
+- **实际**：`_AUDIT_RUN_ID` 在函数内赋值未声明 `global`，backtrace.jsonl 14,160 行 `_run_id` 全为空串。多 run 混排时无法区分来源（本次为单 run，暂未造成混淆）。
+- **建议 Pro 检查**：main.py 中 `_AUDIT_RUN_ID` 赋值处，补 `global _AUDIT_RUN_ID`。
+
+---
+
+## 五、T2 八项硬指标门槛对照
+
+| # | 硬指标 | 目标 | fix2 实际 | 判定 |
+|---|---|---|---|---|
+| 1 | 柜台拒单 | = 0 | 6 条（N8 根因） | ❌ |
+| 2 | 交易天数占比 | ≥ 60% | ≈34%（20/59），7 月 18 日全静默 | ❌ |
+| 3 | uni_down 日买入熔断 | 0 笔 | 0 笔（买入日与 fix1 uni_down 清单无交集） | ✅ |
+| 4 | 卖出地板保护 | 不破 400 股 | 期末 800 = base_ref，未破地板；14:50 尾盘归位生效 | ✅ |
+| 5 | 主跌段止损触发 | ≥ 1 次 | 0 次（185→108，N9 自解除） | ❌ |
+| 6 | 下跌段 MDD ≤ B&H×60% | ≤ ≈25% | 21.2%（6 月卖强已回收 75% 现金，被动达标） | ✅ |
+| 7 | 费用口径一致 | 与 fix1 同 | 零费用口径，D9 回退 | ❌ |
+| 8 | 审计可解释性 | 每笔可解释 | 7 月静默可解释（分数未过线+panic未触发）；run_id 空为小瑕疵 | ⚠️→✅ |
+
+**通过 4/8（另 1 项 ⚠️ 勉强计入则为 5/8），未达"全绿进入下一阶段"标准。**
+
+---
+
+## 六、总体结论与 fix3 优先序
+
+### 结论
+
+**不可进入 Optuna 参数寻优。** fix2 证明了信号引擎卖出侧的真实有效性（这是本轮最重要的正面结论），但 N8/N9/N10 三个实现级缺陷分别架空了现金风控、止损与仓位管理三大安全网。在假现金、无止损、死 sizer 的系统上做参数寻优，只会把 bug 当成 alpha 拟合进去。
+
+### fix3 优先序（均为一行级~小范围修复，不涉及买卖阈值数值调整）
+
+1. **N8（P0）**：main.py:522-542 修正 cash 属性读取 + except 去静默化。预期效果：拒单归零、仓位上限真实生效（fix2 的 1,700 股超仓将不再出现，KPI 会因此变化，属正常）。
+2. **N9（P0）**：engine.py:349 止损触发线加下限/ATR 封顶（设计修正）。预期效果：07-16 前后（浮亏 -19.4%、触发线若有 -12% 下限）应出现 PANIC 卖出，fpnl -43,163 的底仓深套可截断约一半。
+3. **N10（P1）**：理清 t_qty/qty 语义，让 sizer 复活或显式声明"固定 300 股"为当前设计。二选一，不要继续让死代码伪装成活风控。
+4. **N11（P1）**：fix3 回测统一费用口径（沿用 fix1 的 commission/slippage/adjust），报告头部声明口径。
+5. **N12（P2）**：补 global 声明，顺手在 no_signal 记录里加上 profit_pct / daily_atr / is_deep_loss 三个字段——本次 N9 的复算验证本可由审计记录直接给出，不必外部重建日线。
+
+### fix3 验收标准（预告）
+
+- T2 八项全绿（重点看 #1 拒单=0、#5 主跌段止损≥1 次、#7 口径一致）；
+- 接受 KPI 回撤：修复 N8/N9 后收益大概率低于 +18.29% 的虚高值，**这是健康的方向，不是退步**——fix1→fix2 教会我们"能赚",fix3 要证明"亏不了大的"。
+
+---
+
+## 附：审计过程诚实声明
+
+1. N8 的 `account().cash` 属性判定基于行为证据链（假 100k 同时完美解释超仓与 6 条拒单），未查阅 gm3 SDK 官方文档；置信度高，但若 SDK 实际返回可调用对象，根因需改查 cash dict 取值键名。
+2. N9 的 ATR 复算数据源为本地 archive 日线 CSV（至 07-06）+ 分钟快照重建（07-07 起），与回测内部行情可能有微小差异；触发线 -28.7%→-58.7% 的量级与"0 天可达"结论对此类误差不敏感。
+3. fix2 PDF 报告为图片型单页（无文本层），全部数值以成交/委托 CSV 与 backtrace.jsonl 为准。
+4. 本审计全程只读，未修改任何 .py/.json/.jsonl 文件，未重跑回测。
diff --git "a/docs/\346\250\241\346\213\237\347\233\230\345\256\236\346\226\275\346\226\271\346\241\210_20260727.md" "b/docs/\346\250\241\346\213\237\347\233\230\345\256\236\346\226\275\346\226\271\346\241\210_20260727.md"
new file mode 100644
index 0000000..df76d9e
--- /dev/null
+++ "b/docs/\346\250\241\346\213\237\347\233\230\345\256\236\346\226\275\346\226\271\346\241\210_20260727.md"
@@ -0,0 +1,268 @@
+# 掘金模拟盘测试实施方案（融合版 · 2026-07-27 上线）
+
+- **制定人角色**：专家/审计员（只出方案，不编码、不测试）
+- **制定日期**：2026-07-25（周六）
+- **上线日期**：2026-07-27（周一）
+- **关联文档**：《审核报告_v1.0.1_fix2.md》（N8/N9/N10 三项未决缺陷是本方案护栏设计的直接依据）
+
+---
+
+## 第一部分：对三个设想的专家评估
+
+### 设想 1：模拟盘镜像被套持仓，辅助实盘回本 —— ⚠️ 方向靠谱，但定位必须修正
+
+**一个根本性逻辑问题必须先说清楚：模拟盘的做 T 盈利不会进入你的实盘账户。** 模拟账户 +18%，实盘的套牢成本一分钱都不会降。回本这件事，只有真实账户里的 T 差价能做得到。
+
+所以这个设想的正确定位不是"用模拟盘回本"，而是：
+
+> **模拟盘 = 信号源 + 验证场。模拟盘跑出买卖信号 → 你在实盘跟随执行 → T 的差价真正发生在实盘，成本才真的下降。**
+
+在这个修正后的定位下，设想完全成立，且有三个额外好处：
+
+1. **零风险验证个股适应性**：当前策略只在 000988 单票上验证过。你被套的其他股票波动结构、流动性、股性都不同，直接在实盘跑等于拿真钱做 A/B 测试；模拟盘先把"这套信号在这只票上灵不灵"测出来。
+2. **信号可信度有参照系**：模拟盘信号与你的实盘持仓完全同构（同票同股数），信号的出现时点、价位、仓位变化可以直接对照实盘决策，不存在"回测很美但跟实盘对不上"的断层。
+3. **心理缓冲**：有模拟盘先跑一遍，实盘跟随时是"第二次看到这只票的这个信号"，决策质量会高得多。
+
+**三个必须面对的风险（都有审计依据）：**
+
+| 风险 | 依据 | 对策（详见 Phase 0） |
+|---|---|---|
+| N9 止损自解除未修复：暴跌时 PANIC 不可达，模拟盘和跟随的实盘都会深套 | fix2 审核：-5×日ATR 触发线暴跌期放宽至 -58.7%，23 天 0 触发 | 人工止损纪律 + 监听层旁路告警，实盘跟随单必须自带止损价 |
+| 个股适应性未验证：000988 的阈值参数直接套到别的票上，可能完全失效 | 全部回测/审计仅覆盖 000988 | 逐票评估做 T 门槛，新票先观察 3~5 天再允许跟随 |
+| 模拟撮合偏乐观：无真实排队、冲击成本，成交价比实盘好 | 模拟盘固有属性 | 所有 KPI 标注"模拟口径"，滑点分析纳入日复盘 |
+
+### 设想 2：自动交易 + 飞书通知 —— ✅ 完全靠谱，资产现成
+
+E:\06_T 已有成熟的飞书推送基建（`config.py:103 send_feishu_payload`、webhook 解析、启动自检、加急告警机制），t_trader 的告警体系久经实盘考验。唯一需要工程决策的是：**通知逻辑不要写进 gm3 策略进程内**，理由：
+
+1. gm3 沙箱的网络权限不确定，策略内 `requests.post` 可能直接被拦；
+2. 策略进程崩溃 = 通知能力同时丢失，恰恰是出事时你最需要通知；
+3. 通知代码污染交易策略，违背模块化重构的成果。
+
+**推荐文件桥架构**（详见第二部分）：策略只往本地写事件文件，独立监听进程读文件推飞书。单向、可重放、可审计。
+
+### 设想 3：每日模拟盘质量复盘 —— ✅ 完全靠谱，基建大部分现成
+
+引擎本来就写 backtrace.jsonl（每笔决策可追溯），`t_trader-daily-feedback-loop` 复盘流程、`t_io/reviews/` 目录体系、分钟快照机制都在。缺的是三块：gm3 模拟盘数据采集器、模拟盘专属质量 KPI 层、以及"模拟 vs 回测"一致性对比。第四部分给出完整设计。
+
+### 总评
+
+**三个设想组成闭环：模拟盘验证（设想1）→ 信号送达（设想2）→ 质量复盘反哺优化（设想3）。方向正确，值得做；前提是接受"模拟盘是信号源而非回本工具"的定位修正，并把 N9 未修的风险用人工护栏兜住。**
+
+---
+
+## 第二部分：总体架构（三层解耦）
+
+```
+┌─────────────────────────────────────────────────────────────┐
+│ L1 执行层：gm3 模拟盘（融合版策略）                            │
+│   职责：跑信号 → 下单 → 把事件写入文件桥                       │
+│   唯一新增职责：on_signal/on_order_status/on_execution_report │
+│   里追加写 E:\06_T\t_io\gm_bridge\events_YYYYMMDD.jsonl      │
+│   （只写文件，不碰网络，不碰飞书）                             │
+└──────────────────────┬──────────────────────────────────────┘
+                       │ 文件桥（单向、可重放、可审计）
+                       ▼
+┌─────────────────────────────────────────────────────────────┐
+│ L2 桥接层：E:\06_T\t_io\gm_bridge\                           │
+│   events_YYYYMMDD.jsonl   当日事件流（schema 见附录 A）        │
+│   heartbeat.json          心跳（策略每分钟刷新时间戳）          │
+│   KILL_SWITCH             急停文件（存在即禁止新开仓）          │
+│   follow_log.jsonl        实盘跟随记录（人工登记）              │
+└──────────────────────┬──────────────────────────────────────┘
+                       │
+                       ▼
+┌─────────────────────────────────────────────────────────────┐
+│ L3 通知与复盘层（E:\06_T 进程，复用现有基建）                  │
+│   gm_watcher.py   监听事件桥 → config.send_feishu_payload    │
+│                   去重/限流/聚合/心跳监控/旁路风控告警          │
+│   每日复盘任务    15:30 收盘后采集+分析+飞书简报+本地存档       │
+│   周度深度审计    每周六，K3（审计员）主持                     │
+└─────────────────────────────────────────────────────────────┘
+```
+
+**设计原则**：策略进程只增加"写文件"这一个无副作用动作；通知、复盘、风控旁路全部在外部进程，任何一环挂掉不影响交易主链路，且事件文件可重放补推。
+
+---
+
+## 第三部分：实施计划（按时间线）
+
+### Phase 0：周末准备（7/25 周六 ~ 7/26 周日）
+
+#### 0.1 版本确认与风险定级【最先做，决定后续所有事】
+
+- 确认周一上线的 commit。若 fix3（修 N8 假现金 / N9 止损自解除 / N10 sizer）**尚未完成并通过验收**，则上线版本为带已知缺陷的 fix2，必须执行以下强制护栏：
+  - 人工止损纪律写进每日操作清单（弥补 N9）：任何镜像持仓浮亏超过 -12% 即触发人工评估，不依赖策略 PANIC；
+  - 监听层旁路监控仓位与现金（弥补 N8）：watcher 独立跟踪仓位市值占比，超 80% 即飞书加急告警；
+  - 单票买入固定 300 股视为当前既定行为（接受 N10 现状），仓位预期管理按此做。
+- 明确铁律延伸：**模拟盘测试期间不改买卖阈值**；所有参数变更走"提出→审计→commit→重跑"流程。
+
+#### 0.2 标的与参数清单
+
+建立《模拟盘标的清单表》，逐票填写：
+
+| 字段 | 说明 |
+|---|---|
+| 代码/名称 | 被套个股 |
+| 实盘成本/股数 | 仅作参照，不进模拟盘逻辑 |
+| 模拟镜像股数 | 建议与实盘一致（保持信号同构）；底仓 base_ref 按此设定 |
+| 做 T 门槛评估 | 日均振幅 >3%、日成交额 >2 亿，二者不满足的票标记"仅观察不跟随" |
+| 参数来源 | 000988 直接继承现有参数；新票第一版沿用同套参数，标注"未适配" |
+| 上线批次 | 周一仅 1 只（建议 000988 或套得最深且满足门槛者），周二起每 1~2 天加一只 |
+
+#### 0.3 模拟账户设置
+
+- 资金规模：按镜像持仓总市值 ×1.3 配置（留足做 T 现金水位）；
+- **佣金/滑点口径必须与回测统一**（沿用 fix1 的 0.00015 口径，不要重蹈 N11 费用口径回退的覆辙），并在账户设置截图存档；
+- 建仓方案与 T+1 约束（**关键认知**）：周一买入的底仓当天不可卖，**周一实际只能"建仓 + 观察信号"，真正的做 T 从周二开始**。周一的成功定义见 Phase 1，不要因为周一没有 T 交易而误判系统故障；
+- 底仓建仓方式：开盘后按 base_order 逻辑一次性买入镜像股数，价格以实盘成交价为准记录偏差。
+
+#### 0.4 文件桥 + 飞书联通测试
+
+- 定义事件 schema（附录 A），策略侧在 4 个回调点追加写文件（信号、委托、成交、拒单）+ 每分钟心跳；
+- 编写 `gm_watcher.py` 监听进程：tail 事件文件 → 复用 `config.py:103 send_feishu_payload` 推送，带三个机制：
+  - **去重限流**：同一事件 ID 不重复推；盘中信号类通知按票聚合，避免刷屏；
+  - **心跳监控**：10:00 前未收到任何心跳/事件 → 推"策略可能未启动"加急告警；盘中心跳中断超 10 分钟 → 加急告警；
+  - **旁路风控**：独立累计仓位与现金，超上限即告警（不依赖策略自身的 N8 逻辑）；
+- 联通测试 checklist：周日用回放事件文件的方式测试 watcher（构造假事件 → 验证飞书收到 → 验证去重 → 验证告警格式），不动策略本身。
+
+#### 0.5 风控护栏与应急方案
+
+- `KILL_SWITCH` 文件机制：在桥目录放置该文件，策略每分钟检查，存在即停止新开仓（平仓不禁）。用途：发现异常时无需登录 gm3 即可急停；
+- 单日最大亏损线：账户日内浮亏超 3% → watcher 加急告警；超 5% → 建议人工放置 KILL_SWITCH；
+- 终端/进程崩溃预案：gm3 终端崩溃后策略状态丢失，重启策略后底仓计数器如何恢复——**这是 fix2 审计中 P0-1 类问题的高发区，周一前必须人工演练一遍"崩溃→重启→核对仓位"流程**。
+
+### Phase 1：周一（7/27）上线日 —— 定义"最小成功"
+
+**周一的目标不是赚钱，是验证链路。** 成功标准（checklist）：
+
+| # | 检查项 | 通过标准 |
+|---|---|---|
+| 1 | 策略启动 | 09:25 前策略运行，飞书收到启动自检 |
+| 2 | 底仓建仓 | 镜像股数买入成交，飞书收到成交通知，价格偏差记录 |
+| 3 | 事件桥 | events jsonl 全天持续写入，字段完整 |
+| 4 | 心跳 | 全天无"心跳中断"告警（或告警后能定位原因） |
+| 5 | 信号观察 | 即使不交易，no_signal/信号记录完整，收盘能回答"今天为什么没动作" |
+| 6 | 复盘产出 | 15:30 后收到第一份模拟盘复盘简报 |
+
+周一**不做实盘跟随**——只看信号、熟悉节奏、验证通知链路。若 6 项全绿，周二进入稳态。
+
+### Phase 2：周二 ~ 周五（7/28 ~ 7/31）稳态运行
+
+**每日节奏表：**
+
+| 时间 | 事项 |
+|---|---|
+| 08:15 | （现有）财经新闻 TOP30 推送 |
+| 09:15 | watcher 自检 + 策略心跳确认 |
+| 09:30-15:00 | 模拟盘自动交易；信号/委托/成交/拒单飞书实时通知；旁路风控在线 |
+| 盘中 | **实盘跟随窗口**：信号到达 → 人工确认 checklist → 实盘下单 → 登记 follow_log |
+| 15:30 | 每日复盘任务：采集 → 分析 → 飞书简报 + 本地完整报告存档 |
+| 收盘后 | （可选）对照 t_trader 当日实盘信号，做双系统交叉验证 |
+
+**实盘跟随规则（前两周 100% 人工确认，这是红线）：**
+
+1. 只跟随"仅观察"标记以外的票；
+2. 跟随前过 checklist：信号分数、当前仓位、现金、止损价（人工设定，弥补 N9）、跟随量（建议为模拟信号量的 50%~100%）；
+3. 跟随价格与模拟成交价的偏差记入 follow_log，作为滑点分析素材；
+4. 模拟盘发出卖出信号而你实盘不想卖（或反之）→ 同样登记，标注原因——**分歧记录比一致记录更有复盘价值**；
+5. 任何情况下模拟盘信号不得自动直连实盘下单。
+
+**加票节奏**：周二加第 2 只，周四加第 3 只，每只新票前 3 天标记"仅观察"。
+
+### Phase 3：周度深度分析（每周六，首次 8/1）
+
+- **模拟 vs 回测一致性**：同标的同窗口，信号时点/价位/仓位轨迹对比，量化"行为漂移"；
+- **滑点与撮合偏差**：模拟成交价 vs 信号发生时价 vs 实盘跟随成交价，三方对比；
+- **信号质量分层**：按信号类型（SELL_HIGH / 低吸 / PANIC）统计胜率、平均差价、持有期；
+- **优化候选清单**：只列"值得改什么"，全部走审计流程，不直接动阈值；
+- **K3 周度审计**：按既有审计员规则出周度报告，判定"是否允许扩大跟随规模/放宽人工确认"。
+
+---
+
+## 第四部分：每日复盘报告设计（质量 KPI 体系）
+
+**数据采集三源合并**：gm3 模拟盘成交/委托导出 + 事件桥 jsonl + backtrace.jsonl + 分钟快照。
+
+**五类 18 项质量 KPI：**
+
+| 类别 | KPI | 健康线 |
+|---|---|---|
+| A 执行质量 | A1 柜台拒单数 | =0（N8 修复前允许>0，但每条必须归因） |
+| | A2 信号→委托延迟 | <2 个 bar |
+| | A3 委托→成交滑点 | 统计分布，关注系统性偏移 |
+| | A4 尾盘归位执行率 | 100% |
+| B 信号质量 | B1 买/卖分数分布 | 与回测分布对比，漂移>20% 标记 |
+| | B2 no_signal 占比及原因分类 | 长期 >90% 需检查是否瘫痪（v1.0.1 教训） |
+| | B3 信号→成交转化率 | 突降即查拒单/冷却 |
+| | B4 卖强价位递增性 | 上升趋势中卖出价应递增（fix2 的正面特征） |
+| C 风控有效性 | C1 仓位峰值/曲线 | 峰值 ≤80%（N8 修复前由旁路监控兜底） |
+| | C2 现金水位 | 不做满仓 |
+| | C3 止损/PANIC 触发记录 | 大跌日**应触发而未触发 = 红色警报**（N9 直接观测点） |
+| | C4 uni_down 熔断命中 | 熔断日 0 买入 |
+| D 盈亏归因 | D1 已实现 vs 浮动 | 拆分"卖强赚的钱"与"底仓套的亏"（fix2 的核心矛盾） |
+| | D2 按票归因 | 哪只票的信号在赚钱 |
+| | D3 按信号类型归因 | 哪类信号在赚钱 |
+| | D4 做T差价 vs 底仓波动贡献 | 做T是否真在降低成本 |
+| E 一致性 | E1 模拟 vs 回测同日信号重合度 | 趋势性下降即查数据/环境漂移 |
+| | E2 模拟 vs 实盘跟随偏差 | 仅在有跟随时计算 |
+
+**产出三件套**：
+
+1. **飞书简报**（一屏）：当日成交、盈亏、风控状态、1~3 条最值得注意的异常；
+2. **本地完整报告**：`E:\06_T\t_io\reviews\gm_sim\YYYYMMDD\`（KPI 明细 + 原始数据 + 异常证据）；
+3. **周度汇总**：趋势图 + 优化候选清单。
+
+---
+
+## 第五部分：风险清单与红线
+
+| # | 风险 | 等级 | 缓解 |
+|---|---|---|---|
+| 1 | N9 止损自解除导致镜像持仓深套 | 高 | 人工止损纪律（-12% 触发评估）+ watcher 旁路告警 |
+| 2 | 新票参数未适配，信号大面积失效 | 中 | 逐票门槛评估 + 新票 3 天仅观察期 |
+| 3 | 模拟撮合乐观偏差误导实盘跟随 | 中 | 滑点三方对比纳入日复盘；跟随量打折 |
+| 4 | gm3 终端崩溃/状态丢失 | 中 | 周一前演练崩溃恢复流程；心跳监控 |
+| 5 | 飞书 webhook 失效 | 低 | 启动自检；watcher 推送失败落盘可补推 |
+| 6 | 事件桥写盘失败未察觉 | 低 | 心跳与事件分离；复盘任务校验文件完整性 |
+
+**五条红线（不可逾越）：**
+
+1. 模拟盘信号不得自动直连实盘，跟随必须人工确认（至少前两周）；
+2. 不改买卖阈值——铁律从回测延伸到模拟盘，参数变更一律走审计流程；
+3. 实盘跟随单必须自带人工止损价，不依赖策略 PANIC（N9 修复并验收前）；
+4. 任何代码改动必须 git commit，周日晚统一 review；
+5. KILL_SWITCH 一经放置，当天不再恢复开仓，复盘后再定。
+
+---
+
+## 附录 A：事件桥 schema（events_YYYYMMDD.jsonl，每行一个 JSON）
+
+```json
+{"event": "signal",  "time": "...", "code": "000988", "action": "SELL_HIGH", "score": 74.6, "reasons": [...], "pos_qty": 1300}
+{"event": "order",   "time": "...", "code": "000988", "side": "SELL", "qty": 400, "price": 132.53, "order_id": "..."}
+{"event": "fill",    "time": "...", "code": "000988", "side": "SELL", "qty": 400, "price": 132.53, "order_id": "...", "pos_after": 900}
+{"event": "reject",  "time": "...", "code": "000988", "side": "BUY",  "qty": 300, "reason": "资金不足", "raw": "..."}
+{"event": "risk",    "time": "...", "kind": "position_limit|panic|kill_switch", "detail": "..."}
+{"event": "heartbeat", "time": "...", "bar": "10:36", "pos": {...}, "cash": ...}
+```
+
+## 附录 B：目录结构
+
+```
+E:\06_T\t_io\
+├── gm_bridge\            # 文件桥（events/heartbeat/KILL_SWITCH/follow_log）
+├── reviews\gm_sim\YYYYMMDD\   # 每日复盘完整报告
+└── minute_snapshots\     # （现有）分钟快照，复盘数据源
+```
+
+## 附录 C：周一前最终 checklist（按执行顺序）
+
+1. ☐ 确认上线 commit 与已知缺陷清单，护栏对应到位
+2. ☐ 《模拟盘标的清单表》填写完毕，周一首批 1 只票确定
+3. ☐ 模拟账户资金/费用口径设置并截图存档
+4. ☐ 策略侧事件桥写入 + 心跳就位
+5. ☐ gm_watcher 联通测试通过（回放假事件验证）
+6. ☐ KILL_SWITCH 机制验证一次（放置→策略停止开仓→移除）
+7. ☐ 崩溃恢复流程演练一次
+8. ☐ 飞书启动自检收到
diff --git "a/docs/\346\250\241\346\213\237\347\233\230\346\223\215\344\275\234\346\211\213\345\206\214_\345\221\250\344\270\200\344\270\212\347\272\277.md" "b/docs/\346\250\241\346\213\237\347\233\230\346\223\215\344\275\234\346\211\213\345\206\214_\345\221\250\344\270\200\344\270\212\347\272\277.md"
new file mode 100644
index 0000000..5934530
--- /dev/null
+++ "b/docs/\346\250\241\346\213\237\347\233\230\346\223\215\344\275\234\346\211\213\345\206\214_\345\221\250\344\270\200\344\270\212\347\272\277.md"
@@ -0,0 +1,254 @@
+# 掘金模拟盘操作手册 — 周一上线
+
+> 版本: v1.0.1-fix3 (commit `96054b6`)
+> 上线日期: 2026-07-27 (周一)
+> 标的: 华工科技 000988
+
+---
+
+## 一、上线前检查（周末完成）
+
+### 1.1 代码拉取
+
+```bash
+cd C:\Users\Lenovo\.goldminer3\projects\e8bb1f4d-87ce-11f1-97f7-98fa9b8df5e7
+git pull
+git log --oneline -3   # 确认最新 commit 是 96054b6
+```
+
+### 1.2 确认运行模式
+
+打开 `main.py`，底部应为 `mode=MODE_LIVE`：
+
+```python
+# ---- 模拟盘模式 ----
+run(strategy_id="e8bb1f4d-87ce-11f1-97f7-98fa9b8df5e7",
+    filename="main.py",
+    mode=MODE_LIVE,
+    token="480a6c84b0f43417ffcc9c15162dd7256ca9c3b0")
+```
+
+> 如需切回回测，注释掉 MODE_LIVE 块、取消注释 MODE_BACKTEST 块即可。
+
+### 1.3 模拟账户设置（掘金终端内操作）
+
+| 设置项 | 建议值 | 说明 |
+|--------|--------|------|
+| 账户类型 | 模拟账户 | 新建一个专用模拟账户 |
+| 初始资金 | 被套持仓市值 × 1.3 | 留足做 T 现金水位。如 800 股华工 ≈ 8 万，配 25 万 |
+| 佣金 | 0.015% | 与回测一致 |
+| 滑点 | 0.01% | 与回测一致 |
+
+**设置完成后截图存档。**
+
+---
+
+## 二、周一 9:15 前操作
+
+### 2.1 启动策略
+
+1. 打开掘金终端
+2. 加载 `main.py`
+3. 确认顶部策略 ID 为 `e8bb1f4d-87ce-11f1-97f7-98fa9b8df5e7`
+4. 点击「运行」
+
+**验证启动成功：**
+
+终端日志应依次出现：
+
+```
+[init] 大盘日线已缓存: 900 行, 2022-11-08 ~ 2026-07-24
+[init] 事件桥: E:\06_T\t_io\gm_bridge
+[init] 策略初始化完成: 1 只标的
+```
+
+### 2.2 启动事件监听器
+
+**另开一个终端窗口**（不要关策略进程），执行：
+
+```bash
+cd C:\Users\Lenovo\.goldminer3\projects\e8bb1f4d-87ce-11f1-97f7-98fa9b8df5e7
+python gm_bridge/watcher.py
+```
+
+**验证：** 应看到 `[watcher] 监听 ...` 和 `[watcher] 飞书: 可用`。
+
+如果飞书 Webhook 配置正确，会收到一条启动通知。
+
+### 2.3 启动自检清单
+
+| # | 检查项 | 状态 |
+|---|--------|------|
+| 1 | 策略进程运行中，无报错 | ☐ |
+| 2 | watcher 进程运行中，飞书可用 | ☐ |
+| 3 | `E:\06_T\t_io\gm_bridge\` 目录存在 | ☐ |
+| 4 | `heartbeat.json` 在每分钟刷新 | ☐ |
+| 5 | 飞书收到启动通知（如果 webhook 已配） | ☐ |
+
+---
+
+## 三、周一盘中（9:30 — 15:00）
+
+### 3.1 策略行为预期
+
+| 时间 | 事件 | 说明 |
+|------|------|------|
+| 09:31 | **建底仓** | 市价买入 ≈800 股华工科技（资金×50%/开盘价），飞书收到 `🔵 成交 BUY` |
+| 09:31-09:35 | **买入隔离** | 开盘 5 分钟不产生买入信号（防开盘量能失真） |
+| 09:35-14:50 | **正常交易** | 信号引擎评估每分钟 bar，触发 BUY_LOW / SELL_HIGH |
+| 14:50-15:00 | **尾盘归位** | 持仓超底仓 → 强制卖出归位 |
+
+### 3.2 飞书通知类型
+
+| 通知 | 含义 | 示例 |
+|------|------|------|
+| 🟢 低吸信号 | engine 产生 BUY_LOW 信号 | `华工科技(000988) 评分:55分 持仓:800股` |
+| 🔴 高抛信号 | engine 产生 SELL_HIGH 信号 | `华工科技(000988) 评分:72分` |
+| 🔵 成交 | 买卖单全部成交 | `买入 300股 @ 105.50 成交后持仓:1100股` |
+| ❌ 委托被拒 | 柜台拒单（资金不足等） | `买入 300股 原因: 资金不足` |
+| 🛑 KILL_SWITCH | 急停文件触发 | 放置 KILL_SWITCH → 策略停止开仓 |
+
+### 3.3 风控护栏
+
+| 规则 | 触发条件 | 行为 |
+|------|---------|------|
+| 仓位上限 | 单票市值 > 总资产 80% | 拦截买入，日志+事件记录 |
+| 卖出地板 | 持仓 ≤ 底仓 50% | 拦截卖出 |
+| PANIC 止损 | 浮亏 > max(-5×日ATR, -12%) | 触发恐慌卖出 |
+| uni_down 熔断 | 大盘判定为单边下行 | 禁止所有买入 |
+| KILL_SWITCH | `E:\06_T\t_io\gm_bridge\KILL_SWITCH` 存在 | 禁止新开仓（平仓不禁） |
+
+### 3.4 ⚠️ 重要：周一只看不跟
+
+- **周一不实盘跟随**，目标是验证链路
+- 观察信号节奏，熟悉通知格式
+- 记录任何异常（信号频率、价位偏差、通知延迟）
+
+### 3.5 紧急停止
+
+如果发现策略异常：
+
+1. 在 `E:\06_T\t_io\gm_bridge\` 下创建空文件 `KILL_SWITCH`
+2. 策略会在下一分钟停止新开仓（已持仓可继续卖出）
+3. 飞书收到 `🛑 KILL_SWITCH` 通知
+4. 复盘后手动删除该文件恢复
+
+命令行操作：
+```bash
+# 急停
+echo "" > "E:\06_T\t_io\gm_bridge\KILL_SWITCH"
+
+# 恢复（复盘后）
+rm "E:\06_T\t_io\gm_bridge\KILL_SWITCH"
+```
+
+---
+
+## 四、周一收盘后（15:00 — 15:30）
+
+### 4.1 检查事件记录
+
+```bash
+# 查看今日事件类型统计
+python -c "
+import json; types={}
+with open(r'E:\06_T\t_io\gm_bridge\events_20260727.jsonl', encoding='utf-8') as f:
+    for line in f:
+        try:
+            rec = json.loads(line)
+            types[rec['event']] = types.get(rec['event'], 0) + 1
+        except: pass
+for k,v in sorted(types.items()): print(f'  {k}: {v}')
+"
+```
+
+### 4.2 检查审计日志
+
+```bash
+# 查看决策轨迹
+python -c "
+import json; events=[]
+with open('gmcache/backtrace.jsonl', encoding='utf-8') as f:
+    for line in f:
+        try: events.append(json.loads(line))
+        except: pass
+print(f'审计记录: {len(events)} 条')
+no_sig = [e for e in events if e.get('event')=='no_signal']
+print(f'无信号记录: {len(no_sig)} 条')
+buys = [e for e in events if e.get('event')=='buy']
+print(f'买入记录: {len(buys)} 条')
+sells = [e for e in events if e.get('event')=='sell']
+print(f'卖出记录: {len(sells)} 条')
+"
+```
+
+### 4.3 Phase 1 成功标准
+
+| # | 检查项 | 通过标准 | 周一结果 |
+|---|--------|---------|---------|
+| 1 | 策略启动 | 09:25 前运行，无报错 | ☐ |
+| 2 | 底仓建仓 | 镜像股数买入成交，价格偏差记录 | ☐ |
+| 3 | 事件桥 | events jsonl 全天持续写入，字段完整 | ☐ |
+| 4 | 心跳 | 全天无"心跳中断"告警 | ☐ |
+| 5 | 信号观察 | no_signal 记录完整，能回答"为什么没动作" | ☐ |
+| 6 | 复盘产出 | 事件统计 + 交易摘要可生成 | ☐ |
+
+**6 项全绿 → 周二进入稳态，开始实盘跟随。**
+
+---
+
+## 五、周二起稳态运行
+
+### 每日节奏
+
+| 时间 | 事项 |
+|------|------|
+| 08:15 | 新闻推送（现有系统） |
+| 09:15 | watcher 自检 + 确认策略在线 |
+| 09:30-15:00 | 模拟盘自动交易 + 实盘跟随窗口 |
+| 15:00 | 收盘，检查事件完整性 |
+| 15:30 | 日复盘（事件统计 + 交易摘要） |
+
+### 实盘跟随规则（前两周 100% 人工确认）
+
+1. 只跟随"仅观察"标记以外的票（华工科技第一周不标记）
+2. 跟随前过 checklist：信号分数、当前仓位、现金、止损价
+3. 跟随量建议为模拟信号量的 50%~100%
+4. 模拟 vs 实盘偏差记入 follow_log
+5. **绝对禁止模拟盘信号自动直连实盘下单**
+
+---
+
+## 六、回测模式切换
+
+需要回测时，修改 `main.py` 底部：
+
+```python
+# ---- 回测模式 ----
+backtest_start = (datetime.now() - timedelta(days=90)).strftime("%Y-%m-%d %H:%M:%S")
+backtest_end = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
+run(strategy_id="...",
+    mode=MODE_BACKTEST,
+    ...)
+
+# ---- 模拟盘模式 ----
+# run(strategy_id="...",
+#     mode=MODE_LIVE,
+#     ...)
+```
+
+---
+
+## 七、目录速查
+
+| 路径 | 用途 |
+|------|------|
+| `main.py` | 策略主入口 |
+| `signals/engine.py` | 信号评分引擎 |
+| `signals/position_sizer.py` | 仓位计算 |
+| `gm_bridge/writer.py` | 事件桥写入 |
+| `gm_bridge/watcher.py` | 事件监听 + 飞书推送 |
+| `E:\06_T\t_io\gm_bridge\events_YYYYMMDD.jsonl` | 当日事件流 |
+| `E:\06_T\t_io\gm_bridge\heartbeat.json` | 心跳文件 |
+| `E:\06_T\t_io\gm_bridge\KILL_SWITCH` | 急停开关 |
+| `gmcache/backtrace.jsonl` | 审计决策轨迹 |
diff --git a/gm_bridge/__init__.py b/gm_bridge/__init__.py
new file mode 100644
index 0000000..8f2324d
--- /dev/null
+++ b/gm_bridge/__init__.py
@@ -0,0 +1,2 @@
+# gm_bridge — 事件桥模块
+# 策略进程只写文件，通知/复盘/风控在外部进程
diff --git a/gm_bridge/watcher.py b/gm_bridge/watcher.py
new file mode 100644
index 0000000..00c7e2c
--- /dev/null
+++ b/gm_bridge/watcher.py
@@ -0,0 +1,269 @@
+# coding=utf-8
+"""
+gm_bridge/watcher.py — 事件桥监听进程（去重/限流/心跳监控/旁路风控/飞书推送）
+
+独立进程，通过 tail 事件文件实现实时通知。
+不与 gm3 策略进程耦合——watcher 挂掉不影响交易。
+
+使用方式:
+  python gm_bridge/watcher.py [--date YYYYMMDD]
+
+依赖:
+  - E:\06_T\config.py 的 send_feishu_payload（若不可用则降级为 print）
+"""
+
+import json
+import os
+import sys
+import time
+import signal
+from datetime import datetime, timedelta
+from typing import Dict, Set, Optional
+
+# ── 桥路径 ──
+_06T_BRIDGE = r"E:\06_T\t_io\gm_bridge"
+_LOCAL = os.path.dirname(os.path.abspath(__file__))
+BRIDGE_DIR = _06T_BRIDGE if os.path.exists(r"E:\06_T") else _LOCAL
+
+# ── 股票名称映射 ──
+STOCK_NAMES = {
+    "000988": "华工科技",
+    "600481": "双良节能",
+    "600176": "中国巨石",
+    "603667": "五洲新春",
+    "588170": "科创芯片ETF",
+    "300153": "科泰电源",
+    "300364": "中文在线",
+}
+
+
+def _stock_label(code: str) -> str:
+    name = STOCK_NAMES.get(code, "")
+    return f"{name}({code})" if name else code
+
+
+def _events_path(date_str: str = None) -> str:
+    if date_str is None:
+        date_str = datetime.now().strftime("%Y%m%d")
+    return os.path.join(BRIDGE_DIR, f"events_{date_str}.jsonl")
+
+
+def _heartbeat_path() -> str:
+    return os.path.join(BRIDGE_DIR, "heartbeat.json")
+
+
+# ── 飞书推送（尝试复用 E:\06_T 基建） ──
+FEISHU_AVAILABLE = False
+_send_feishu = None
+_FEISHU_KEYWORD = "掘金模拟盘"
+
+try:
+    _06T = r"E:\06_T"
+    if _06T not in sys.path:
+        sys.path.insert(0, _06T)
+    import config as _cfg
+    _send_feishu = _cfg.send_feishu_payload
+    _FEISHU_KEYWORD = getattr(_cfg, 'FEISHU_KEYWORD', '掘金模拟盘')
+    FEISHU_AVAILABLE = True
+except Exception as e:
+    print(f"[watcher] 飞书不可用 ({e}), 降级为 stdout")
+
+
+def _push(title: str, content: str, level: str = "info"):
+    """推送通知（飞书优先，不可用时 print）"""
+    if FEISHU_AVAILABLE:
+        try:
+            template = {"green": "green", "orange": "orange", "red": "red"}.get(level, "blue")
+            card = {
+                "msg_type": "interactive",
+                "card": {
+                    "config": {"wide_screen_mode": True},
+                    "header": {
+                        "title": {"tag": "plain_text", "content": title},
+                        "template": template,
+                    },
+                    "elements": [{"tag": "markdown", "content": content}],
+                },
+            }
+            _send_feishu(payload=card, success_log="", error_prefix="watcher",
+                         trigger_urgent_alarm_after_success=(level == "red"))
+            return
+        except Exception:
+            pass
+    print(f"[watcher] {level.upper()} {title}: {content[:200]}")
+
+
+# ── 去重与限流 ──
+_seen_signals: Set[str] = set()
+_last_push: Dict[str, float] = {}  # 同类事件 60s 不重复推
+_last_heartbeat_ok = True
+
+
+def _dedup_key(rec: dict) -> str:
+    """去重键：event + code + action + bar"""
+    return f"{rec.get('event')}|{rec.get('code','')}|{rec.get('action',rec.get('side',''))}|{rec.get('time','')}"
+
+
+def _should_push(event_type: str, dedup_key: str) -> bool:
+    if dedup_key in _seen_signals:
+        return False
+    _seen_signals.add(dedup_key)
+    now = time.time()
+    last = _last_push.get(event_type, 0)
+    if now - last < 60:
+        return False
+    _last_push[event_type] = now
+    return True
+
+
+# ── 旁路风控 ──
+_positions: Dict[str, Dict] = {}
+_cash_estimate: float = 200000
+
+
+def _track_position(rec: dict):
+    global _positions, _cash_estimate
+    code = rec.get("code", "")
+    if rec["event"] == "fill":
+        side = rec.get("side", "")
+        qty = rec.get("qty", 0)
+        price = rec.get("price", 0)
+        if code not in _positions:
+            _positions[code] = {"qty": 0, "cost": 0}
+        if side == "BUY":
+            old_q = _positions[code]["qty"]
+            old_c = _positions[code]["cost"]
+            new_q = old_q + qty
+            _positions[code]["qty"] = new_q
+            _positions[code]["cost"] = (old_c * old_q + price * qty) / new_q if new_q > 0 else price
+            _cash_estimate -= qty * price
+        else:  # SELL
+            _positions[code]["qty"] = max(0, _positions[code]["qty"] - qty)
+            _cash_estimate += qty * price
+        # 仓位超限告警
+        total_mv = sum(p["qty"] * p["cost"] for p in _positions.values())
+        total_eq = _cash_estimate + total_mv
+        if total_eq > 0 and total_mv / total_eq > 0.80:
+            _push("⚠️ 仓位超限", f"{code} 市值占比 {total_mv/total_eq:.0%} > 80%", "orange")
+
+
+# ── 心跳监控 ──
+_last_hb_ts: float = time.time()
+
+
+def _check_heartbeat():
+    global _last_hb_ts, _last_heartbeat_ok
+    now = time.time()
+    try:
+        if os.path.exists(_heartbeat_path()):
+            mtime = os.path.getmtime(_heartbeat_path())
+            _last_hb_ts = mtime
+    except Exception:
+        pass
+    gap = now - _last_hb_ts
+    if gap > 600 and _last_heartbeat_ok:
+        _last_heartbeat_ok = False
+        _push("🚨 心跳中断", f"最后心跳 {gap/60:.0f} 分钟前，策略可能已停止", "red")
+    elif gap <= 600 and not _last_heartbeat_ok:
+        _last_heartbeat_ok = True
+        _push("✅ 心跳恢复", f"心跳已恢复 ({gap:.0f}s)", "green")
+
+
+# ── 事件处理 ──
+def handle_event(rec: dict):
+    event = rec.get("event", "")
+    dk = _dedup_key(rec)
+
+    code = rec.get("code", "")
+    label = _stock_label(code)
+
+    if event == "signal":
+        if _should_push("signal", dk):
+            action_cn = {"BUY_LOW": "低吸", "SELL_HIGH": "高抛", "PANIC_SELL": "恐慌卖出", "ADD_POS": "加仓"}.get(rec.get("action"), rec.get("action"))
+            emoji = "🟢" if "BUY" in str(rec.get("action", "")) else "🔴"
+            _push(
+                f"{emoji} {action_cn}信号 — {label}",
+                f"动作: {rec['action']} | 评分: {rec.get('score',0):.0f}分 | "
+                f"持仓: {rec.get('pos_qty',0)}股",
+                "green" if "BUY" in str(rec.get("action", "")) else "orange"
+            )
+
+    elif event == "fill":
+        _track_position(rec)
+        if _should_push("fill", dk):
+            side = rec.get("side", "")
+            emoji = "🔵" if side == "BUY" else "🔴"
+            side_cn = "买入" if side == "BUY" else "卖出"
+            _push(
+                f"{emoji} 成交 — {label}",
+                f"{side_cn} {rec.get('qty',0)}股 @ {rec.get('price',0):.2f} | "
+                f"成交后持仓: {rec.get('pos_after', '?')}股",
+                "green"
+            )
+
+    elif event == "reject":
+        side_cn = "买入" if rec.get("side") == "BUY" else "卖出"
+        _push(
+            f"❌ 委托被拒 — {label}",
+            f"{side_cn} {rec.get('qty',0)}股 | "
+            f"原因: {rec.get('reason','未知')}",
+            "red"
+        )
+
+    elif event == "risk":
+        kind = rec.get("kind", "")
+        if kind == "kill_switch":
+            _push(f"🛑 KILL_SWITCH — {label}", rec.get("detail", ""), "red")
+        elif kind in ("position_limit", "floor_protection"):
+            pass  # 不推飞书（噪音），仅记录
+        elif kind == "cash_insufficient":
+            if _should_push("risk", dk):
+                _push(f"💰 现金不足 — {label}", rec.get("detail", ""), "orange")
+        else:
+            if _should_push("risk", dk):
+                _push(f"⚠️ 风控 — {label}", f"{kind}: {rec.get('detail','')}", "orange")
+
+    elif event == "order":
+        pass  # 订单事件不单独推送，等 fill 再推
+
+    elif event == "heartbeat":
+        _last_hb_ts = time.time()
+
+
+# ── 主循环 ──
+def run(date_str: str = None):
+    if date_str is None:
+        date_str = datetime.now().strftime("%Y%m%d")
+    path = _events_path(date_str)
+    print(f"[watcher] 监听 {path}")
+    print(f"[watcher] 飞书: {'可用' if FEISHU_AVAILABLE else '不可用(stdout)'}")
+
+    last_size = 0
+    while True:
+        try:
+            if os.path.exists(path):
+                size = os.path.getsize(path)
+                if size > last_size:
+                    with open(path, "r", encoding="utf-8") as f:
+                        f.seek(last_size)
+                        for line in f:
+                            line = line.strip()
+                            if not line:
+                                continue
+                            try:
+                                rec = json.loads(line)
+                                handle_event(rec)
+                            except json.JSONDecodeError:
+                                pass
+                    last_size = size
+        except Exception as e:
+            print(f"[watcher] 读取异常: {e}")
+
+        _check_heartbeat()
+        time.sleep(2)
+
+
+if __name__ == "__main__":
+    date_arg = sys.argv[2] if len(sys.argv) > 2 and sys.argv[1] == "--date" else None
+    signal.signal(signal.SIGINT, lambda s, f: sys.exit(0))
+    run(date_arg)
diff --git a/gm_bridge/writer.py b/gm_bridge/writer.py
new file mode 100644
index 0000000..55e7118
--- /dev/null
+++ b/gm_bridge/writer.py
@@ -0,0 +1,166 @@
+# coding=utf-8
+"""
+gm_bridge/writer.py — 事件桥写入（仅写文件，不碰网络/飞书）
+
+事件 schema 见 docs/模拟盘实施方案_20260727.md 附录 A：
+  signal: 信号生成
+  order:  委托发出
+  fill:   全部成交
+  reject: 拒单/撤单
+  risk:   风控事件 (仓位/地板/PANIC/熔断/急停)
+  heartbeat: 每分钟心跳（仓位+现金）
+"""
+
+import json
+import os
+import time
+from datetime import datetime
+from typing import Dict, Any, Optional
+
+# ── 桥目录配置 ──
+_06T_BRIDGE = r"E:\06_T\t_io\gm_bridge"
+_LOCAL_BRIDGE = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "gm_bridge")
+
+def _bridge_dir() -> str:
+    """桥目录：E:\06_T\t_io\gm_bridge 优先，不存在则用本地 gm_bridge"""
+    try:
+        if os.path.exists(r"E:\06_T"):
+            d = _06T_BRIDGE
+            os.makedirs(d, exist_ok=True)
+            return d
+    except Exception:
+        pass
+    os.makedirs(_LOCAL_BRIDGE, exist_ok=True)
+    return _LOCAL_BRIDGE
+
+BRIDGE_DIR = _bridge_dir()
+
+
+def _events_path(date_str: str = None) -> str:
+    if date_str is None:
+        date_str = datetime.now().strftime("%Y%m%d")
+    return os.path.join(BRIDGE_DIR, f"events_{date_str}.jsonl")
+
+
+def _heartbeat_path() -> str:
+    return os.path.join(BRIDGE_DIR, "heartbeat.json")
+
+
+def _kill_switch_path() -> str:
+    return os.path.join(BRIDGE_DIR, "KILL_SWITCH")
+
+
+# ── 写入工具 ──
+
+def _append_jsonl(path: str, rec: dict):
+    """安全追加一行 JSON"""
+    try:
+        rec["_ts"] = time.time()
+        with open(path, "a", encoding="utf-8") as f:
+            f.write(json.dumps(rec, ensure_ascii=False, default=str) + "\n")
+    except Exception:
+        pass
+
+
+def _write_json(path: str, data):
+    """整文件覆写（heartbeat 用）"""
+    try:
+        with open(path, "w", encoding="utf-8") as f:
+            json.dump(data, f, ensure_ascii=False, default=str)
+    except Exception:
+        pass
+
+
+# ── 公开 API：事件写入 ──
+
+def write_signal(time_str: str, code: str, action: str, score: float,
+                 reasons: list = None, pos_qty: int = 0):
+    """信号生成事件"""
+    _append_jsonl(_events_path(), {
+        "event": "signal",
+        "time": time_str,
+        "code": code,
+        "action": action,
+        "score": score,
+        "reasons": reasons or [],
+        "pos_qty": pos_qty,
+    })
+
+
+def write_order(time_str: str, code: str, side: str, qty: int, price: float,
+                order_id: str = "", order_type: str = "MKT"):
+    """委托发出事件"""
+    _append_jsonl(_events_path(), {
+        "event": "order",
+        "time": time_str,
+        "code": code,
+        "side": side,
+        "qty": qty,
+        "price": price,
+        "order_id": str(order_id),
+        "order_type": order_type,
+    })
+
+
+def write_fill(time_str: str, code: str, side: str, qty: int, price: float,
+               order_id: str = "", pos_after: int = 0):
+    """全部成交事件"""
+    _append_jsonl(_events_path(), {
+        "event": "fill",
+        "time": time_str,
+        "code": code,
+        "side": side,
+        "qty": qty,
+        "price": price,
+        "order_id": str(order_id),
+        "pos_after": pos_after,
+    })
+
+
+def write_reject(time_str: str, code: str, side: str, qty: int,
+                 reason: str = "", raw: dict = None):
+    """拒单/撤单事件"""
+    _append_jsonl(_events_path(), {
+        "event": "reject",
+        "time": time_str,
+        "code": code,
+        "side": side,
+        "qty": qty,
+        "reason": reason,
+        "raw": str(raw) if raw else "",
+    })
+
+
+def write_risk(time_str: str, kind: str, detail: str = "", code: str = ""):
+    """风控事件（仓位拦截/地板保护/PANIC/熔断/急停）"""
+    _append_jsonl(_events_path(), {
+        "event": "risk",
+        "time": time_str,
+        "code": code,
+        "kind": kind,
+        "detail": detail,
+    })
+
+
+def write_heartbeat(time_str: str, bar: str, positions: Dict[str, Any],
+                    cash: float = 0.0, index_regime: str = "", index_score: float = 0.0):
+    """每分钟心跳"""
+    _write_json(_heartbeat_path(), {
+        "event": "heartbeat",
+        "time": time_str,
+        "bar": bar,
+        "positions": positions,
+        "cash": cash,
+        "index_regime": index_regime,
+        "index_score": index_score,
+    })
+
+
+# ── 公开 API：风控文件 ──
+
+def check_kill_switch() -> bool:
+    """检查 KILL_SWITCH 文件是否存在。存在 → 返回 True（禁止新开仓）"""
+    try:
+        return os.path.exists(_kill_switch_path())
+    except Exception:
+        return False
diff --git a/main.py b/main.py
index d59d134..7c11a3b 100644
--- a/main.py
+++ b/main.py
@@ -18,20 +18,49 @@ if PROJECT_DIR not in sys.path:
 
 from config.params import PARAMS, STOCK_PARAMS
 from data.indicators import add_indicators, clean_code
-from signal.engine import SignalEngine, SIM_NOW as SE_SIM_NOW
-from signal.position_sizer import PositionSizer
+from signals.engine import SignalEngine, SIM_NOW as SE_SIM_NOW
+from signals.position_sizer import PositionSizer
 from utils.helpers import SIM_NOW, _now, get_today_str, _default_daily_context
+from gm_bridge.writer import (
+    write_signal, write_order, write_fill, write_reject, write_risk,
+    write_heartbeat, check_kill_switch,
+)
 
 # ── 标的 ──
-STOCKS = {"000988": "SZSE.000988"}
-STOCK_NAMES = {"000988": "华工科技"}
+# 代码 → gm_symbol 映射
+STOCKS = {
+    "000988": "SZSE.000988",
+    "600481": "SHSE.600481",
+    "600176": "SHSE.600176",
+    "603667": "SHSE.603667",
+    "588170": "SHSE.588170",
+}
+STOCK_NAMES = {
+    "000988": "华工科技",
+    "600481": "双良节能",
+    "600176": "中国巨石",
+    "603667": "五洲新春",
+    "588170": "科创芯片ETF",
+}
 REVERSE_MAP = {v: k for k, v in STOCKS.items()}
+
+# ── 镜像持仓（与实盘账户一致） ──
+# 模拟盘建仓时按此表中的股数/成本下单
+MIRROR_HOLDINGS = {
+    "000988": {"qty": 300,  "cost": 247.29},
+    "600481": {"qty": 1400, "cost": 6.03},
+    "600176": {"qty": 300,  "cost": 67.93},
+    "603667": {"qty": 500,  "cost": 58.78},
+    "588170": {"qty": 4000, "cost": 0.92},
+}
+
 COMMISSION = PARAMS["commission_rate"]
 MIN_BARS = 25
-BASE_POSITION_PCT = 0.50           # 底仓占用资金比例
 T1_AUTO_UNLOCK_HOUR = 9
 T1_AUTO_UNLOCK_MINUTE = 31
-INITIAL_CASH = 200000
+# 镜像持仓总市值约 77,000（300×247 + 1400×6 + 300×68 + 500×59 + 4000×1 ≈ 77k）
+# 按市值×1.5 配置模拟盘资金（留足做T现金水位）
+INITIAL_CASH = 150000
 MAX_BASE_RETRY = 3
 
 
@@ -49,14 +78,6 @@ def _build_bar_df(context, code: str, gm_symbol: str) -> pd.DataFrame:
     return df
 
 
-def _calc_base_qty(cash: float, price: float) -> int:
-    """按资金反推算底仓股数（100 的倍数）"""
-    if price <= 0:
-        return 0
-    raw_qty = cash * BASE_POSITION_PCT / price
-    qty = int(raw_qty // 100) * 100
-    return max(qty, 100)
-
 
 def _get_holding(context, code: str, gm_symbol: str) -> dict:
     """多源持仓读取，按优先级：
@@ -91,7 +112,10 @@ def _get_holding(context, code: str, gm_symbol: str) -> dict:
                 mp = context.manual_position.get(gm_symbol)
                 if mp and abs(int(mp.get("qty", 0)) - int(p.volume)) > 0:
                     context.manual_position[gm_symbol] = gm_pos
-                return gm_pos
+                # qty 一致时返回 manual_position（我们跟踪的成本），不返回 gm_pos
+                # gm_pos 的 vwap 可能含前复权调整，与真实买入成本不一致
+                if mp and int(mp.get("qty", 0) or 0) > 0:
+                    return mp
         except Exception:
             pass
 
@@ -276,6 +300,10 @@ def init(context):
     symbols = list(STOCKS.values())
     subscribe(symbols=symbols, frequency="60s", count=240,
               fields="symbol,eob,open,high,low,close,volume,amount")
+    # 确保事件桥目录存在
+    from gm_bridge.writer import BRIDGE_DIR
+    os.makedirs(BRIDGE_DIR, exist_ok=True)
+    print(f"[init] 事件桥: {BRIDGE_DIR}")
     print(f"[init] 策略初始化完成: {len(symbols)} 只标的")
 
 
@@ -283,7 +311,7 @@ def on_bar(context, bars):
     now = context.now if hasattr(context, "now") else datetime.now()
     import utils.helpers as uh
     uh.SIM_NOW = now
-    import signal.engine as se
+    import signals.engine as se
     se.SIM_NOW = now
 
     t = now.time()
@@ -298,6 +326,9 @@ def on_bar(context, bars):
         context.engine._check_date_reset()
         _audit_write({"event": "date_reset", "date": str(today)})
 
+    # ── KILL_SWITCH 检查 ──
+    _killed = check_kill_switch()
+
     if t < dtime(9, 30) or (dtime(11, 30) < t < dtime(13, 0)) or t > dtime(15, 0):
         return
 
@@ -341,6 +372,18 @@ def on_bar(context, bars):
         except Exception as e:
             print(f"[ir] 大盘态势判定失败: {e}")
 
+    # ── 心跳（每分钟写一次） ──
+    _hb_positions = {}
+    for _s, _h in context.manual_position.items():
+        _hb_positions[_s] = {"qty": _h.get("qty", 0), "cost": _h.get("cost", 0)}
+    write_heartbeat(
+        time_str=str(now), bar=f"{now:%H:%M}",
+        positions=_hb_positions,
+        cash=INITIAL_CASH,  # 实际现金由 N3 逻辑读取
+        index_regime=context.last_index_regime,
+        index_score=context.last_index_score,
+    )
+
     for bar in bars:
         gm_sym = str(bar["symbol"])
         code = _raw_code(gm_sym)
@@ -375,20 +418,25 @@ def on_bar(context, bars):
                 print(f"[D6] 结论: volume单位为股，需移除×100")
             context._vwap_checked = True
 
-        # ── D2: 底仓（按资金反推） ──
+        # ── D2: 底仓（按镜像持仓表逐股建仓） ──
         if code not in context._base_ordered and code not in context._base_settled:
-            # 尚未下单，计算底仓
-            # 初始资金用初始化参数
-            base_qty = _calc_base_qty(INITIAL_CASH, cp)
+            mirror = MIRROR_HOLDINGS.get(code, {})
+            base_qty = mirror.get("qty", 0)
             if base_qty < 100:
-                base_qty = 100
+                print(f"[{now:%H:%M:%S}] BASE {code} 跳过: MIRROR_HOLDINGS 中无此标的或 qty<100")
+                context._base_settled.add(code)  # 标记已处理，不再尝试
+                return
             try:
+                try:
+                    write_order(str(now), code, "BUY", base_qty, cp, order_id="base")
+                except Exception:
+                    pass
                 order_volume(symbol=gm_sym, volume=base_qty,
                              side=OrderSide_Buy,
                              order_type=OrderType_Market,
                              position_effect=PositionEffect_Open)
                 context._base_ordered.add(code)
-                print(f"[{now:%H:%M:%S}] BASE {code} 下单 {base_qty}股@{cp:.2f} (资金=200000, 价格={cp:.2f})")
+                print(f"[{now:%H:%M:%S}] BASE {code} {STOCK_NAMES.get(code,code)} 下单 {base_qty}股@{cp:.2f}")
                 _audit_write({"event": "base_order", "code": code, "qty": base_qty, "price": cp, "time": str(now)})
             except Exception as e:
                 print(f"[{now:%H:%M:%S}] BASE {code} 下单失败: {e}")
@@ -442,9 +490,12 @@ def on_bar(context, bars):
         if is_tail and sig and sig.action in ("BUY_LOW", "ADD_POS"):
             sig = None
 
-        # ── D5/N4: 深度亏损 → PANIC_SELL（移除 sell_score < 40 死锁条件） ──
+        # ── D5/N4: 深度亏损 → PANIC_SELL ──
         feats_cache = getattr(context.engine, "_last_feats", {}).get(code, {})
-        if feats_cache.get("is_deep_loss"):
+        # 防重复：已进入冷却的 PANIC 不重复生成
+        _panic_on_cooldown = (code in context.engine.sell_cooldown
+                              and now < context.engine.sell_cooldown.get(code, now))
+        if feats_cache.get("is_deep_loss") and not _panic_on_cooldown:
             from data.indicators import Signal
             sig = Signal(code=code, name=STOCK_NAMES.get(code, code),
                          action="PANIC_SELL", price=cp, score=75.0,
@@ -460,15 +511,22 @@ def on_bar(context, bars):
                 if excess >= 100:
                     qty = (excess // 100) * 100
                     try:
-                        order_volume(symbol=gm_sym, volume=qty,
-                                     side=OrderSide_Sell,
-                                     order_type=OrderType_Market,
-                                     position_effect=PositionEffect_Close)
-                        print(f'[{now:%H:%M:%S}] TAIL {code} 尾盘归位 {qty}股 (pos={pos_qty} target={target})')
-                        context.total_trade_count += 1
-                        context.daily_sell_count[code] = context.daily_sell_count.get(code, 0) + 1
+                        write_order(str(now), code, "SELL", qty, cp, order_id="tail")
                     except Exception:
                         pass
+                    order_volume(symbol=gm_sym, volume=qty,
+                                 side=OrderSide_Sell,
+                                 order_type=OrderType_Market,
+                                 position_effect=PositionEffect_Close)
+                    # 立即更新 manual_position 防下一分钟重复触发
+                    if gm_sym in context.manual_position:
+                        new_pos = pos_qty - qty
+                        context.manual_position[gm_sym]["qty"] = new_pos
+                        context.manual_position[gm_sym]["available"] = new_pos
+                        context.manual_position[gm_sym]["t_qty"] = new_pos
+                    print(f'[{now:%H:%M:%S}] TAIL {code} 尾盘归位 {qty}股 (pos={pos_qty}→{pos_qty-qty} target={target})')
+                    context.total_trade_count += 1
+                    context.daily_sell_count[code] = context.daily_sell_count.get(code, 0) + 1
                     continue
 
         # ── N6: 开盘5分钟拦截买入信号 ──
@@ -485,22 +543,44 @@ def on_bar(context, bars):
                 "buy_blocks": _last_dec.get("buy_blocks", []),
                 "sell_blocks": _last_dec.get("sell_blocks", []),
                 "decision_reason": _last_dec.get("reason", ""),
+                "profit_pct": feats_cache.get("profit_pct", 0),
+                "daily_atr": feats_cache.get("daily_atr", 0),
+                "is_deep_loss": feats_cache.get("is_deep_loss", False),
             })
             continue
 
+        # ── 信号事件写入 ──
+        if sig is not None:
+            try:
+                write_signal(str(now), code, sig.action, sig.score,
+                             reasons=sig.reasons, pos_qty=pos_qty)
+            except Exception:
+                pass
+
         # ── D5: 卖出地板保护 ──
         stock_params = STOCK_PARAMS.get(code, {})
         max_sells = stock_params.get("max_sell_times_per_stock", 3)
         max_buys = stock_params.get("max_buy_times_per_stock", 3)
 
         if sig.action in ("SELL_HIGH", "PANIC_SELL"):
-            sell_floor_ratio = float(PARAMS.get("sell_floor_ratio", 0.5))
-            # base_qty 跟踪（首次有持仓时记录）
             base_ref = getattr(context, f"_base_ref_{code}", pos_qty)
             setattr(context, f"_base_ref_{code}", base_ref)
+            # PANIC_SELL 地板降至 0（深亏时允许清仓），普通卖出保持 50%
+            if sig.action == "PANIC_SELL":
+                sell_floor_ratio = 0.0
+            else:
+                sell_floor_ratio = float(PARAMS.get("sell_floor_ratio", 0.5))
             min_hold = int(base_ref * sell_floor_ratio)
             if pos_qty - 100 < min_hold:
                 print(f"[{now:%H:%M:%S}] SELL {code} SKIP: 地板保护 base_ref={base_ref} min_hold={min_hold} pos_qty={pos_qty}")
+                try:
+                    write_risk(str(now), "floor_protection",
+                               f"base_ref={base_ref} min_hold={min_hold} pos_qty={pos_qty} action={sig.action}", code=code)
+                except Exception:
+                    pass
+                # PANIC 虽被地板挡，仍设冷却防每分钟重复触发
+                if sig.action == "PANIC_SELL":
+                    context.engine.record_trade_action(code, "PANIC_SELL", 0, cp)
                 continue
 
         # ── D1: 引擎冷却/计数 ──
@@ -512,34 +592,74 @@ def on_bar(context, bars):
 
         # 执行交易
         if sig.action in ("BUY_LOW", "ADD_POS"):
+            if _killed:
+                try:
+                    write_risk(str(now), "kill_switch", f"KILL_SWITCH 阻止 {code} 买入", code=code)
+                except Exception:
+                    pass
+                continue
             bc = context.daily_buy_count.get(code, 0)
             if bc >= max_buys:
                 continue
-            qty = context.sizer.calc_buy_qty(code, holding, sig.score, threshold)
-            if qty < 100:
-                qty = 300
-            # N3: 现金预检
-            available_cash = 100000
+
+            # N3: 现金预检（移到 sizer 之前，供 target_t 计算）
+            available_cash = INITIAL_CASH
+            _cash_ok = False
             try:
                 _acct = context.account()
-                if hasattr(_acct, 'cash'):
-                    available_cash = float(_acct.cash())
+                _c = getattr(_acct, 'cash', None)
+                if _c is not None:
+                    _c = _c() if callable(_c) else _c
+                    if isinstance(_c, dict):
+                        # gm3 account().cash 返回 dict，键名可能是 available/available_cash/total
+                        _v = _c.get('available') or _c.get('available_cash') or _c.get('cash') or _c.get('total') or 0
+                        available_cash = float(_v)
+                    else:
+                        available_cash = float(_c)
+                    _cash_ok = True
             except Exception:
                 pass
+            if not _cash_ok and not getattr(context, '_cash_warned', False):
+                context._cash_warned = True
+                print(f'[N8] WARN: 无法读取可用现金, 使用初始资金={INITIAL_CASH}')
+
+            # N10: 算 target_t（仓位上限约束下的最大仓位，供 sizer 算 max_buyable）
+            pos_limit_pct = float(PARAMS.get('max_single_position_pct', 0.80))
+            estimated_equity = available_cash + pos_qty * cp
+            max_pos_shares = int(estimated_equity * pos_limit_pct / cp / 100) * 100 if cp > 0 else 0
+            _base_ref = getattr(context, f'_base_ref_{code}', 0) or pos_qty
+            target_t = max(max_pos_shares, _base_ref, pos_qty)
+            holding_with_target = dict(holding, target_t=target_t)
+
+            qty = context.sizer.calc_buy_qty(code, holding_with_target, sig.score, threshold)
+            if qty <= 0:
+                qty = 300  # sizer 返回 0 时的最小交易量
+
+            # N3: 现金预检
             max_by_cash = int(available_cash * 0.95 / cp / 100) * 100 if cp > 0 else 0
             qty = min(qty, max_by_cash) if max_by_cash > 0 else qty
             if qty < 100:
                 if bc == 0:
                     print(f'[{now:%H:%M:%S}] BUY {code} 现金不足跳过: 可用={available_cash:.0f}')
+                try:
+                    write_risk(str(now), "cash_insufficient",
+                               f"available={available_cash:.0f} needed={qty*cp:.0f}", code=code)
+                except Exception:
+                    pass
                 continue
+
             # N2: 仓位上限检查
-            pos_limit_pct = float(PARAMS.get('max_single_position_pct', 0.30))
             current_pos_value = pos_qty * cp
             new_pos_value = current_pos_value + qty * cp
             total_equity_value = available_cash + current_pos_value
             if total_equity_value > 0 and new_pos_value / total_equity_value > pos_limit_pct:
                 print(f'[{now:%H:%M:%S}] BUY {code} 仓位上限拦截: {new_pos_value/total_equity_value:.0%}>{pos_limit_pct:.0%}')
-                continue  # fallback
+                try:
+                    write_risk(str(now), "position_limit",
+                               f"{new_pos_value/total_equity_value:.1%}>{pos_limit_pct:.0%} qty={qty}", code=code)
+                except Exception:
+                    pass
+                continue
             try:
                 order_volume(symbol=gm_sym, volume=qty,
                              side=OrderSide_Buy,
@@ -574,6 +694,10 @@ def on_bar(context, bars):
             if qty < 100:
                 continue
             try:
+                try:
+                    write_order(str(now), code, "SELL", qty, cp)
+                except Exception:
+                    pass
                 order_volume(symbol=gm_sym, volume=qty,
                              side=OrderSide_Sell,
                              order_type=OrderType_Market,
@@ -603,6 +727,13 @@ def on_order_status(context, order):
 
     if status == 3:  # 全部成交
         code = _raw_code(symbol)
+        _side = "BUY" if side == 1 else "SELL"
+        _pos_after = int(context.executed_orders.get(symbol, {}).get("qty", 0)) if _side == "SELL" else                      int(context.executed_orders.get(symbol, {}).get("qty", 0)) + volume if symbol in context.executed_orders else volume
+        try:
+            write_fill(str(datetime.now()), _raw_code(symbol), _side, volume, price,
+                       pos_after=_pos_after)
+        except Exception:
+            pass
         # P0-2: 成交回调接线冷却（只有真的成交了才计冷却，避免下单即计）
         if code in STOCKS:
             _action = 'BUY_LOW' if side == 1 else 'SELL_HIGH'
@@ -626,17 +757,21 @@ def on_order_status(context, order):
             if code in context._base_ordered and code not in context._base_settled:
                 context._base_settled.add(code)
                 context.manual_position[symbol] = dict(context.executed_orders[symbol])
+                # 记录底仓参考量（供 sizer/sell_floor/tail 使用）
+                setattr(context, f'_base_ref_{code}', volume)
                 print(f"[BASE] {code} 底仓成交 {volume}股@{price:.2f}")
         elif side == 2:  # 卖出
             old = context.executed_orders.get(symbol, {"qty": 0, "available": 0})
             old_qty = int(old.get("qty", 0))
+            old_cost = old.get("cost", price)
             new_qty = max(0, old_qty - volume)
+            # cost 保持不变（买入成本），不覆写为卖出价
             context.executed_orders[symbol] = {
                 "name": STOCK_NAMES.get(code, code),
                 "qty": new_qty,
                 "available": new_qty,
                 "t_qty": new_qty,
-                "cost": price,
+                "cost": old_cost,
                 "type": "stock",
                 "pre_close": price,
             }
@@ -655,6 +790,13 @@ def on_order_status(context, order):
             else:
                 print(f'[ORDER] {code} 底仓拒单已达上限({MAX_BASE_RETRY})，停止重试')
         print(f"[ORDER] {symbol} 被拒 status={status}")
+        try:
+            _r_code = _raw_code(symbol)
+            _r_side = "BUY" if side == 1 else "SELL"
+            write_reject(str(datetime.now()), _r_code, _r_side, volume,
+                         reason=f"status={status}", raw={"status": status, "side": side, "volume": volume})
+        except Exception:
+            pass
 
 
 def on_backtest_finished(context, indicator):
@@ -674,17 +816,24 @@ def on_backtest_finished(context, indicator):
 
 if __name__ == "__main__":
     _AUDIT_RUN_ID = datetime.now().strftime("%Y%m%d_%H%M%S")
-    backtest_start = (datetime.now() - timedelta(days=90)).strftime("%Y-%m-%d %H:%M:%S")  # 90天 ~4-7min
-    backtest_end = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
 
+    # ---- 回测模式（快速测试） ----
+    backtest_start = (datetime.now() - timedelta(days=5)).strftime("%Y-%m-%d %H:%M:%S")
+    backtest_end = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
     run(strategy_id="e8bb1f4d-87ce-11f1-97f7-98fa9b8df5e7",
         filename="main.py",
-        mode=MODE_BACKTEST,
+        mode=MODE_LIVE,
         token="480a6c84b0f43417ffcc9c15162dd7256ca9c3b0",
         backtest_start_time=backtest_start,
         backtest_end_time=backtest_end,
         backtest_adjust=ADJUST_PREV,
-        backtest_initial_cash=200000,
+        backtest_initial_cash=150000,
         backtest_commission_ratio=COMMISSION,
         backtest_slippage_ratio=0.0001,
         backtest_match_mode=1)
+
+    # ---- 模拟盘模式 ----
+    # run(strategy_id="e8bb1f4d-87ce-11f1-97f7-98fa9b8df5e7",
+    #     filename="main.py",
+    #     mode=MODE_BACKTEST,
+    #     token="480a6c84b0f43417ffcc9c15162dd7256ca9b8df5e7")
diff --git a/signal/__init__.py b/signals/__init__.py
similarity index 100%
rename from signal/__init__.py
rename to signals/__init__.py
diff --git a/signal/engine.py b/signals/engine.py
similarity index 98%
rename from signal/engine.py
rename to signals/engine.py
index ebf9961..c5d746d 100644
--- a/signal/engine.py
+++ b/signals/engine.py
@@ -346,7 +346,13 @@ class FeatureExtractor:
         daily_atr = float(daily_ctx.get("daily_atr", 0) or 0)
         if daily_atr <= 0:
             daily_atr = atr * 14  # 1分钟ATR×14 ≈ 日ATR 近似
-        feats["is_deep_loss"] = cost > 0 and feats["profit_pct"] < -5 * daily_atr
+        # N9 fix: PANIC触发线带固定下限 -12%，防止暴跌中ATR自解除
+        _panic_floor = -0.12  # -12% 固定下限
+        _panic_atr_line = -5 * daily_atr
+        _panic_trigger = max(_panic_atr_line, _panic_floor)
+        feats["is_deep_loss"] = cost > 0 and feats["profit_pct"] < _panic_trigger
+        feats["panic_trigger"] = _panic_trigger
+        feats["panic_atr_line"] = _panic_atr_line
         dc = daily_ctx if isinstance(daily_ctx, dict) else {}
         for k in ["daily_status", "daily_gate", "daily_trend_bg", "daily_ma5_state",
                    "daily_support_name", "index_regime"]:
diff --git a/signal/position_sizer.py b/signals/position_sizer.py
similarity index 82%
rename from signal/position_sizer.py
rename to signals/position_sizer.py
index 3c9b5ec..cd51283 100644
--- a/signal/position_sizer.py
+++ b/signals/position_sizer.py
@@ -67,11 +67,17 @@ class PositionSizer:
         else:
             pct = p.get("stock_first_add_weak_pct", 0.10)
 
+        # N10 fix: 从外部传入 target_t（最大允许仓位，由仓位上限/资金约束算出）
+        # target_t 应 > 当前持仓，给 T 交易留出空间
         hold_qty = int(holding.get("qty", 0) or 0)
-        max_buyable = max(0, total_t - hold_qty)
+        target_t = int(holding.get("target_t", 0) or 0)
+        if target_t <= hold_qty:
+            # 未传入 target_t 或 target_t 不合理时，至少给 hold_qty*1.3 的 T 仓空间
+            target_t = max(total_t, int(hold_qty * 1.5))
+        max_buyable = max(0, target_t - hold_qty)
         if max_buyable <= 0:
             return 0
-        qty = int(total_t * pct)
+        qty = int(target_t * pct)
         qty = min(qty, max_buyable)
         qty = max(min_unit, (qty // min_unit) * min_unit)
         return qty
