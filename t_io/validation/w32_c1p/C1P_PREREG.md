# C1' 全管线决赛 · 预注册闸门（跑前锁定）

> **锁定时间**：2026-08-08 09:44（用户拍板口径 B 后）
> **变体定义（口径 B，用户已拍板）**：C1' = C1 接回解耦（W32 既有实现，绕过 {daily_overheated, index_uni_down_clearance}）**+ 全部买信号单股日限 7 条内置于状态机**（按时间序保留前 7 条/(code,date)，第 8 条起不再产生买入信号；cap 作用于全部买信号，不分 ctl 原有/接回/二阶增量）
> **基线**：ctl = V1.1.2 现行生产口径同窗重跑（或复用 W32 ctl 产物 + ≥4 块字节级确定性校验，由执行者按代码隔离度判定并留证）
> **离线预判**（`w32_c1p/c1p_offline_cap.json`，2026-08-08）：口径 B 六闸全过——买 194@0.5055 / 闭环 99 对 +598.85 / 密度 0.431 / max=7；被 cap 仅 3 条（2 VOID + 1 FAIL，000988 尾盘 14:31~14:40）。离线过滤是近似，状态机内置 cap 的二阶效应以全管线为准。

## 口径发现注记（W32 勘误，痕迹保留）

W32 报告 §4.3 破闸归因"接回状态机同股日重触发"**经解剖证伪**：3 个破闸日（000988：2026-03-25 / 04-29 / 06-12）各 8 条买信号**全部为 ctl 基线原有信号**（接回增量 0、二阶增量 0）；ctl 基线自身 max_per_code_day = 8。闸门⑤后半句实际严于基线现状。W32 不翻案（C1 未采纳的裁决维持——当时口径下破闸属实），但 C1' 的闸门⑤语义随之明确为：**变体必须满足全局日限 7（用户拍板认可该约束本身是有益的全局防刷屏机制，同时约束基线原有信号流）**。本注记同步登记 W33 候选池，W33 周复盘正式勘误 W32 报告（不改原文）。

## 六闸门（同 W32 §5，逐项判定，任一破即 GATE_BREACH 不采纳）

| # | 闸门 | 阈值 |
|---|---|---|
| ① | 总买 wr | ≥ 0.4788（ctl −2.5pp） |
| ② | 闭环 | ≥ 47 对 且 PnL ≥ +252.98（以本次 ctl 实测值为准，若与 W32 ctl 有确定性漂移则记录并以新鲜值为准） |
| ③ | 卖侧 wr 退化 | ≤ 1pp（vs 本次 ctl） |
| ④ | 阴跌日买 wr | ≥ 0.30 |
| ⑤ | 买密度 | ∈ [0.30, 0.60]/股日 **且** 单股 max ≤ 7/日（复合闸，任一子项破即整闸破） |
| ⑥ | 绕过白名单校验 | 仅 {daily_overheated, index_uni_down_clearance} 被绕过，violations = 0 |
| 附加 | bear 日型接回 wr | ≥ 0.35（防 uni_down 熔断日接飞刀） |
| 附加 | cap 命中审计 | 输出全部被 cap 信号清单（ts/code/settle/分数），人工复核无高分 WIN 被误杀 |

## 纪律

- 破闸即否决，不因其余过闸放行；不改闸。
- 确定性：≥4 块双跑字节级一致；合并 MERGE_OK；MD5 双跑一致。
- 跑批前查杀孤儿进程（`ps aux | grep -E "harness_backtest|run_w32_c1"`），超时块根因=孤儿 stdout 死管道写阻塞。
- cap 实现：harness 注入开关 + 引擎软消费，**默认关，生产零改动**；harness 开关清单写入决赛报告。
- 决赛通过后采纳动作（改 config/引擎生产语义）仍需用户最终确认。
