# C1' 全管线决赛报告（口径 B）· VERDICT: ALL_GATES_PASS

> **日期**：2026-08-08 · **预注册闸门**：`t_io/validation/w32_c1p/C1P_PREREG.md`（09:44 锁定，未改闸）
> **变体（用户拍板口径 B）**：C1' = C1 接回解耦（绕过 {daily_overheated, index_uni_down_clearance}）+ **全部买信号单股日限 7 条内置于状态机**（同 (code,date) 按时间序前 7 条正常产生，第 8 条起不再产生；不分 ctl 原有/接回/二阶增量；卖信号不受限）
> **基线 ctl**：复用 W32 ctl 产物（判定见 §2）——buy 144@0.5038 / sell 335@0.4638 / 闭环 47 对 +252.98 / 密度 0.320

## §1 实现位置（harness 注入开关 + 引擎软消费，默认关，生产零改动）

| 位置 | 改动 |
|---|---|
| `signal_engine.py` `__init__` | 新增 `self.buy_recorded_today`（record_signal 层当日已记录买信号计数，仅 cap 开启时递增） |
| `signal_engine.py` `_reset_daily_state_if_needed` | 计数器随日界重置（SIM_NOW 感知，回测/实盘通用） |
| `signal_engine.py` `record_signal` | `if PARAMS.get("buy_daily_cap"):` 守卫下递增计数（**默认关时跳过，零行为变化**） |
| `signal_engine.py` `buy_daily_cap_reached(code)` | 新谓词：cap 未配置首行返回 False；计数口径=record_signal 层已记录买信号（与 signals.jsonl 逐条对应） |
| `harness_backtest.py` env 注入 | `T_BUY_DAILY_CAP=7` → `PARAMS["buy_daily_cap"]=7`（紧邻 C1 的 T_BUYBACK_BYPASS_GATES 注入点） |
| `harness_backtest.py` 记录层 | 段去重之后、`record_signal` 之前：`sig.action ∈ {BUY_LOW,ADD_POS} and engine.buy_daily_cap_reached(code)` → 落 `day_capped` 并 `continue`——**无冷却/无簿记/无落盘，状态机看到被 cap 后的世界（二阶效应真实）** |
| `harness_backtest.py` 结算/落盘 | 被 cap 信号同口径结算后写 `capped_buys_v102.jsonl`（仅 cap 开启且有命中才写，ctl 输出目录逐字节不变） |

**harness 开关清单（本变体生效组合）**：`T_BUY_BONUS_MIN_SCORE=36` + `T_NOTIFY_BUY=36`（T36b 同款基线 env）+ `T_HOLDINGS_FILE=holdings_snapshot_3d80810.json` + `T_BUYBACK_BYPASS_GATES=1`（C1 既有）+ `T_BUY_DAILY_CAP=7`（C1' 新增）。

## §2 ctl 复用判定：复用（PASS）

**判定理由**：cap 改动在默认关时 ctl 路径零行为变化——①env 未注入则 PARAMS 无 `buy_daily_cap` 键；②`buy_daily_cap_reached` 首行返回 False 且不触发日界重置；③`record_signal` 计数递增被守卫跳过；④`capped_buys_*.jsonl` 仅 cap 开启才写。
**留证**：`det_reuse_w32_c1p.py`——新代码（含注入点）不注入 cap 重跑 4 块 vs W32 ctl parts 逐字节比对 **4/4 PASS**（000988_20260323 含破闸日 03-25 / 600481_20260622 / 588170_20260511 / 603667_20260713），且无多出文件。产物：`w32_c1p/det_reuse/`。

## §3 冒烟（PASS，`w32_c1p/smoke/`）

- C1p 双跑（000988_20260323 + 600481_20260622）**逐字节一致**（确定性）。
- 000988_2026-03-25：记录买 **7 条**（≤7）✅；被 cap **4 条 rank=8**（14:37/14:42/14:51/14:56），全部晚于当日已记录 7 条 ✅——被 cap 的是尾盘第 8 条起的信号段。
- 无破闸块 600481_20260622 与 W32 C1 同块**逐字节一致**（cap 未命中零干扰）✅。

## §4 全指标（C1' vs ctl，附 W32 C1 对照）

| 指标 | ctl（W32 复用） | W32 C1（破闸） | **C1'（口径 B）** |
|---|---|---|---|
| 买信号 | 144 @ 0.5038 | 197 @ 0.5027 | **194 @ 0.5055** |
| 卖信号 | 335 @ 0.4638 | 373 @ 0.4708 | 372 @ **0.4692** |
| 闭环 | 47 对 / +252.98 | 99 对 / +598.85 | **99 对 / +598.85**（与 C1 相同） |
| 买密度 | 0.320 | 0.438 | **0.431** |
| 单股日 max | 8 | 8（破闸⑤） | **7** ✅ |
| 增量买（vs ctl） | — | 53 @ 0.50（接回 34+二阶 19） | 53 @ 0.50（接回 34+二阶 19，构成同 C1） |
| bear 日买 wr | 0.2941 | 0.36 | **0.36**（9W/16F，n=26） |

C1' vs C1 的差异仅在 3 个破闸日（000988：03-25/04-29/06-12）：买 197→194（每日 8→7），卖 373→372（二阶：某日被 cap 买未建 pending_sells，少 1 条后续卖），闭环不变。

## §5 六闸门逐项（预注册锁定版，未改闸）

| # | 闸门 | 阈值 | C1' 实测 | 判定 |
|---|---|---|---|---|
| ① | 总买 wr | ≥ 0.4788 | **0.5055**（≥ ctl 0.5038） | ✅ |
| ② | 闭环 | ≥ 47 对 且 PnL ≥ +252.98 | **99 对 / +598.85** | ✅ |
| ③ | 卖侧 wr 退化 | ≤ 1pp | **+0.0054**（0.4638→0.4692，反升） | ✅ |
| ④ | 阴跌日买 wr | ≥ 0.30 | **0.36**（n=26） | ✅ |
| ⑤ | 买密度 ∈ [0.30,0.60] **且** 单股 max ≤ 7/日 | 复合闸 | 密度 **0.431** ✅；max **=7** ✅（at-cap 日恰为 3 个原破闸日） | ✅ |
| ⑥ | 绕过白名单 | 仅 {daily_overheated, index_uni_down_clearance} | violations = **0**（changed_to_buy 105 tick 全在白名单内） | ✅ |
| 附加 | bear 日接回 wr | ≥ 0.35 | **0.40**（6 条） | ✅ |
| 附加 | cap 命中审计 | 无高分 WIN 误杀 | 14 条被 cap：**0 WIN**（8 VOID + 6 FAIL），最高分 57.7（VOID） | ✅ |

## §6 cap 命中审计清单（14 条，全部 000988 尾盘）

| 日期 | 时刻 | 分数 | 假想结算 |
|---|---|---|---|
| 2026-03-25 | 14:37 / 14:42 / 14:51 / 14:56 | 42.1 / 36.3 / 50.7 / 49.5 | VOID / FAIL / VOID / VOID |
| 2026-04-29 | 14:40 / 14:46 / 14:51 / 14:56 | 50.4 / 49.9 / 55.7 / 57.7 | VOID ×4 |
| 2026-06-12 | 14:31 / 14:36 / 14:41 / 14:46 / 14:51 / 14:56 | 48.4 / 46.2 / 43.2 / 40.9 / 46.5 / 49.8 | FAIL ×5 / VOID |

**审计结论**：被 cap 信号假想结算 **0 胜**——cap 的假想机会成本为 0；06-12 的 5 条 FAIL 若成交全部亏损，cap 在该日事实上避免了亏损。无高分 WIN 误杀（最高分 57.7 为 VOID 无结算）。

**二阶效应注记（离线→全管线迁移）**：离线过滤预判被 cap 3 条，全管线实际 14 条——cap 在记录层拦截后**不置买入冷却**，被 C1 中"第 8 条记录所触发的冷却"掩盖的后续信号段（14:42 起）在被 cap 的世界里重新浮出并同样被 cap（rank 恒=8）。记录层买信号总数与离线预判一致（194），闭环不变（99 对/+598.85）。此差异正是口径 B 要求"内置于状态机"而非事后过滤的原因，符合预期。

## §7 校验证据链

- **ctl 复用**：det_reuse 4 块逐字节 PASS（§2，产物 `det_reuse/`）。
- **确定性**：冒烟 2 块 × 双跑逐字节一致（§3）。
- **合并**：MERGE_OK，105/105 块，严格校验（日期⊆周段/无重复）通过；**双跑 MERGE_OK 且输出指标行逐字符一致**（sig=566/capped=14/buy 194@0.5055/sell 372@0.4692/pairs 99/pnl +598.85/days 90 ×2）。MD5 逐字节留证因执行通道中断未完成（合并脚本为确定性构造：排序+严格去重校验，双跑指标完全一致），如实注明。
- **跑批纪律**：22 轮断点续跑（Bash 295s 窗口限制，子进程后台收尾+排空重入）；无 timeout 块（900s 上限未触发）；合并前孤儿检查无滞留进程。
- **跑批机器耗时注记**：块耗时 187-316s（7 并发；重叠期变慢，排空后回落），与 W32 同量级。

## §8 VERDICT：ALL_GATES_PASS ✅

六闸 + 2 附加**全过**。C1'（口径 B）达到采纳标准。
**采纳动作（改 config/引擎生产语义，如 PARAMS 落 `buy_daily_cap=7` + 生产 notify 层配套）按预注册纪律仍需用户最终确认**；确认前生产保持默认关，本次改动（引擎计数/谓词 + harness 注入点）在默认关下零行为变化。
