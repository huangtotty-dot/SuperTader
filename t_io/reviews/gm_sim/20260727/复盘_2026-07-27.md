# 模拟盘日复盘 2026-07-27（首日）

> **复盘人**：K3（审计员）｜ **数据截止**：14:58（尾盘 14:50-15:00 TAIL 窗口行为未纳入，明日补核）
> **线上版本**：fix8 + 盘中激进建仓补丁（commit `61a19e6`，10:26 盘中提交并重启，run_id `20260727_102651`）
> **输入材料**：事件桥 events_20260727.jsonl（13 条）✅ / backtrace.jsonl（724 事件）✅ / heartbeat.json ✅ / 终端成交导出 ❌ 未提供 / follow_log 无（今日无跟随）/ 用户口述异常 无

---

## 总判定：❌ 红（首日）

红色依据：① 000988 成交回填 price=0.0 → 成本=0 → **保护通道（PANIC/TRAIL/TARGET）对其全部失明过夜**（等同"大跌无止损能力"）；② SELL_HIGH 信号发出后无委托/无成交/无拦截记录的"信号消失案"未归因（等同"拒单未归因"级）；③ 盘中改代码拆闸门上线（流程红线）。

---

## C 区逐项

### C1 执行质量
| 项 | 结果 |
|---|---|
| C1-1 拒单=0 | ✅ 事件桥无 reject |
| C1-2 信号→委托延迟 | ❌ **无法判定**：全天唯一信号（000988 SELL_HIGH 14:45:57 score=71）之后**无 order、无 fill、无拦截事件**——信号消失案（见①-2） |
| C1-3 滑点 | ⚠️ order 记录价 vs fill：600176 39.63→41.07（+3.6%）、603667 50.70→54.08（+6.7%）、600481 3.91→4.20（+7.4%）。注意口径：order 事件记录的是信号 bar 收盘价（市价单无委托价），**真实滑点须以终端成交回报为准**（材料#4 未提供） |
| C1-4 尾盘归位 | ✅ 持仓=base_ref，无超额，无需归位（截至 14:58） |
| C1-5 飞书完整性 | ⏳ 待用户核对（事件桥侧：order×4 / fill×4 / signal×1 / risk×4） |
| C1-6 backtrace 落地 | ✅ 271KB、当日新增 run、724 事件——P-3 遗留项首日合格。附注：08:57/09:27 两个 run 各仅 1 行（启动即重启） |
| C1-7 费用 | ⏳ 事件桥无 fee 字段，待终端导出核实费率 0.00015 |

### C2 信号与通道
| 项 | 结果 |
|---|---|
| C2-1 信号分布 | 全天 1 信号（SELL_HIGH 71 分）。uni_down + 四票 TREND_BREAKDOWN：G2/G3 买入全闸 ✅ 符合设计（首日均值基线，今日即为 D1） |
| C2-2 通道分布 | 仅底仓通道 4 笔；无通道冲突 |
| C2-3 no_signal 可解释 | ✅ 719 条原因链在案 |
| C2-5 亏损高抛 | ✅ 0 笔（无 SELL_HIGH 成交） |
| C2-6 逆势买入 | ❌ **登记 4 笔**：底仓 4 票全部在 TREND_BREAKDOWN 状态建仓（09:30 全部被闸 deferred → 10:26 激进模式强拆闸门后买入） |

### C3 风控状态
| 项 | 结果 |
|---|---|
| C3-1 仓位峰值 | ✅ 总仓位约 7.84 万 / 52%，单票最大 000988 约 22% |
| C3-2 现金水位 | ❌ heartbeat cash=150,000 全天静止（实际已消耗约 7.84 万）——现金监控失真（见①-3） |
| C3-3 止损表现 | ⚠️ 000988 因成本=0，is_deep_loss 恒 False → **PANIC 对其失明**；其余三票成本正常 |
| C3-4 盈利保护 | ⚠️ 同上，000988 的 TRAIL/TARGET 因 profit_pct 无法计算而失明 |
| C3-5 熔断命中 | ✅ uni_down 日 0 信号买入（底仓豁免为设计，但被激进模式放大为四票满仓） |
| C3-6 地板/绞肉机 | ✅ 无卖出 / 无连续割肉 |

### C4 盈亏归因
⏳ 待终端导出。底仓成本合计约 78,400 元（600176 300@41.07 / 000988 300@?（fill价缺失）/ 603667 500@54.08 / 600481 1400@4.20）。

### C5 一致性
| 项 | 结果 |
|---|---|
| C5-1 模拟 vs 回测 | ❌ 与 fix6 验收行为**背离**：回测中 600481 被 pool_gate 拦截、603667 底仓延迟至趋势恢复——今日全部即时建仓。原因 = 代码版本差异（激进模式），属自造环境漂移 |
| C5-3 仅观察纪律 | ❌ 违反：600481（M2 未过票）已建底仓 1400 股 |

---

## 三分类判定与处置

### ① 执行 bug（随时修）
| # | 现象 | 根因方向 | 处置 |
|---|---|---|---|
| ①-1 | **000988 fill price=0.0 → cost=0.0**（heartbeat 实证）→ PANIC/TRAIL/TARGET/R2 成本锚对其全部失效 | on_order_status 取 `order["price"]`，市价单回调该字段为 0；应回退用 vwap/成交均价字段 | **今晚必修，明日盘前完成**：修字段回退 + 手工把 000988 成本更正为真实成交价（查终端成交回报），否则止损失明过夜 |
| ①-2 | **SELL_HIGH 信号消失案**（14:45:57 后无委托/成交/拦截记录） | 最可疑：T+1 锁定（10:26 买入当日 available=0），order_volume 同步抛错被 arbiter 的 try/except 吞掉只剩 print——N25 系列在 LIVE 的首个实案；write_order 在 try/except 内故无桥记录 | 今晚查掘金终端 print 日志定位；修复方向：arbiter 下单失败也写事件桥（`order_failed` 事件），不能只 print |
| ①-3 | heartbeat cash=150,000 全天静止 | heartbeat 取现金字段未从 account() 实时读取（或取了 total 而非 available） | 今晚修：heartbeat 现金改用账户实时 available；明日盘前验证 |

### ② 结构/流程问题（进 backlog / 走流程）
| # | 现象 | 处置 |
|---|---|---|
| ②-1 | **激进建仓模式**（main.py:544-547：`if False and` 拆 M2 闸 + 删趋势延迟块）——在 uni_down 日、四票全 BREAKDOWN 下满仓建仓，且 10:26 盘中提交盘中重启上线 | **流程定性**：拆闸门比改阈值更严重，未走"改→回放→审计"WP 流程、盘中上线，双重违规。**处置二选一**：a) 明日盘前回退 `61a19e6`，600481 底仓平仓回归仅观察；b) 保留既成事实，但本周内补走正式立项（回放验证 + K3 审计），并把"激进建仓"写成带开关的正式机制而非 `if False`。K3 倾向 a)——首日样本不应以拆掉两道刚验收的闸门为代价 |
| ②-2 | C5-3 仅观察纪律被突破（600481） | 随②-1 一并处置 |

### ③ 数值观察（只记录）
- 000988 SELL_HIGH 71 分出现在 14:45（uni_down 日尾盘）——该卖出若成交属"成本未知的盲卖"，数值层面无观察条目。

---

## 明日关注（盘前清单）
1. **000988 成本修复验证**（①-1）：heartbeat cost 非 0、PANIC 触发线恢复可计算——未修复前 000988 等同裸奔；
2. **激进模式去留决策**（②-1）：回退 or 补流程，盘前定；
3. **卖出链路实测**：明日 T+1 解锁后若有 SELL_HIGH 信号，验证能否正常委托-成交-飞书（①-2 的自然实验场）；
4. 终端成交导出（材料#4）纳入复盘输入，核费率与真实滑点。

## 用户补充
无口述异常。C1-5 飞书侧对账待用户回复确认。

---

## 六、今日必修项（具体修改方案 + 验证步骤）

### ①-1 000988 成本失明（P0，今晚修，明日盘前验证）

**修改方案（两处）**：
1. main.py `on_order_status`（约 :899）：`price = order["price"]` 改为回退链：
   ```python
   price = order.get("price") or order.get("vwap") or 0
   if price <= 0:
       price = context.latest_pre_close.get(code, 0)  # 市价单回调price=0时兜底
   ```
2. 一次性成本校正（明日盘前）：reconcile 块（main.py:189-195）增加条件——`mp` 的 `cost<=0` 时允许采用 `gm_pos["cost"]`（gm vwap）覆盖，并写 `reconcile_fix` 事件注明 `cost_fix`；正常情况下保留我方 cost 的逻辑不变。

**验证步骤**：① 明日 09:31 后查 heartbeat.json：`000988.cost > 0`；② 查 backtrace 有 `reconcile_fix` 且 detail 含 cost；③ 人工抽查 no_signal 记录中 000988 的 `profit_pct` 字段非 0 且合理（±10% 内）；④ 终端成交回报核对 vwap 与写入成本一致（材料#4）。

### ①-2 信号消失案（P0，今晚修记录能力，归因待终端日志）

**修改方案**：
1. `_sell_arbiter` 下单 except 分支（main.py:132-134）补事件桥记录：
   ```python
   except Exception as e:
       print(f"[{now:%H:%M:%S}] SELL {code} 失败: {e}")
       try: write_risk(str(now), "order_failed", f"SELL {qty}@{cp:.2f} err={e}", code=code)
       except Exception: pass
   ```
2. `write_order` 的 try/except 同样把异常写入本地（至少 print 出异常内容，不裸 pass）。

**验证步骤**：① 明日任一卖出信号触发后，事件桥必出现 order+fill 或 order_failed 之一（三态闭合）；② 今晚 Pro 从掘金终端控制台导出 14:45-14:50 段 print 日志完成归因（该日志当前不落盘，只能终端内查看/复制——见第七节 L1）。

### ①-3 heartbeat 现金静止（P1，今晚修）

**修改方案**：heartbeat 写入处（main.py 心跳段）的 cash 字段改用 N15 同款账户实时读取（`context.account().cash` dict 取 available），读取失败保留旧值并标注 `_cash_stale: true`。

**验证步骤**：明日 09:31 后 heartbeat cash ≈ 7.16 万±交易变动，且两连续分钟 bar 间随成交实时变化。

## 七、记录充分性评估（周末统一改/验证，现有文件够不够？）

**结论：不够。今日事件链主干齐全（events/backtrace/heartbeat 已归档 evidence/），但有四个缺口会让周末归因死无对证：**

| 缺口 | 后果 | 今晚补救（保证明天有） |
|---|---|---|
| **L1 策略 print 不落盘** | ①-2 信号消失案的唯一归因线索在终端控制台，终端一关即灭失 | ①-2 修复（失败写事件桥）已覆盖未来；**另需 Pro 今晚从终端复制 14:40-15:00 段控制台日志存 evidence/** |
| **L2 真实成交回报缺失** | 000988 真实成本、真实滑点、费率无从核验 | 明日收盘后从掘金终端导出当日成交/持仓，存 evidence/（今日份的需终端历史成交查询补导） |
| **L3 heartbeat 每分钟覆盖** | 盘中现金/仓位时序无法回放 | writer.py 改为同时追加 `heartbeat_YYYYMMDD.jsonl`（一行一快照，不覆盖）；今晚改，明天生效 |
| **L4 backtrace 无按日归档机制** | 多日追加混在一个文件，按 run_id 可区分但易被清理覆盖 | 建立每日 15:05 归档动作：events/heartbeat/backtrace/终端导出 → `reviews\gm_sim\YYYYMMDD\evidence\`（已附 archive_daily.bat） |

**周末统一改/验证所需的最小文件集**：`events_YYYYMMDD.jsonl`（事件桥）+ `backtrace_YYYYMMDD.jsonl`（决策审计）+ `heartbeat_YYYYMMDD.jsonl`（时序快照）+ 终端成交导出（真实 fill/fee）+ 终端控制台日志（print 归因）。L1-L4 今晚补齐后，明天起每天五件套齐全。

## 八、归档与 GitHub

- 今日证据已归档：`E:\06_T\t_io\reviews\gm_sim\20260727\evidence\`（events/backtrace/heartbeat 快照）
- 本报告同步至：`掘金项目\docs\每日复盘\复盘_2026-07-27.md`
- Git 存档：E:\06_T（SuperTader 仓）+ 掘金项目（MyGoldminer 仓）均已 commit 并 push GitHub
- 命名约定（已写入复盘清单 5.2）：`复盘_YYYY-MM-DD.md`，日报目录固定 `docs\每日复盘\`
